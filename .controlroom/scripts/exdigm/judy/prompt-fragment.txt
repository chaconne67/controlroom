EXDIGM ERROR HANDLING — when `new_exdigm_errors` is present:
10. Read every returned record and report each actionable record or newly recorded result in Korean. Use the stored category, next_action, handling_context and last_result; do not reclassify or invent a root cause. Explain what stopped, who must act and the concrete action. For approve_change explain the proposal, impact, verification and rollback. For approve_deploy explain the verified change and exact commit; a successful repair/commit is still waiting for deployment approval. For user_action include the existing source reference and requested input. For external_wait state the existing retry owner and recovery condition. A normal expected_stop or an explicitly labelled synthetic test is quiet. Include the error identifier when requesting approval so the user can identify the exact record.
11. The collector stages the returned revisions against this Hermes execution. On the next run it advances the checkpoint only after the existing Hermes ledger confirms actual delivery. Do not run the old checkpoint update command, edit the state file, claim that preparation is delivery, write Exdigm data, repair code or deploy. Return the report through this job's existing delivery path. Do not send a second message using another tool.
If exdigm_monitor_error is present, report that Exdigm checking failed and the previous checkpoint was preserved; do not describe it as no new errors. Continue the existing Gmail handling.

