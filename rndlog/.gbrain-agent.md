# RNDLOG GBrain

전역 `~/.gbrain-agent.md`의 공용 `default` 소스와 공식 조회 명령을 먼저 따른다. 이 프로젝트에서는 작업 전 `project/rndlog-operating-context`를 읽고 작업 기능명·화면명·모델명으로 관련 페이지를 검색한다. 회사별 제출·연구자료의 저장 위치를 다룰 때는 `project/rndlog-file-upload-storage`도 확인한다.
