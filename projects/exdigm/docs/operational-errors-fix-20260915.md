# 2026-09-15 운영 오류 수정

## 최신 재개 기준 — 2026-09-16 16:24, 항목별 영문 번역 운영 반영

- 최신 사용자 승인은 기존 국문 항목별 추출을 유지하고, 완성·후처리된 각 국문 항목을 따로 영문 번역한 뒤 기존 코드로 조립하는 변경이다. 전체 영문 번역 1회 경로는 제거했다. 기본 추출 구조·항목 분류·PDF 읽기·자기소개 요약·모델·최초 국문 번역 1회의 medium은 유지한다.
- 공식 경로는 기존 원문→국문 기준자료→출처 배정→국문 항목 추출·후처리→항목별 영문 번역→기존 조립·영문 후처리→신원·렌더·저장 경로다. 번역은 각 항목의 지정된 글만 반환하며 코드가 원래 행·순서·계층·조건 구분에 연결한다. 누락·추가·빈 번역·잘못된 형식은 해당 항목에 실제 오류를 한 번 피드백하고, 계속 실패하면 저장 경로로 넘기지 않는다. 별도 검사 모델·원문 재추출·전체 번역 폴백은 없다.
- 날짜 범위·계산된 경력기간·그룹 키는 코드가 유지한다. 시기 칸을 전부 고정값으로 취급한 첫 수정은 `석사 과정 중`을 국문으로 남겨 실패했다. 같은 번역 함수에서 기존 날짜 파서를 재사용해 확인된 날짜 범위는 유지하고 현재 표시는 `Present`로, 나머지 시기 글은 번역하도록 바로잡았다. 같은 실제 사례에서 `During Master's program`, `2021`을 확인했다. 새 호출이나 우회 경로는 추가하지 않았다.
- 실제 PDF 2개의 기존 국문을 고정한 공식 extract_structure 대조에서 국문 내용은 byte-for-byte 동일했다. 번역된 국문 세부 사실·평점·자격·도구·문단·경력 상세를 직접 대조했다. 각각 번역 12항목/2.91초와 14항목/3.03초다. 이는 번역 단계의 측정이지 전체 처리시간 또는 모든 추출 문제의 해결 증거가 아니다.
- 별도 국문 사례는 원문부터 국영문 생성·기존 Word 렌더러까지 전체 공식 경로를 확인했다. 12% 개선, 신입 3명 교육, 자격·발급기관·취득시기, 연봉 7천만 원·성과급 별도·야간 근무 불가·최종 통보 30일 후, 자기소개 보고서 1편과 불확실성 표시가 두 언어에 대응해 남았다. 전체 생성 7.73초, Word 74,639바이트를 확인했다.
- 원본 PDF 전체 경로도 32.55초에 국영문을 생성했다. 다만 국문 출처 배정에서 희망연봉 값의 같은 S 출처를 현재조건과 희망조건에 중복 배정하는 기존 문제가 재현됐다. 번역은 이 국문을 그대로 옮겼다. 출처 배정·기본 항목 추출·문맥 전달·국문 후처리 함수가 시작 판본과 AST 기준 동일함을 확인했다. 이번 번역 변경의 성공으로 이 문제까지 닫지 않는다. 아래 15:32 기록의 급여 재발 닫힘은 이번 새 증거로 다시 열림이며, 소급 성공 데이터는 덮어쓰지 않았다.
- 앞서 학력·면허가 빠졌다는 표현도 구분한다. PDF 텍스트와 국문 기준자료에는 사실이 있고, 일부는 자기소개·핵심역량에 남았지만 학력·자격 표에 배정되지 않았다. 최종 번역의 전체 사실 누락과 같은 문제로 취급하지 않는다. 배정이 그렇게 된 추가 인과관계는 아직 확정하지 않았다.
- 제품 변경은 recommendation_resume_ssp.py 1파일과 관련 테스트 2파일이며 커밋 fb177d6a02f2a5b5ea14ea6440ad679f4331826a이다. 기존 JSON 호출·스키마·병렬 실행·항목 조립을 재사용했다. 번역 계약 fingerprint는 korean_to_english_sections_v1이며 기존 성공 데이터의 일괄 변경이나 재처리는 없다. 기존 캐시는 새 생성 경로에서 계약 구분으로 재사용되지 않는 것을 확인했다.
- 변경 전 보호 검사216개, 수정 후 동일 보호 검사216개, 항목별 번역·시기 변형16개, 직접 영향84개가 통과했다. 확대 검사는256개 통과/9개 기존 ActionType 기준 데이터 실패/3개 제외였으며 이후 시기 검사1개를 추가 통과했다. 기존 실패 검사·기대값을 완화하지 않았다. 보호18파일 중 승인된 번역 테스트만 변경했고 나머지17파일은 동일하다. Django·Ruff·catalog·diff와 Direct Review도 통과했다.
- 승인된 보호 pin/ref는 fb177d6a로 갱신했고 root:root/0444를 유지한다. 이전 pin은 /etc/exdigm/verification-baseline.before-fb177d6a에 같은 권한으로 보관했다. 새 판본에서 공식 contracts 검사216개와18파일 보존, 빌드 이미지 DOC/DOCX/PDF 실제 전달 검사를 통과했다.
- 공식 scripts/deploy/deploy.sh prod가16:24:00 KST prod ok/exit0으로 완료됐다. origin/main·운영 clean main·debug detached HEAD·실행 app/SSE/notification source commit이 fb177d6a로 일치한다. 이미지 exdigm_app:20260916162236, ID sha256:3b4f84b520520bee4756eaa7f9171186b7b912a8ca17fb5f64e7623af58bfcbc다. 서비스5개1/1, 작업자·지원11개 active/jobs0/drain off, HTTPS200을 확인했다. DB 인프라·Hermes·후보자 DB 소급 교정은 하지 않았다.
- 다른 활성 잡코리아 작업의5파일은 소유 작업이 실제 dry를 마친 뒤 허용한 시점에만 보관하고 배포 후 동일 복원했다. tracked4파일 diff SHA256 a2d4a7231e4d8abdcaf43dc016f26bfbde28237a7474d0722fce3b5c117e59c1, 신규 테스트 SHA256 f43ce32760c309c1f71c5b068fd987166aca1736f3ec6b7703328e45734f5bdf가 보관 전후 같았다. 복구 stash8168ce335945a09b09f2fe7e376c1f14149bce60는 보존했고 귀 작업에 새 판본·복원 결과와 재개 가능을 알렸다. 이 코드들은 번역 커밋·운영 배포에 포함하지 않았다.
- 비공개 증거는 원격 debug .debug/section-translation-ca5-v2.json, section-translation-930-v1.json, section-translation-korean-full-v1.json/.docx, section-translation-ca5-full-v1.json이며 모두0600이다. 실제 원문·연락처·비밀값을 문서나 GBrain에 옮기지 않는다.
- 다음 작업은 번역 변경이 아니라 기존 출처 배정 함수에서 급여 같은 출처의 중복 배정과 학력·자격 표 배정 누락을 별도 조사하는 것이다. 기본 추출 프로세스를 재설계하지 않고 최초 이탈 근거부터 확인한다. 이번 범위의 항목·번역 필드 누락 경로는 검증 범위에서 닫힘이며 문장 내 의미 누락·오역 전체의 무발생을 보장하지 않는다.

## 최신 재개 기준 — 2026-09-16 13:58, 급여 지침 보완 회수·PDF 읽기 순서 원인 조사

- 최신 승인은 **급여 항목 배정 지침만 국소 보완**이다. 이 범위로 두 차례 시도했으나 전체 경로의 같은 오배정이 재발해 모두 회수했다. 기본 추출 구조·항목 분류·모델·기존 성공 데이터는 변경하지 않았다. PDF 읽기 순서 옵션의 국소 수정·검증은 새 범위라 별도 승인 질문을 보냈으며, 아직 적용하지 않았다.
- 급여 지침 v1(SSP SHA25681cf1967)의 현재ca5 전체 v4는35.83초/69S로 실행됐지만 희망연봉을 현재 급여에 넣어 실패했다. 지침 위치·표 라벨 대응을 보완한 v2(SHA25649235194)는 고정 국문 자료의 분리 실행에서7.8초로 올바르게 배정했으나, 전체 v5는37.87초/69S로 같은 오배정을 재현했다. 단독 성공을 전체 성공으로 인정하지 않았다.
- v2의 분리 성공과 전체 실패는 원문·extraction_metadata·canonical_source69개가 정확히 동일하다. 재구성한 실제 source-map 프롬프트도11289자/SHA25679035c4da3e7ecd9658b602ee88d75acb445f4be252bde0b12e458ccd0f40f9d로 같다. 따라서 입력 변형 가설은 기각했다. 기존 지침에 이미 라벨·값 연결 규칙이 있었으므로 규칙 부재를 근본 원인으로 단정하지 않는다.
- 실패한 v2 제품 패치와 그 문구 전달만 확인하던 새 테스트 assertion2개를 정확히 역적용했다. 현재 SSP SHA25659c6e7cf4edf13e8b41166ca0c9291fce92d202046f325627fddc979a4432aa9이며, 기존 번역5파일만507추가/134삭제 상태다. v3 번역 분리·최초 국문 번역 medium·기존 SECTION_RULES/요약/신원/저장 함수는 보존했다. 이 판본도 전체 급여 품질은 미완료이므로 배포 후보로 승격하지 않는다.
- 보완 전 지침으로 과거 저장 국문106S와 현재 국문69S를 같은 공식 create_source_map에 한 번씩 넣는 대조를 했다. 각각7.40초/6.14초에 모두 희망 값의 현재 급여 오배정을 재현했다. 증거는 `.debug/translation-fidelity-20260916-compensation-baseline-contrast-v1.json`(0600)이다. 따라서 새 번역 분리만을 원인으로 볼 수 없다. 과거 저장된 두 Knowledge/Resume의 해당 급여는 희망 조건으로 올바르게 보관되어 있다.
- 원본 PDF는 운영의 기존 캐시를 읽기 전용으로 열었다. PDF SHA2568e113105060d56da852bcd9160b9c2e22eb5be2796bd6fe87af4f1c4bf63bc10,4쪽이다. 첫 쪽 화면에서 희망연봉 제목과 값은 같은 카드 안에 있으나 기존 `extract_pdf_text_with_profile`의 `page.get_text()` 결과에는 값이 먼저, 제목은 소개문·인적사항 뒤에 나온다. 이 반환 텍스트의 SHA25609605dc7cb34a70b868a4a77a1ad4827b33830522c307302de3079ec419fe26a는 운영 저장 원문과 정확히 같다. PDF 메타데이터에는 해당 카드의 위치 관계가 없다.
- 같은 원본을 라이브러리의 `get_text(sort=True)`로 읽은 비교에서는 네 카드 제목 행과 값 행이 바로 이어졌다. 이는 원문 읽기 순서의 차이에 대한 직접 증거이지 전체 추출·번역·급여 배정 성공 증거가 아니다. 기존 PDF 읽기 옵션만 보완하는 후보이며, 승인 전 제품 코드·테스트·추출 캐시 계약은 변경하지 않는다. 별도 AI 검사기·후처리 급여 이동·새 파이프라인은 만들지 않는다.
- 개발 Drive OAuth로 원본을 받으려던 첫 조회는 `invalid_grant`로 실패했고 파일 생성 전 중단됐다. 인증을 교체·우회하거나 운영 비밀값을 개발에 복사하지 않았다. 이미 보관된 동일 원본 캐시를 읽어 화면 조사를 마쳤으므로 이 인증 실패는 현 원인 확인을 막지 않는다. 새 인증 요청은 하지 않았다.
- 회수 후 공식 보호대상6파일211개와 관련25개, 총236개 독립 검사를 다시 통과했다. Django/Ruff/diff 검사와 코드 카탈로그 current/local.valid=true/broken=[]도 통과했다. 기존 전체 local_worker 기준 데이터 부재9건은 여전히 별도 미해결 기준선이며 검사 삭제·완화는 없다. 캐시 구판 거절/신판 재사용과 기본 추출·신원·저장 함수 및 SECTION_RULES의 HEAD 일치도 재확인했다.
- 기존 후보자·파일·원문·Resume·Knowledge·하위 경력/학력/자격/어학·Application이 저장 전 비공개 백업과 다시 모두 정확히 일치했다. 보호18파일 중 승인된2테스트만 달라 나머지16개와 제외 설정 부재는 유지했다. pin/ref6ac315e9, debug detached/운영 clean main2620b6fc는 그대로다. 제품 커밋·push·운영 배포·DB 쓰기·ca5 재처리는 없다.
- 다음은 **PDF 읽기 순서 국소 보완 승인 여부 확인**이다. 승인 시 원래 실패 PDF와 다른 기존 성공 PDF의 수정 전 기준선·추출 내용/필드/기간 보존을 먼저 잠그고, 기존 함수 옵션의 최소 변경만 적용한다. 같은 공식 전체 생성에서 원래 실패·기존 성공·표 변형을 대조하고 회귀가 없을 때만 번역 변경과 함께 커밋·보호 기준·배포·원래930 복구 단계로 진행한다. 미승인 시 변경하지 않고59c6 상태와 증거를 유지한다. 재발 판정은 **열림**, 읽기 옵션의 전체 효과는 **미검증**이다.

## 최신 재개 기준 — 2026-09-16 13:36, 영문 분리 검증·급여 배정 국소 보완 승인

- 최신 사용자 승인은 완성된 국문만 입력받는 영문 번역 1회 분리와, 검증 중 발견한 급여 항목에서 제목·값을 함께 선택하도록 하는 기존 배정 지침의 국소 보완이다. 기본 데이터 추출 구조·항목 체계·자기소개 요약·모델·신원/저장 경로는 유지한다. 전체 추출 재설계, 별도 AI 검사기, 전체 medium은 금지한다.
- 공식 경로는 기존 원문→국문 기준 자료→기존 출처 지도→국문 항목 작성→기존 정규화→영문 번역 1회→기존 신원/형식/렌더/저장 경로다. 영어가 별도로 원문에서 내용을 골라 쓰던 지침은 제거하고 한 번의 번역 프롬프트로 옮겼다. 최초 원문 국문 번역만 medium이며 새 영문 호출을 포함한 나머지는 기존 기본값이다.
- `knowledge_fingerprint`에 번역 계약 구분을 추가했다. 기존 저장 캐시의 재사용 함수로 과거 fingerprint 거절·새 fingerprint 재사용·반복 일치를 읽기 전용 확인했다. 기존 캐시/성공 데이터는 일괄 수정하지 않았다.
- 분리 v1에서 원래930은51.15초/60S, 현재ca5는37.30초/68S로 실제 국영문을 생성했다. 두 언어의 면허·장학금·도구·경력 및 국문 자기소개 요약 대응을 직접 확인했다. 날짜 이후를 strictly after로 옮긴 양언어 샘플은 v2에서 on or after로 보완했다. 원문 four weeks after를 최초 국문 번역이 이내로 옮긴 별도 사례는 번역 책임 맥락과 시간·의사·단복수 보존 지침을 명확히 하여 수정했다.
- v3 코드 SHA25659c6e7cf4edf13e8b41166ca0c9291fce92d202046f325627fddc979a4432aa9의 실제 실행: 원래930 31.32초/63S, 현재ca5 36.94초/69S, 단일 외국어 샘플17.49초/18S, 양언어18.47초/11S. 단일 샘플은 오퍼 수락4주 후·국내 이전 의사·논문1편과 두 학위/GPA/면허/도구를 국영문으로 유지했다. 양언어는 고유 자격·파견 불가·입사 가능 기준일을 보존했다. 합성 기관명의 영문 음차 차이는 정확한 영문 표기 보존과 구분한다.
- 현재ca5 v3 전체 품질은 통과가 아니다. 원문/국문 기준에는 희망연봉 제목과 회사내규 값이 있지만 source_map이 값 S0005를 현재 급여에 넣고 제목 S0025는 선택하지 않았다. 이후 국문·영문은 이 잘못된 분류를 따랐다. 이것은 번역 분리 성공만으로 전체 추출 품질을 닫을 수 없는 별도 경계다. 기존 성공 ca5는 덮어쓰지 않았다.
- 주인님은 이 급여 배정 지침만 국소 보완하도록 승인했다. d8_source_map_rules의 기존 보상조건 안내에 값과 구분 라벨/제목을 함께 선택하고, 떨어진 근거는 각각의 기존 range로 연결하도록 한 줄 추가했다. 기존 D map 구조·분류 항목·변환 함수·SECTION_RULES·create_extracted_content·신원/저장 함수는 변경하지 않았다. 기존 source-map 피드백 검사에서 지침 전달 RED4/GREEN4를 확인했다.
- 현재 SSP SHA25681cf19675d50027c57e8061640843fabcaaada8cfdf418f73c802d4994ba4070이다. debug detached HEAD와 운영 clean main/GitHub는2620b6fcb1e0075220353e9965147c4198f889cd다. dirty는 기존5파일만510추가/134삭제. 코드 카탈로그 current/local.valid=true, broken_references=[]다. 현재ca5 공식 전체 v4 검증 실행 중이며 결과를 읽기 전 품질 통과로 판정하지 않는다.
- v3에서 고정 보호 대상6개 검사 파일의211개와 관련25개, 총236개 독립 검사를 통과했다. 기존 전체 local_worker의 submit_to_pm 데이터 부재9건은 고치거나 제외 기준을 바꾸지 않았다. 보호18파일 중 승인된 tests/test_llm.py와 tests/test_resume_knowledge.py만 다르고 나머지16파일과 제외 설정 부재는 유지한다. pin/ref는6ac315e9 그대로다.
-13:29 공식 shell-readonly에서 후보자·파일2개·원문2개·Resume2개·Knowledge2개·하위 경력/학력/자격/어학·Application이 저장 전0600백업과 모두 정확히 일치했다. 해당 백업 SHA2568a40da248453d4272dc0bf739a13a20c36b2e231cb03dece46bc20fc3a759b8d 유지. 원래930 v3 결과는 기존 최신성 판정에서 current ca5를 교체하지 않는 것으로 확인했다. 운영 DB 쓰기·제품 커밋·push·배포·보호 pin 변경은 아직 없다.
- 다음: 현재ca5 v4 전체 결과 대조→변경 지침을 고정한 기존 성공/원래930 및 조건 변형 검증→직접 diff 리뷰와 동일 보호/관련 검사→검증된5파일 커밋·기존 승인된 보호 pin/ref 갱신·공식 contracts/prod→백업 정확 일치 재잠금 후 원래930만 공식 저장 경로 복구 및 재조회. 현재ca5·85건·사람인 완료 건 재처리 금지. 의미/분류 결함이 남으면 배포하지 않고 최초 이탈 경계를 다시 확인한다.

## 최신 재개 기준 — 2026-09-16 12:57, 기존 추출 보존·영문 번역 1회 분리 승인

- 주인님은 자기소개서는 요약해도 되고 오히려 요약이 낫다고 명시했다. 자기소개 요약을 원문 번역 누락과 혼동하지 않는다. 기본 추출 구조·항목 분류·자기소개 요약 규칙은 유지한다.
- 주인님은 기존 동시 영문 작성을 제거하고, 완성된 국문만 입력받는 영문 번역 1회 호출로 분리하는 변경을 승인했다. 별도 AI 검사 호출, 원문 재추출, 전체 사실 구조 재편, 모델 변경은 승인 범위가 아니다. 첫 원문 국문 번역만 medium이며 다른 호출의 기본 설정은 유지한다.
- 이번 국문 번역 수정은 물리적 원문 줄을 번역 단위로 강제하지 않고, 기존 단일 S 레코드와 original_source_ids 목록에 의미 단위의 연속 원문 범위를 연결한다. 기존 source_id_range_inclusive를 재사용하며 새 상품 함수·클래스·파일은 아직 추가하지 않았다. 실제 양언어의 중복은 대응 출처와 근거를 남기며, 고유 원문이나 재번역 실패 행을 버리지 않는다.
- 기준선83개 통과 후 RED26실패/9통과, GREEN35통과를 확인했다. 현재 관련 독립 검사107개(82+25), Ruff, diff, Django, catalog가 통과한다. 전체 local_worker 검사는 기존 submit_to_pm 기준 데이터 부재9건 실패/32건 통과이며 기대값을 완화하거나 고치지 않았다.
- 실제 원래930의 공식 create_canonical_source는32.70초,128L→60S, 제외0으로 완료됐다. 원문 전체를 직접 대조했으며 면허·장학금·도구와 앞서 줄 중간에서 끊겨 오역된 문맥이 보존됐다. .debug/translation-fidelity-20260916-original-930-meaning-canonical-v1.json(0600)이 증거다.
- 같은 경로의 전체 지식 생성은 원래930 40.89초/60S, 현재ca5 29.27초/68S, 양언어 회귀17L→10S/근거 있는 중복7개/12.83초였다. 면허·장학금·도구 등은 국영문에 반영됐지만, 일부 자기소개에서 영문이 국문보다 원문 세부 사실을 더 작성했다. 자기소개 요약 자체는 정상으로 판정하며 문서 전체와 표별 배치를 분리한다. 현재ca5의 기존 운영 결과는 재작성하지 않고 보호 기준으로 유지한다.
- ‘한 응답 안에서 국문을 먼저 쓰라’는 언어 지침 수정과 후속 중복 지침 정리만으로는 영문이 완성된 국문만 번역한다고 입증하지 못했다. 후속 실제930 v2는42.90초/60S로 생성됐지만 세부 차이가 남았다. 해당 최종 작성 지침 수정과 새 테스트는 회수했고, 모든 모듈 수준 추출·언어·요약 규칙 및 section_content_prompt를 HEAD와 동일하게 복원했다.
- 회수 전5파일 복구 사본은 stash ba5b2d1680de7e3ce36873efdcc56899666b952f(translation-meaning-before-language-rollback-20260916)다. 기존 사본과 비공개 산출물은 보존했다. v1 코드 SHA256686710ec22da5423ef7edc6acfed794098f790ee2e9bfdea6c7e34870b3d02ae, 회수된 v2는e81e48042346f56a5dc57c12742fac7300b476f8ca89406d183d9c3ed639a536다.
- 현재 debug detached HEAD와 운영 clean main은2620b6fcb1e0075220353e9965147c4198f889cd다. 미커밋5파일은 common/llm.py, recommendation_resume_ssp.py, test_llm.py, test_resume_knowledge.py, test_recommendation_resume_local_worker.py이며351추가/126삭제다. SSP SHA2569ff866a5decdb794f707ef56d241206069ed6f69cff95b097e4776b963ce4f6c다. AST로 모듈 상수 변경0, 새/삭제 함수0, 원문 지도·기본 추출·항목 작성·fingerprint 함수 원상 보존을 확인했다.
- 운영 DB 쓰기·커밋·push·배포·보호 pin 변경은 이번 번역 작업에서 하지 않았다. 모든 실제 생성은 shell-readonly의 exdigm_debug_ro/read_only on으로 실행했다. 현재 보호 pin/ref6ac315e9와 운영 저장 전 비공개 백업8a40da248453d4272dc0bf739a13a20c36b2e231cb03dece46bc20fc3a759b8d를 보존한다.
- 다음 변경은 기존 extract_structure에서 항목 작성에 국문 양식만 전달하고 기존 정규화를 거친 뒤, 같은 모델의 영문 번역을 한 번 호출하는 연결이다. create_extracted_content의 항목별 병렬 추출·출처 배정·요약 규칙은 변경하지 않는다. 영어 작성 지시는 기존 동시 작성 위치에서 번역 책임으로 옮기고, 기존 형식 검사·신원 보호·지식/후보자 저장을 재사용한다. 새 번역 방식은 기존 fingerprint에서 구분해 요청 시 오래된 생성 결과를 그대로 재사용하지 않되, 일괄 DB 재작성은 하지 않는다.
- 다음 검증은 공식 진입점에서 국문 정규화 결과만 번역 입력으로 전달되는지, 번역 호출1회·기존 국문/요약 불변·오류 시 저장 중단, 실제 원문·기존 정상/양언어 자료의 두 결과 대조다. 원문 국문 번역의 개선과 최종 국영문 동등성·운영 저장 완료를 별도로 판정한다. 새 검사기·추출 구조 전면 재편·85건 재대기 금지.

## 최신 재개 기준 — 2026-09-16 12:11, 번역 실험 회수·국문 정규화 제안 검토

- 기본 추출 구조·검증된 지침을 바꾸지 않는다는 최신 지시를 유지한다. 앞선 통합 사실 구조, 공통 추출 지침 교체, 전체 medium 실험은 재개하지 않는다. 주인님의 최신 질문은 `국문을 정규화해서 번역하면 어떤가`이며, 새 구조 구현 승인이 아니라 방향 검토로 처리했다.
- 시작 시 broad draft 5파일605추가/601삭제는 d9b192f5c6683ea785c840c76444f941470dc655와 정확히 같았다. 이를 보존한 채 SSP와 관련 테스트2개를 HEAD로 되돌렸다. 기존 승인된 common/llm.py의 추론 설정 전달6줄과 tests/test_llm.py의21줄은 유지했다.
- 이후 기존 언어 분기와 추출 지침을 유지하는 작은 번역 수정안을 시험했다. 대상은 한 벌/두 벌 판정 설명, 고유 원문 누락과 중복 대응 근거, 누락 번역만 한 번 복원한 뒤 명시적 실패, 첫 번역 호출만 medium이다. 함수 추가/삭제와 모든 모듈 수준 추출 규칙 변경이 없음을 AST로 확인했다. 이 수정안도 아래 실제 품질 실패로 현재 작업 트리에서는 회수했다.
- 기준선72개 통과, 추가 계약 검사 RED16실패/9통과 후 GREEN25통과, 관련 독립 검사 총97개와 Ruff/diff 검사가 통과했다. 자동 검사 통과를 번역 품질 완료로 판정하지 않았다.
- 원래930의 공식 create_canonical_source를 읽기 전용으로 실행한 minimal-canonical-v1은17.78초, 원문128행→국문128행, 제외0이었다. 면허·장학금·도구는 복원됐지만 PDF에서 끊긴 단어의 뒷조각이 별도 의미로 오역되고 여러 문장이 반복됐다. 최초 이탈은 기본 항목 추출에 들어가기 전 번역 단계다.
- 원문 줄을 문맥으로 번역하고 같은 개수의 출력 줄에 나누도록 기존 번역 문구만 교체한 v2는18.82초,128행/제외0이었다. 앞선 단어 오역은 없어졌지만 긴 자기소개 뒤쪽에서 번역 내용과 원문 줄 번호가 밀리고 뒤쪽 서명 내용이 반복됐다. 따라서 품질 실패이며 국문 번역 완료나 재발 닫힘으로 판정하지 않는다. 전체 구조화 추출·DOCX·운영 저장까지 실행하지 않았다.
- v1 복구 사본은6aabdd341bf7f66df682ca5a7b544aad9d1e8d94, v2 복구 사본은28eaea492788d858f2a1a1e2c6ae5e96d6ca205c다. 두 사본을 stash에 보존한 뒤 이번 실험의 정확한 patch만 역적용했다. 다른 세션 작업과 기존 stash·비공개 산출물은 삭제하지 않았다.
- 현재 debug detached HEAD와 운영 clean main은2620b6fcb1e0075220353e9965147c4198f889cd다. debug의 변경은 common/llm.py와 tests/test_llm.py 두 파일27줄 추가뿐이다. SSP·test_resume_knowledge·test_recommendation_resume_local_worker는 HEAD와 정확히 같다. 회수 후 기준선72개와 diff 검사를 다시 통과했다. 이번 커밋·push·배포·운영 DB 쓰기는 없다.
- 실제 증거는 debug .debug/translation-fidelity-20260916-original-930-minimal-canonical-v1.json 및 v2.json(0600)에 있다. v1 SSP SHA2562967942234ae98fe2424f58bcfdb8d64c8ff1c4d2c3d3052ccbac2a326426ee6, v2 SHA256154a1df48689d419d8d9a4bb1de945b333a3f8af90e651cde0fd54ab20caed48다. 후보자 원문·연락처를 문서/Git/GBrain에 복사하지 않는다.
- 코드에서 확인한 현행 경로는 원문→국문 기준 자료→기존 출처 지도→항목별 국영문 동시 작성→정규화/검증→출력이다. 항목 작성 지시는 동일한 국문 근거에서 국문과 영문을 각각 채우는 방식이며, 완성된 국문 결과 자체만 번역하라는 계약과는 다르다. 현재의 normalize_extracted_content_for_rendering은 두 언어 작성 뒤 형태·순서·날짜 등을 정리하며 의미 번역을 하지 않는다.
- 국문 정규화 제안은 내용을 다시 추출하거나 요약하는 작업과 구분해야 한다. 기존 국문 내용을 원문에 충실하게 정돈하고 영문을 그 내용에 대응시키는 것은 검토 가능하지만, 영문 원문의 첫 국문 번역 오류까지 자동으로 해결한다고 주장하지 않는다. 특히 물리적 줄 수를 번역 문장 수로 강제하는 계약의 영향과 출처 연결 보존을 먼저 확인해야 한다. 기존 추출 단계·규칙·모델·저장 구조 변경 없이 번역 경계 안의 최소 변경만 검토한다.

## 최신 원인 분리 — 2026-09-16 02:48, 기본 추출 구조 변경 금지

- 주인님은 기본 데이터 추출 구조·검증된 지침·기존 정상 흐름을 바꾸지 말고, 번역 문제인지 기존 추출 문제인지 먼저 정확하게 판정하도록 명시했다. 번역 문제는 기존 번역 경로의 실패 부분만, 추출 문제는 기존 흐름 안의 국소 보완만 대상이다. 앞선 통합 사실 구조 재편 제안은 철회 상태를 유지하며 아래 과거의 전체 구조/규칙 통합 계획을 재개하지 않는다.
- 이번 작업은 원인 재판정이다. 제품 코드 변경·되돌리기·커밋·push·배포·운영 DB 쓰기는 하지 않았다. 새 LLM 호출도0회이며 기존 운영 산출물과 실패 기록, 배포 코드의 순수 함수만 읽기 전용으로 대조했다.
- 최초 실패는22:44:21 KST의 `canonical source base translation item 4 missing text`다. 원문 텍스트는 있고 완성 구조화 결과는 없었다. 해당 빈 번역을 오류 피드백으로 다시 생성하는 보완은22:51 이후03116556에 포함돼 현재 운영에도 있지만, 그 기술적 중단 대응을 내용 보존 완료로 판정하지 않는다.
- 원래930의 원문128행에는 조사 대상 사실이 모두 있다. 면허 L0117, 장학금 L0108, 도구 L0116은 저장된 국문 기준 자료에서 제외돼 최종 국영문 본문에도 없다. 최초 이탈은 파일 텍스트 추출이 아니라 번역·기준 자료 선별 경계다. 혼합형 정규화는 기준본/추가번역에 선택되지 않은 원문을 대응 근거 없이 중복 제외로 처리한다. 배포 코드 그대로의 작은 재현에서도 고유 사실2개가 제외된3행→1행 결과가 통과했다. 최초 모델의 혼합형 판단 이유는 응답이 없어 미검증이다.
- 반면 두 학점은 국문 기준 자료 S0008에 번역돼 있고 기술 항목은 S0045에 남아 있었다. 저장된 출처 지도는 두 근거를 핵심역량에만 연결했고 학력/기술 항목에는 전달하지 않았다. 최종 핵심역량은 요약4항목으로 작성돼 해당 구체 사실을 보존하지 않았다. 이 부분은 번역 전 원문 누락이나 미번역이 아니라 후속 항목 배정·작성의 국소 누락이다. 기존 지침에 사실 공유/기술 보존 규칙은 이미 있으며 모델이 해당 배정을 선택한 세부 이유와 최소 보완의 효과는 아직 미검증이다.
- 현재ca5의 기존 운영 결과에는 같은 주요 사실이 이미 국영문으로 존재한다. 두 원문은 정규화 후128행이고 다른 행은 L0017/L0018/L0091뿐이다. 제외 행 개수나 특정 표의 항목 개수만으로 전체 내용 소실을 판정하지 않는다. 문서 전체의 의미 보존과 개별 표의 배치는 따로 확인한다.
- 미커밋 초안은 번역 경로 통합뿐 아니라 공통 출처 배정 규칙 교체, 공통 작성 규칙과 추론 적용 범위까지 확장돼 있다. 전체 처리 순서를 교체한 것은 아니며 source_records/create_source_map/create_extracted_content/section_content_prompt/d_map_to_source_map의 본체는 배포 코드와 같지만, 이 사실이 초안의 영향 범위가 작다는 뜻은 아니다. 검증된 기본 규칙을 대체하는 이 초안을 그대로 배포하지 않는다.
- 운영 clean main과 debug detached HEAD는2620b6fcb1e0075220353e9965147c4198f889cd다. 개발에는 기존5파일만605줄 추가/601줄 삭제 상태로 남고 복구 사본 d9b192f5c6683ea785c840c76444f941470dc655를 유지한다. 아직 초안 축소나 되돌리기는 실행하지 않았다. 다른 작업의 사람인 배포·복구와 기존 stash를 보존한다.
- 이번 공식 shell-readonly 재확인에서 후보자, 두 파일과 원문, 저장 이력서/지식, 하위 경력·학력·자격·어학, Application이 비공개 저장 전 백업과 모두 정확히 같았다. 번역/추출 품질 완료나 재발 닫힘을 선언하지 않는다. 다음 구현은 기존 경로 안의 번역 결함과 별도 입증된 국소 추출 결함을 분리해 최소 차이로 다시 잠근 뒤 검증해야 하며, 전체 생성 구조 재편·공통 추출 지침 교체·묶음 재처리는 금지한다.

## 최신 재검토 결론 — 2026-09-16 02:34, 수정 범위 확대 중단

- 주인님은 전체 구조 재편을 승인하지 않았다. 기존에 문제가 이 정도로 크지 않았는데 왜 계속 많은 수정을 하는지 방향을 다시 검토하도록 요청했다. 이후 제품 코드 변경·되돌리기·커밋·push·배포·운영 자료 저장을 멈추고 기존 산출물과 실제 diff만 읽기 전용으로 대조했다. 바로 앞서 제안한 추출/생성 구조 재편안은 철회한다.
- 검토 결과, 실제 내용 소실과 특정 표에 별도로 배치되지 않은 문제를 같은 실패로 묶어 수정 범위를 과도하게 넓혔다. 원래930의 운영 결과에는 면허·장학금·EndNote·Mendeley 등이 국영문 본문 전체에서 빠져 있어 수정이 필요하다. 그러나 현재ca5의 기존 운영 결과에는 이 사실들이 이미 핵심역량·학력·수상·기술·자기소개에 존재한다. 현재ca5가 완벽하다는 판정은 아니지만, 원래930과 같은 내용 소실 상태라고 일반화할 수 없다.
- 정정: 전체 medium v14의 학력 표에는 학위1개만 있었지만, 두 원본 모두 국영문 자기소개에는 학위2개와 두 성적이 남아 있었다. 원래930의 면허도 자기소개에는 남아 있었다. 이를 이력서 전체에서 학위/면허가 없어졌다고 보고한 것은 부정확했다. 표별 분류 개선과 번역 내용 보존은 별도 판정해야 한다.
- 원래930의 v6/v9에서 주요 번역 사실은 이미 복원됐다. 이후 남은 일부 기술 항목과 언어 표현, 표별 배치 변화를 이유로 사실 배정 규칙 전체와 추론 적용 범위까지 계속 바꾼 것은 최소 수정의 필요성이 충분히 입증되지 않은 확대였다. 줄 수의 순감소는 변경 책임 범위가 작다는 증거가 아니다.
- 전체 medium은 일회성 읽기 전용 설정으로 시험했다. Shape의 한 벌/두 벌 중복 예시를 맞춘 뒤 두 실제 원문은128행 보존/제외0으로 각각62.67초,62.23초에 생성됐다. 그러나 항목별 배치 차이와 일부 실제 기술 항목 누락은 남았다. 전체 medium 또는 새 구조가 근본 해결이라고 판정하지 않는다. 작은 세 형태 표본도 생성됐지만 이번 재검토에서 전체 의미 품질 통과로 승인하지 않았다.
- 단순히 모든 원문을 개별 항목 호출에 전달한 별도 진단은 학위·면허를 가져왔지만, 근무기간을 학업기간으로 섞는 문제가 있어 채택하지 않았다. 해당 full-context-probe 파일은 진단 자료일 뿐 공식 생산/저장 성공 증거가 아니다.
- 현재 debug/prod/GitHub 기준은2620b6fcb1e0075220353e9965147c4198f889cd다. debug에는 common/llm.py, recommendation_resume_ssp.py, test_llm.py, test_resume_knowledge.py, test_recommendation_resume_local_worker.py의5파일만 미커밋이다. 전체605줄 추가/601줄 삭제이며, 최신 실험까지 담은 복구용 stash d9b192f5c6683ea785c840c76444f941470dc655를 만들고 작업 트리와 정확 일치를 확인했다. 이름은 translation-medium-unverified-20260916이다. 이 사본은 검증된 배포 커밋이 아니다. 앞서 예고한 실험 되돌리기는 아직 실행하지 않았다.
- 기존 stash90f00688, a98dba4c, bf49f100,4f14aa73과 비공개 원문/실험 산출물을 모두 보존한다. 마지막 관련45개 자동 검사 통과를 번역 품질 완료로 대신하지 않는다. 보호 pin/ref는6ac315e9를 유지하고 배포 적합성 판정은 보류한다.
-02:34 공식 shell-readonly에서 exdigm_debug_ro/read_only on을 확인했다. 후보자, 두 FileData, 원문, 저장 이력서, 지식, 하위 경력/학력/자격/어학, Application이 저장 전 비공개 백업과 모두 정확히 일치했다. 이번 번역 실험은 운영 DB를 바꾸지 않았다. 사람인 별도 배포2620b6f는 그대로 유지한다.
- 권고 방향은 기존 공식 경로를 유지하고 원래 실패한 번역의 잘못된 언어 분류·근거 없는 제외·빈 번역의 보정에 필요한 차이만 다시 추리는 것이다. 현재 사용 중인 자료는 결함이 별도로 입증되지 않는 한 재작성 대상이 아니라 기존 성공 보호 기준으로 둔다. 새 모델·추가 검사기·전체 생성 구조 재편이나 묶음 재처리를 진행하지 않는다. 주인님의 재검토 요청에 대한 보고가 우선이며, 최종 축소 패치는 아직 작성하지 않았다.

## 번역 최신 재개 기준 — 2026-09-16 02:22, 전체 medium 실제 비교 진행 중

- 목표는 오류 검사를 통과시키는 것이 아니라 원문에 충실한 동일 내용의 국문·영문 결과를 실제로 만드는 것이다. 최소 코드·SSP를 유지한다. 최신 사용자 지시 `추론 수준을 미듐으로 올려서 해봐라`에 따라 이 이력서 처리의 기존 호출 전체를 medium으로 설정한 읽기 전용 비교를 진행 중이다. 다른 제품·운영 설정은 바꾸지 않았다.
- 운영과 GitHub는 clean main2620b6fcb1e0075220353e9965147c4198f889cd다. debug detached HEAD는 같은 base이며 번역 관련5파일만 미커밋이다. 이번 번역의 커밋·push·배포·대상 DB 저장은 아직 하지 않았다. 다른 작업의 사람인6건 복구/15건 원본 부재 종료와 stash는 보존한다.
- 아래01:30의 v6 대조는 전체 내용 보존 완료 증거로 사용하지 않는다. 추가 대조에서 원래930의 원문 기술 항목이 출처 배정과 양언어 출력에서 빠지는 것을 확인했다. v6/v9의 지도는 같았으므로 그 누락은 당시 결과에도 존재했다.
- v10은 기존 기술 규칙을 배정 단계에 연결해930의 누락 항목을 복원했지만, 현재ca5에서는 자기소개에 남은 실제 면허를 자격 목록으로 전달하지 못했다. 해당 결과는 저장·배포하지 않았다. 최종 항목별 작성 규칙과 별도로 중복된 사실 배정 규칙을 기존 SECTION_RULES 재사용으로 통합했다. 새 호출·모델·별도 검토기를 추가하지 않았다.
- v12는 번역 완성과 출처 배정만 medium으로 실행한 결과다. 면허는 복원됐으나 기술 항목 누락과 일부 영문 직함의 국문 잔존이 있어 최종 품질 완료로 판정하지 않았다. 제품5파일 전체 diff는 이 시점에601줄 추가/601줄 삭제이며 제품 소스만217줄 순감소였다.
- 전체 D8 호출을 medium으로 설정한 v13 읽기 전용 실험은 실제 두 원문 모두 한 벌짜리 원문에 중복 제외 목록을 생성해 canonical 계약에서 중단됐다. 원문 제외 검사는 완화하지 않았다. 같은 생성 함수에서 언어 분류 결과가 한 벌이면 기존 Shape의 중복 목록을 비워 보내도록3줄을 추가했다. 두 벌의 실제 국영문 이력서의 근거 있는 중복 계약은 유지한다.
- 현재 SSP SHA256은 a29ad391f8cfd81dd6423ef9b1f0e29572515c74859131a96de4b466f096e334, common/llm.py는02ad4d8bedcf4e44a9a6d15ce44c3d635f5c29651a1e2d3b371684372bbe501f다. 실제 제품 코드의 강제 medium은 아직 완성/배정 두 호출이며, 전체 medium은 일회성 RO 프로세스의 기존 RECOMMENDATION_RESUME_D8_REASONING_EFFORT 설정으로 비교한다. 결과를 확인하기 전에 운영 기본값까지 적용했다고 보고하지 않는다.
- 최신 관련 검사는45개 통과/80개 제외다. source-map 정상/실제 오류 피드백8개는 같은 공통 규칙과 Gemini medium 전달, 다른 provider 보존을 확인한다. 한 벌짜리 Shape의 빈 중복 목록 검사는 수정 전 실패/수정 후 통과했다. 마지막210개 확대 검사는 규칙 통합 직후 판본의 결과이므로 최신 코드의 최종 검사는 다시 필요하다. 기존 submit_to_pm 기준 데이터 실패9개를 삭제·완화하지 않는다.
- 실제 두 원문의 v14 전체 medium 결과를 기다리는 중이다. 증거는 debug .debug/translation-fidelity-20260916-*-all-medium-v14.json에 성공한 경우에만0600으로 생성된다. 오류 없이 끝난 사실과 내용 대조 통과는 구분한다. 후보자·원문 내용은 문서/Git/GBrain에 복사하지 않는다.
- root 검증 pin과 승인ref는6ac315e930880406f6f7f9d489322b07b85e380f를 유지한다. 승인된 보호 변경은 tests/test_llm.py, tests/test_resume_knowledge.py 두 파일뿐이다. 나머지16개 및 검사기/manifest는 변경하지 않는다.
- 저장 전 백업 .debug/operational-errors-20260916-before-faithful-translation-save.json(0600, SHA2568a40da248453d4272dc0bf739a13a20c36b2e231cb03dece46bc20fc3a759b8d)을 유지한다. 검증된 결과만 공식 persist_pending_resume_knowledge → process_resume --output candidate → mark_file_status_llm_extracted 경로로 저장할 수 있다. 쓰기 직전 원본/기존 두 이력서/후보자/하위자료의 정확 일치를 잠그고930 먼저, 현재ca5 나중 순서로 한 transaction에서 처리한다. 현재 포인터·신원·원문·기존 이력을 보존한다.
- 다음: v14의 실제 원문·두 언어 내용 대조 → 필요한 생산 계약의 최소 수정 → 같은 최종 코드의 실제 두 원문/세 언어 형태/회귀 검사와 직접 리뷰 → 정확한5파일 커밋과 승인된 보호 판본 갱신 → 공식 contracts/prod → 대상 두 결과 공식 저장 및 재조회 → 사람인21건 불변 확인 → 최신 GBrain/문서 기록과push. 내용 문제를 남긴 배포·수동 결과 보정·85건 전체 재시도는 하지 않는다.

## 최신 완료 기준 — 2026-09-16 01:42, 사람인 잔여 21건 분리 처리 완료

- 주인님은 검증된 사람인 수정의 운영 배포, 복구 가능한 6건의 1회 재처리, 원본을 더는 찾을 수 없는 15건의 재시도 중단을 승인했다. 성공한 과거 건과 다른 오류·번역 작업은 재실행하거나 함께 배포하지 않았다.
- 결론은 최근 수정이 없던 오류를 새로 만든 것이 아니라, 과거 코드가 첫 페이지만 찾고 바깥 상세 화면의 짧은 글을 이력서 전체로 오인하던 기술부채가 대량 재처리에서 드러난 것이다. 21건 조사는 배포 전에 끝냈고, 배포 뒤에는 조사 결과대로 6건만 재처리했다.
- 수정 경로는 기존 `Saramin.download_resume` 하나다. 모든 전형과 페이지를 같은 로그인 세션에서 끝까지 읽어 유일한 지원자를 선택하고, 정확한 지원자 상세의 원본 첨부 또는 온라인 인쇄 결과를 검증한다. 정상 조회를 모두 마친 뒤에도 지원자가 없을 때만 `resume_source_unavailable`로 영구 종료한다.
- 배포 커밋은 `2620b6fcb1e0075220353e9965147c4198f889cd`이며, 사람인 소스와 직접 테스트 2파일만 포함한다. GitHub main·운영 체크아웃·앱/SSE/알림 컨테이너가 같은 커밋이고, 운영 이미지는 `exdigm_app:20260916012801` / `sha256:b5d22d0da409b7360f571ded92aa08cadd1f1d64c0bd08276dfd706f69f7bfb2`다.
- 수정 후 직접 영향 검사 119개, 확대 검사 275개, 고정 보호 검사 194개, Django check, Ruff, code knowledge catalog, diff 검사가 통과했다. 최종 코드 리뷰의 승인 finding과 열린 질문은 0개다.
- 운영 변경 직전 21건의 원장·메일·오류·알림 상태를 `.debug/saramin-21-before-deploy-repair-20260915T162648Z.json`에 0600으로 보관했다. 크기는 409430바이트, SHA-256은 `c531fe887fd0393df591c68d06c4a71eba08439eafbdc606a78325e0d48df723`다. 민감한 원문과 개인정보는 문서·Git·GBrain에 복사하지 않는다.
- 한 트랜잭션에서 현재 상태가 백업과 정확히 일치하는지 먼저 잠갔다. 복구 가능 6건만 대기 상태로 돌리고 이전 실패·시도 사유를 감사 이력에 남겼다. 나머지 15건은 실패 상태를 유지한 채 `resume_source_unavailable`로 분류하고 재시도 시각·작업자 점유·파일 연결을 비워 두었다.
- 6건은 각각 시도 횟수가 정확히 1회만 증가해 모두 수집 `done`이 됐다. 6개 실제 Drive 파일은 ID·PDF 형식·크기·MD5·휴지통 아님·정확한 수신 폴더·원장 식별 속성이 모두 일치했다.
- 6건의 후속 내용 처리도 모두 끝났다. 본문과 구조화 해시가 생성됐고, 각 파일마다 저장된 이력서 1건과 후보자 1명이 만들어졌다. 후보자의 현재 파일·현재 이력서 연결까지 일치하며 LLM 실패, 재시도 대기, 처리 중 상태는 0건이다.
- 15건은 모두 실패/`resume_source_unavailable`이며 시도 횟수·원본 URL/metadata·메일 연결·기존 알림 이력이 백업과 같다. 재시도 예약, 작업자 점유, FileData/후보자 생성은 모두 0건이다.
- 21건의 기존 OperationalError 21개는 내용까지 그대로이고 새 관련 오류는 0개다. 새 Notification과 NotificationDispatch도 0개이며, 6건의 기존 알림 발송 시각과 시도 횟수도 바뀌지 않았다. 메일 분류의 완료·성공·실패 집계만 현재 원장 상태에 맞게 정상 갱신됐다.
- 마지막 확인에서 운영 서비스 5개는 1/1, 작업자·지원 프로세스 11개는 active, drain은 off, HTTPS는 200이다. 현재 작업자 상태 확인 명령은 `scripts/run_workers.sh status all`이며, 예전 `scripts/deploy/deploy_workers.sh status` 표기는 현재 명령 계약과 맞지 않는다.
- 다른 번역 작업의 5파일은 배포 전에 stash `90f00688d5e6a60978719ed6211d925728ab89a5`로 분리했고, 사람인 처리가 끝난 뒤 원래 5파일 SHA-256과 전체 diff SHA-256 `6b6ceeb9328a91f43851b3ecbd5dc43e2cfe66fac430da814541aff62ef4675c`가 같게 복원했다. 안전 사본과 기존 사람인 stash는 삭제하지 않았다. 번역 작업의 검증·배포 여부는 이 완료 판정에 포함하지 않는다.

## 최신 재개 기준 — 2026-09-16 01:03, 사람인 잔여 21건 원인 분리와 미배포 수정

- 사용자 후속 지시는 사람인 이력서 업로드 실패가 계속되는 원인을 확인하고, 서로 다른 실패 형태의 실제 화면을 읽기 전용으로 대조한 뒤 수정하는 것이다. 운영 재시도·다운로드·DB 변경·배포는 이번 승인에서 제외했다.
- 기준 판본은 운영 clean main과 debug detached HEAD의 `03116556bbc3d6ce95638045abe72cb52f4e4575`다. debug에 있던 번역 작업의 소스1개·테스트2개는 다른 작업으로 보존했다. 이번 소유 파일은 `auto_posting/sites/saramin.py`, `tests/test_auto_posting_browser_runtime.py` 두 개뿐이며 아직 미커밋·미배포다.
- 85건 재처리 뒤 남은 사람인 수집 실패21건을 실제 현재 화면과 API로 분리했다. 5건은 대상이 다른 페이지(2쪽4건·3쪽1건)에 있었고, 1건은 대상·실제 이력서가 존재하지만 바깥 상세 화면 본문만 검사해 거절됐다. 나머지15건은 현재 모든 전형과 모든 페이지에 대상이 없고 복구 가능한 이력서 자료도 없다. 마지막 추가 실패 `e9d91381`도 이15건과 같은 대상 부재형이다.
- 최근 목록 준비 대기 수정은 화면이 늦게 나타나는 실패만 줄였다. 현재 페이지 한 장만 읽는 선택과 바깥 상세 화면 본문 검사는 기존 코드에 남아 있던 별도 결함이다. 따라서 이번 연속 실패 전체가 최근 수정 때문에 새로 생긴 것은 아니다. 재처리로 기존 결함이 서로 다른 데이터에서 드러난 것이다.
- 현재 사람인 화면은 `hiring.saramin.co.kr`이고, 후보자 자료는 실제 화면이 호출한 `api-hiring.saramin.co.kr/api/applicants`에서 전형별20건 페이지로 온다. 공고 상세 응답의 `recruitmentHiringProcessList`, 후보자 목록의 `totalPages`와 `careerPeriod` 계약, 지원자 상세 `/applicant-view/{id}`를 현재 로그인 세션에서 직접 확인했다. API 경력 개월 수와 화면의 년·개월 표기는 현재 표본20건 모두 일치했다.
- 수정은 기존 `Saramin.download_resume` 공식 경로 안에서만 이뤄졌다. 화면이 실제로 받은 공고·전형별 첫 목록 응답을 기준으로 모든 페이지를 같은 로그인 세션에서 조회하고, 마스킹 이름과 경력이 유일한 지원자만 선택한다. 페이지 수·전체 인원·지원자 ID가 조회 중 바뀌거나 API가 실패하면 일반 재시도 오류로 남겨 대상 부재로 오판하지 않는다.
- 선택 뒤에는 정확한 지원자 상세를 열어 기존 인쇄 동의·이력서 영역의 유일한 원본 첨부 링크·지원자 ID·온라인 인쇄 결과의 학력/경력/PDF 검사를 그대로 사용한다. 바깥 React 화면의 짧은 본문은 이력서 완전성 근거에서 제거했다. 모든 전형·페이지를 정상적으로 끝까지 읽고도 이름이 한 건도 없을 때만 `resume_source_unavailable` 영구 실패로 종료해 같은 작업을 의미 없이 반복하지 않는다.
- 수정 전 관련 기준선117개가 통과했다. 새 대상 부재 검사는 기존 코드에서 일반 `matches=0` 오류로 실패했고, 수정 후 페이지1·페이지3, 지연 응답, 온라인형·첨부형, 첨부 중복·다른 지원자, API 실패의 재시도 보존, 완전 부재 종결을 포함한 관련119개가 통과했다. 직접 영향 확대 검사275개, Django check, Ruff, code knowledge catalog, diff 검사가 통과했다.
- 실제 읽기 전용 세션에서 장부 `31811a81`의 3쪽 대상이 정확한 경력으로 선택되고, 장부 `84c7c124`는 전체 탐색 후 `resume_source_unavailable`로 분류되는 것을 수정 코드로 확인했다. 장부 `8f56f3c7`의 실제 상세는 인쇄 버튼이 유일하게 표시되지만 바깥 본문419자에 경력 표기가 없어 기존 검사가 오판한다는 사실도 확인했다. 조사 소유 탭은 모두 닫았고 공유 브라우저에는 `about:blank` 한 장만 남겼다.
- 코드 리뷰에서 추가 페이지의 오래된 브라우저 캐시가 대상 부재 오판을 만들 수 있는 위험1건을 확인해 `no-store` 조회와 전체 지원자 ID 개수 대조를 추가했다. 재검증 후 승인 finding과 열린 코드 계약 질문은0개다.
- 공식 `scripts/debug_workspace.sh contracts`는 다른 번역 작업이 승인 아래 바꾸는 보호 파일 `tests/test_resume_knowledge.py`가 기존 pin과 달라 그 지점에서 정지했다. 이번 두 사람인 파일 때문이 아니며, 번역 작업의 보호 기준 갱신 뒤 같은 검사를 다시 실행해야 한다.
- 운영 장부21건은 failed/no claim/다음 재시도 없음 상태를 유지한다. 실제 이력서 다운로드, Drive/FileData/Candidate 저장, 알림 재발송, 커밋·push·운영 배포는 실행하지 않았다. 다음 운영 단계는 별도 확정 뒤 비공개 기준선 백업, 검증된 커밋 배포, 정확한21건의 공식 worker 1회 처리, 예상6건의 실제 파일·후보자 저장과15건의 영구 종료·알림 불변을 각각 확인하는 것이다.

## 번역 최신 재개 기준 — 2026-09-16 01:30, 실제 두 원문 최종 내용 대조 완료·배포 전

- 주인님은 공용 LLM 설정 전달 코드와 관련 보호 테스트까지 범위를 넓혀, 같은 모델의 추론 수준을 기존 번역 완성 호출 한 번에만 높이는 변경을 승인했다. 현재 모델, 다른 호출의 기본 설정, 호출 횟수, 60초 제한은 유지한다.
- 최종 수준은 medium이다. high는 짧은 표본에서 시기·의사 표현을 복원했지만 실제 긴 원문의 번역 완성 호출이 60초 제한을 넘었다. high를 반복하거나 제한 시간을 늘리지 않았다. medium은 같은 의미를 복원했고 실제 두 원문을 끝까지 처리했다.
- 번역 관련 파일은 common/llm.py, projects/services/recommendation_resume_ssp.py, tests/test_llm.py, tests/test_resume_knowledge.py, tests/test_recommendation_resume_local_worker.py의 5개다. 제품 코드 253줄 추가/421줄 삭제로 순감소168줄이다. 새 모델·의존성·별도 처리기·P_KO/P_EN 좌표는 없다.
- 공식 경로는 원문 L → 통합 국문 번역 → 기존 번역 완성 호출의 정확 문구 수정 → 단일 S 지도 → 같은 S에 연결한 원문으로 국영문 생성 → 기존 검증·후보자 저장이다. 일반화된 계약을 기존 함수·공통 규칙에 통합하는 최소 구현2단계에서 멈췄다.
- 원래930 최종 산출물 .debug/translation-fidelity-20260916-original-930-final-v6.json은 49.90초, 128L→89S, 제외0, 정확 수정3개다. 현재ca5 최종 산출물 .debug/translation-fidelity-20260916-current-ca5-final-v6.json은 48.03초, 128L→82S, 제외0, 정확 수정4개다. 두 결과 모두 공식 extract_resume_knowledge_from_text와 기존 지식 검증·후보자 projection으로 생성했다. 임시 모델 설정이나 진단 wrapper를 쓰지 않았다.
- 두 실제 원문과 국영문 전체 본문을 대조했다. 학위2개·성적·실제 약사 자격1개·연구/논문/장학금/도구·자기소개 세부 사실과 4개 경력 기간이 보존됐다. 원문에 있던 근무지가 지도에서 빠지는 생산 계약도 기존 경력 근거 범위 지시에 근무지를 포함하도록 바로잡아 네 근무지가 두 언어에 모두 전달됐다. DOCX 파일은 검증하지 않았다.
- 산출물 SHA256은 원래930=73f85b292fece619b158fa78da2fba6858f17f52510ef4009d3255423c6ff4f1, 현재ca5=293c766d449ac3e281472ddc7ca47f86fec62b2c95ad9ab2ec88fe97ec0bbee4다. 둘 다0600이고 원문 해시와 최종 SSP 코드 해시29971faa159c74524af1452e7e38594bb2930c6898d3fd7fb7c0c8f1243332ec가 일치한다. 원문 내용은 문서·Git·GBrain에 복사하지 않는다.
- 최신 관련 검사40개 통과. 최종 보호·후보자 전체 검사와 세 언어 형태 표본은 아래 코드 복구 후 동일 판본으로 다시 실행한다. 기존 확대 검사9개 실패는 submit_to_pm 기준 데이터 부재이며 삭제·완화하지 않는다.
- 다른 작업은 새로 사람인 배포·복구 가능6건 처리를 승인받았다. 번역5파일은 소유 작업의 동의로 stash90f00688d5e6a60978719ed6211d925728ab89a5에 정확히 보관하고, 사람인2파일의 먼저 배포를 허용했다. 번역 코드 실행·편집은 일시 중단했다. 보관본과 .debug 증거를 삭제하지 않으며, 복구 뒤5파일 해시를 대조하고 번역 검증을 재개한다.
- 운영 저장은 아직 하지 않았다. 읽기 전용 사전 대조에서 원래930은 현재 프로필을 바꾸지 않고, 현재ca5는 기존 최신 연결을 유지하며 갱신한다는 공식 선택 결과를 확인했다. 기존 이메일·전화·출생연도·성별도 최종ca5와 일치한다. 이전 포괄 백업 SHA8a40da248453d4272dc0bf739a13a20c36b2e231cb03dece46bc20fc3a759b8d를 보존하며 실제 쓰기 직전 현재 DB와 다시 일치 검사한다.
- 다음은5파일 복구 → 세 형태 표본·최종 회귀 검사·직접 리뷰 → 해당5파일만 커밋 → 승인된2보호파일 변경을 반영한 root pin/ref 갱신 → 공식 contracts/prod → 검증한 두 결과를 기존 persist_pending_resume_knowledge 및 process_resume --output candidate로 한 transaction에서 저장 → 신원·현재 연결·원문 불변과 실제 저장 내용 재조회다. 다른 작업자의 실행 중 처리와 배포를 겹치지 않는다.

### 번역·공용 호출 코드 리뷰 계약 갱신

1. 원천: 원문 충실성·국영문 동일 내용, 최소 코드·SSP, 내용 보존 기준/보호 검사 변경 및 번역 완성 한 호출의 추론 수준 변경 승인.
2. 역할: 원문 사실을 정상 생성 경로에서 두 언어와 후보자 저장 결과까지 전달한다.
3. 경계:03116556 기준 번역5파일 diff와 공용 호출·원문 지도·section 생성·지식/후보자 저장의 직접 소비자. 사람인 별도 변경은 이 리뷰에서 제외한다.
4. 입력: 원문 L, 단일 S, 같은 gemini-3.1-flash-lite 응답, 기존 작업·원본·성공 데이터.
5. 절차: 의미 생성은 AI, 코드 검사는 출처/형식/정확 교체다. 완성 호출만 medium이며 다른 기본값과60초 제한은 유지한다. 무효 응답은 실제 오류를 한 번 돌려주고 반복 실패는 저장하지 않는다.
6. 출력: 내용과 조건이 원문에 충실한 국영문 결과와 출처 연결. 의미 검증 자체를 별도 운영 검사 단계로 늘리지 않는다.
7. 보호: 신원·원본·현재 이력서 선택·국문 성공 흐름·다른 호출 기본값. 보호18파일 중 tests/test_resume_knowledge.py와 tests/test_llm.py만 승인된 변경이며 나머지16파일과 보호 검사기·manifest는 동일하다.
8. 비목표: 새로운 모델/실행기/일괄 재대기, 사람인 작업 대신 수행, 인증 복사, 원문 임의 수정, 필수 경력 완화, 메일/알림 재발송.
9. 검증: 기존 기준선과 추가 오류/호출 범위 테스트, 실제 두 원문과 세 형태 표본, 고정 보호 검사, 공식 prod와 실제 저장/원본·현재 포인터 재조회. 공용 호출·배포 부분은 주 에이전트 code-review-loop, 번역 반복은 resume-evolution-loop Direct Review Only를 적용한다.

## 최신 재개 기준 — 2026-09-16 01:01, 실제 번역 의미 검증 미완료

- 사용자 최종 지시는 최소 코드·SSP이며 원문에 충실한 국문·영문 실제 생산이 완료 조건이다. 형식 검사 통과나 두 언어의 같은 누락을 성공으로 보지 않는다.
- 운영/GitHub/debug base는03116556bbc3d6ce95638045abe72cb52f4e4575다. 현재 번역 소스1개와 테스트2개는 미커밋이다. 이번 번역 개선의 push·배포·운영 DB 저장·보호 기준 승격은 아직 실행하지 않았다.
- 최종 실행 경로는 통합 canonical 번역 → 원문에 근거한 정확 문구 수정 → 기존 단일S 지도 → 기존 국영문 section 생성이다. 번역 완성과 section 생성에 필요한 원문 연결은 기존 canonical_source_records의 include_original 인자로 공유한다. 새 모델·의존성·별도 처리기·P_KO/P_EN 좌표를 추가하지 않았다.
- 원래 오류 외에 확인한 생산 결함은 긴 자기소개 요약 지시, 서로 다른 학위·연구를 하나의 거대S행으로 묶는 문제, L원문 번호를 S수정 좌표로 착각하는 수정 지시, 국문 초안에서 영문을 재구성하며 공식 명칭·수량을 바꾸는 문제다. 자기소개 요약 지시와 중복 Source contract를 제거하고 짧은 의미 단락·정확S좌표·동일S에 연결된 원문 전달로 정리했다.
- 원문만 최종 생성에 주는 실험은 입사시기 after four weeks를4주 이내로 바꾸어 실패했다. 해당 변경은 되돌렸다. 파일명 english-original-only.json은 패치 적용 전 실행된 진단 산출물이므로 그 설계의 증거가 아니다. 실제 적용 실험은 english-original-only-applied.json이며 실패다.
- 실제 모델과 공식 extract_resume_knowledge_from_text 결과에서 현재까지 원문20행의 출처·학위·면허·논문 단복수·입사4주 후는 보존됐다. 다만 willing to relocate가 국문 국내 이전 근무 가능으로 남는 의미 정밀도 문제는 미해결이다. english-aligned-completion.json은 형식 통과이나 전체 의미 동일성 완료 증거가 아니다.
- 이전 실제 원문930과 현재ca5의 v5 실행은 각각128L→89S/82S, 제외0, 양언어 학력2·실제 약사 자격1을 확인했다. 이후 원문 전달 계약이 바뀌었으므로 최신 코드의 실제 두 원문 검증과 저장은 다시 필요하다. DOCX는 검증하지 않았다.
- 최신 기준선 검증: 관련34개 통과/56개 제외, 보호·후보자 대상207개 통과, Django/Ruff/diff 통과. catalog는 /home/chaconne/.local/bin/uv run --locked python -m tools.code_knowledge catalog_update로 current/local.valid=true/broken_references=[]다. 확대 검사243개 통과/9개 실패는 기존 submit_to_pm 기준 데이터 누락이며 없애거나 완화하지 않았다.
- 현재233줄 추가/416줄 삭제의 제품 소스와 테스트 포함507줄 추가/533줄 삭제다. root pin과보존ref는6ac315e930880406f6f7f9d489322b07b85e380f 그대로다. 기존 승인된 tests/test_resume_knowledge.py 외17보호파일은 바꾸지 않았다.
- 추론 설정 진단: D8는동일gemini-3.1-flash-lite를사용하며 공용Gemini호출은reasoning_effort를전달하지 않는다. 공급자기본minimal을쓰는것은코드와공식문서로확인했다. 같은모델high진단을일회성RO프로세스에서만실행중이며첫60초요청504를관측했다. high설정의효과나운영적합성은아직미검증이며공용LLM코드·운영설정은변경하지않았다.
- 운영 저장 전 포괄 백업 .debug/operational-errors-20260916-before-faithful-translation-save.json은0600,489348바이트,SHA256 8a40da248453d4272dc0bf739a13a20c36b2e231cb03dece46bc20fc3a759b8d다. 후보자·두FileData/Source/Resume/Knowledge·하위경력/학력/자격/어학을포함한다. 저장전신선한상태일치검사와단일transaction이필요하다.
- 별도작업「이력서 업로드 실패 진단」소유의사람인2파일은그작업자가stash a98dba4ce57a09f6720ffcf8530ca8b30c320328에직접보관했다. 메시지saramin-pagination-owner-20260916, diffSHA d2983983a1f7190bc6b2f62437548e2fac5c50bb248e943c0faa648fd4cda75e다. 그작업의운영배포·21건재처리는승인되지않았으며이번배포에포함하지않는다. 우리배포또는보류결정후HEAD와상태를알리면소유작업자가복원·해시재검증한다. stash를drop하지않는다.
- 다음: 의미 차이의 실제 해결과 같은원인변형/기존성공대조가먼저다. 필요한모델호출계약·보호범위·비용변경이현승인범위를넘으면정확한증거와한가지결정을요청한다. 그전에는현재품질문제를닫거나미검증수정의배포·대상저장을진행하지않는다.

## 최신 재개 기준 — 2026-09-16 00:05, 최소 코드·SSP 번역 완성 경로 검증

- 사용자 목표는 검사 자체가 아니라 원문에 충실한 국문·영문 결과의 실제 생산·저장이다. 마지막 지시인 최소 코드·SSP를 적용한다. 기존 단일 S 좌표, 현재 모델, 공식 지식 추출과 저장 경로를 유지한다.
- 운영은 clean main 03116556bbc3d6ce95638045abe72cb52f4e4575다. debug는 같은 base의 detached HEAD이며 번역 소스1파일과 테스트2파일만 미커밋 상태다. 최신 번역 개선은 아직 운영 배포·실제 파일 재처리하지 않았다.
- 번역 생산 경로를 하나로 통합했다. 원문의 물리 줄을 문맥에 따라 묶어 번역하되 모든 L 출처를 보존한다. 실제 두 언어 이력서에서만 중복 대응 출처와 근거를 남기고 제외한다. 불완전한 번역 행을 버리는 기존 경로는 제거했다.
- 번역 완성은 기존 생성 함수 안에서 같은 모델이 원문과 초안을 보고 정확한 교체 문구를 작성하며 스크립트가 그 문구만 적용한다. 오류 목록만 반환하는 별도 검토 경로와 전체 문단을 다시 생성하는 실험 경로는 남기지 않았다. 코드가 의미를 판정하거나 번역을 지어내지 않는다. 비국문 처리에 통상 AI 호출1회가 추가되며 무효 교체 지시는 실제 오류를 한 번 피드백한다.
- 변경 파일은 projects/services/recommendation_resume_ssp.py, tests/test_resume_knowledge.py, tests/test_recommendation_resume_local_worker.py다. 00:05 diff는436줄 추가/520줄 삭제다. 지식 fingerprint의 구조 판본을v2로 바꿔 정상 재생성 때 옛 누락 자료를 재사용하지 않으며 일괄 재대기는 하지 않는다.
- 최소 구현 게이트는2단계에서 멈춘다. 기존 create_canonical_source, SourceRecord, source-map, 국영문 section 추출, 기존 저장 함수를 재사용한다. 새 모델·의존성·별도 실행기·두 벌의 원천 좌표는 추가하지 않는다.
- 검증: 관련32개 통과/56개 제외, 후보자·보호 파일 대상207개 통과, Django/Ruff/catalog/diff 통과다. 이후 국문→영문 고유명사 번역의 기존 지침1줄을 복원했으므로 최종 영향 검사는 다시 실행한다. 이전부터 존재한 확대 검사10개 ActionType 기준 데이터 실패는 이번 변경의 통과로 포장하지 않는다.
- 실제 현재 모델과 공식 순수 추출 함수를 이용한 읽기 전용 검증: 영문 한 벌20행, 실제 양언어17행, 국문11행 holdout을 실행했다. 원문 출처·자격·학위·경력·도구의 목적·수치·조건의 보존과 지식 검증/후보자 projection을 대조했다. 양언어 holdout은 각 언어에만 있던 자격과 근무 조건도 두 결과에 반영됐다. 국문 원천은 변경 없이 유지되고 영문 회사명 변환도 재확인했다.
- 영문 synthetic 결과의 publication 단복수 및 willing/possible 표현은 엄밀한 의미 정밀도 잔여 관찰이다. 모든 의미 변형의 무결성을 보장한다고 주장하지 않는다. 실제 실패 원문930의 새 결과와 저장은 아직 검증 전이다. DOCX 검증을 수행했다고 주장하지 않는다.
- 비공개 실험 증거는 debug .debug/translation-fidelity-20260915-*.json,0600이다. 공식 경로의 마지막 결과는 single-full-final, bilingual-holdout, korean-holdout-v2이며 중간 실패·폐기 설계 결과와 구분한다. 민감 실제 원문은 문서·Git·GBrain에 복사하지 않는다.
- 공식 contracts는 승인된 변경 파일 tests/test_resume_knowledge.py 하나가 이전 root pin과 달라 정지했다. 나머지17개 보호 파일은 유지했다. pin6ac315e930880406f6f7f9d489322b07b85e380f와 승인 ref는 아직 바꾸지 않았다. 실제 검증·코드 리뷰·정확한 커밋 기록 후 기존 명시적 승인에 따라 관련 기준만 갱신하고 공식 검사·배포를 진행한다.
- 85건은 재대기0으로 종료됐다. 마지막 실제 조회는 수집 done64/failed21, 후보자 연결·db_saved58, 추출/큐0이다. 수집 완료64를 후보자58 또는 내용 품질 전체 성공과 동일시하지 않는다. 경력 없는 파일51d12bdd는 필수 항목을 지어내지 않고 종결됐다. 남은 사람인 화면 조사와 직원4사이트 비밀 등록은 응답 대기이며 조건 변화 없는 재시도는 하지 않는다.
- 다음: 같은 diff와 직접 소비자의 코드 리뷰, 최종 회귀 검사, 실제930 원문에서 국영문 내용 비교, 승인된 보호 판본 갱신과 공식 prod, 공식 저장 함수로 대상만 안전 재처리, 실제 저장·현재 이력서 포인터·원천 불변 검증, 문서/GBrain 기록과 push다.

### 번역 변경의 코드 리뷰 계약

1. 원천: 원문 충실성·국영문 동일 내용이라는 최신 목적, 번역 보존 기준과 보호 검사 변경 승인, 기존 코드·커밋·push·운영 배포 승인이다.
2. 역할: 이력서 원문을 잃지 않고 동일한 사실의 국영문 지식과 후보자 저장 결과로 전달한다.
3. 경계: base03116556부터 위3파일 diff, create_canonical_source → canonical_source_records → source-map/section 추출 → ResumeKnowledgeArtifact/후보자 projection과 공식 저장의 직접 소비자다.
4. 입력: 원문 L 좌표, 같은 모델의 JSON, 기존 단일 S 좌표다. 현재 운영·debug 원문·성공 데이터·신원·사용자 작업은 보존한다.
5. 절차: 언어 분류, 통합 번역, 원문 좌표의 기계적 보존 검사와 제한 피드백, AI 작성 정확 교체의 적용, 기존 섹션 생성·검증·저장이다. 국문 원천의 불필요한 재번역은 없다.
6. 출력: 원문 사실과 조건이 같은 국영문 내용, 보존된 원문 연결과 중복 근거다. 미완성 번역을 행 제외로 성공 처리하지 않으며 호출 오류와 무효 수정은 명시적으로 실패한다.
7. 보호: 원본과 이전 성공 데이터, 모델, 후보자 신원, 최신 Resume 선택, 기존 국문 성공 흐름, 기존18파일 중 승인된 번역 기준 외17파일의 동일성이다.
8. 비목표: 다른 오류 기능 재수정, 자격정보 복사, 수집/메일 재발송, 필수 경력 완화, 원문 임의 보정, 새로운 처리기나 전체 재처리다.
9. 검증: 동일 focused/protection 검사, 실제 모델의 세 언어 구성 holdout, 실제 실패 원문930, 보호 pin의 승인된 차이만 검증, 공식 운영 배포·저장 후 조회다. 코드 검사/내용 검증/운영 저장 성공을 각각 구분한다.

## 최신 재개 기준 — 번역의 원문 충실성·국영문 동등성 재검증

- 최신 사용자 정정: 목표는 기술 오류 통과가 아니라 원문에 충실하며 국문과 영문이 동일한 내용을 담는 것이다. 원문 → 공통 사실 자료 → 동일 사실의 국문/영문 → 저장·표시를 정본으로 잠근다. 양언어가 똑같이 정보를 잃는 경우도 실패다.
- 사용자 `번역 내용 보존 기준과 관련 보호 검사 변경 승인`으로 기존 번역 실패 행 제외 규칙과 관련 고정 보호 검사·배포 기준 판본의 변경을 승인했다. 기존 원문·성공 데이터·모델·신원 보호 규칙은 유지한다. 일반 코드 수정·커밋·push·운영 배포 승인은 유효하다.
- 현재 운영/debug/GitHub 판본은 03116556bbc3d6ce95638045abe72cb52f4e4575이며 양쪽 clean이다. 공식 prod 22:53:15 KST, 이미지 exdigm_app:20260915225148, SHA25648a2e6d4a28fc895a49742beac1894e192898d41e3517a9d43e1cae41359e5a7. 실제 app/SSE/notification의 source commit, 서비스5개1/1, 작업자11개 active, HTTPS200을 확인했다.
- 03116556은 빈 혼합언어 번역 결과에 실제 오류와 이전 응답을 한 번 돌려주는 기술적 중단 복구다. 신규7검사, 범위 관련 포함11개, 지식44개, 보호194개 통과. 이 검사를 번역 품질 성공으로 보지 않는다.
- 대상 FileData93072c89-9eb4-4275-a248-d22d7a8731c1은 원문과 실패 횟수3을 보존하고 내용 단계만 다시 대기시켰다. 23:02:14 KST Resume88541a99-7991-44a1-b4bf-2d4931cd8994 저장, Candidate70a585b5-ae6b-42c4-b79d-18a2af1d76e6 연결, 실패/큐/claim 해제다. **내용 품질은 Fail**이다.
- 실제 저장된 canonical_source는 한 벌의 한국어 항목명+영문 본문 이력서를 ko_en_mixed로 분류했다. 원문36행을 중복으로 제외하고 일부 번역 행을 상동으로 채웠다. 약사 자격, 장학금, 일부 도구·연구 상세가 국문과 영문 양쪽에서 누락됐다. 원응답 전체는 과거 오류에 저장되지 않아 최초 missing text가 빈값인지 잘못된 키인지는 미확인이다.
- 최초 통제 가능한 이탈은 번역 생산·조립 계약이다. 물리 행1:1 강제, 근거 없는 중복 제외, 번역 실패 행 제외가 원문 충실성과 충돌한다. 의미 판단은 LLM, 스크립트는 원문 좌표의 보존·중복 대응·출력 계약만 검사한다. 단일 S 좌표와 기존 양언어 섹션 생성은 보존한다.
- 수정 예정 범위는 recommendation_resume_ssp.py와 관련 테스트2개다. 기존 두 번역 프롬프트/생성/조립 경로를 동일한 내용 보존 계약으로 정리한다. 문맥상 이어지는 행의 묶음 번역을 허용하되 원문 refs를 모두 보존한다. 실제 중복은 retained refs와 이유를 남기며 미포함 행을 자동 중복 판정하지 않는다. 한 번의 오류 피드백 뒤에도 누락이면 저장하지 않는다. 새 모델·별도 처리기·국영문 원천 좌표 두 벌은 추가하지 않는다.
- 변경 전 기준선은 관련15검사 및 기존 고정194검사 통과다. 보호18파일 중 번역 관련 tests/test_resume_knowledge.py의 승인된 계약 변경만 허용한다. 기존 기준을 무조건 승격하지 않고 나머지17파일 동일성, 변경 기대값, 직접 소비자 검증 후 root 소유 pin/ref를 승인된 새 커밋에 갱신한다.
- 재처리 직전0600 백업 .debug/operational-errors-20260915-translation-before-retry.json,21693바이트,SHA25654ecc134aea28cf7259a139b38bc12b9b3d3ea9a329355172b4b134eded12d60. 처리 후 원문·지식·Resume·Candidate/current_resume 증거는 .debug/operational-errors-20260915-translation-quality-evidence.json,250388바이트,SHA256f614f834debf7978a5a178ebeb27ecef1863684d5e4297d128f5f4f70258b7db다. 개인정보 원문은 문서/Git/GBrain에 복사하지 않는다.
- 해당 후보자의 현재 Resume2ad4d163-3c33-415f-a773-2475aeb4cac1 / FileDataca5e44fc-604e-40ee-8f45-626a37b8b2f3는 22:52:19 저장된 다른 원천이다. 대상930 재처리는 이 현재 포인터를 덮어쓰지 않았다. 향후 재처리도 공식 최신 이력서 선택·신원 보호 규칙을 따른다.
- 원래 사람인3건은 모두 원본 PDF→후보자 저장까지 완료했다. B22:38:53/C22:40:47 저장과 Candidate.current_resume 연결, 실제 Drive 바이트 SHA/MD5/크기/폴더/장부 appProperty를 검증했다. 원래 표지3개와 기존 성공6건, 게시 공고2609150004510, 알림 영수증은 보존됐다.
- 85건 재처리는 계속 실행 중이다. 23:06 전후 수집 done36/실패15/대기34, 후보자 연결·db_saved31, 추출 대기2, 경력 없는 파일51d12bdd-49b5-4054-b2c7-054a0780012f 종결1건이다. 수집 완료와 내용 품질 완료는 구분한다. 사람인 재실패 화면 조사는 별도 승인 대기이며 조건 변화 없는 재시도는 하지 않는다.
- 직원 bk@exdigm.com의4사이트 개인 인증은 사용자 등록/승인된 비밀 경로 응답 대기다. CEO 계정/전역 인증을 복사하지 않는다.

## 현재 재개 기준 — 22:27 배포 완료, 원본 복구와 85건 재처리 중

- 최신 오류 수정 요청과 기존 커밋·push·운영 배포 승인으로 10파일 변경을 0768ea164e5bf93b2f0ea82baa48737d17d2a1c0에 커밋했다. 공식 prod가22:27:35 KST 완료됐으며 GitHub/main/debug와 운영 앱 판본이 같다. 이미지 exdigm_app:20260915222614, SHA256 b7fb502d37e9a8146c15f3234e8cd0cac702db99ae528d69ddf62d261f519c2e다.
- 최종 코드 리뷰 승인 finding0개, 열린 코드 계약 질문0개다. 브라우저/uploader117개, 오류47개, 이메일10개, 출처 범위4개, 고정 보호194개를 통과했다. 확대 검사235개 통과/10개 실패는 수정 전에도 있는 ActionType pre_meeting1건/submit_to_pm9건 누락이다. 검사나 기대값은 완화하지 않았다. Django/Ruff/catalog/diff 검사 및 배포 이미지 실제문서 검사도 통과했다.
- CEO의 실제 Hermes MCP→email_list 공식 경로에서 원래 제목 검색이84.09초에서0.32초로 줄었다. 결과는 INBOX0건/HTTP200이다. 한글 이력서 검색34건 중2건은1.21초, 일반1000개 UID의2건씩 다음 페이지는0.34/0.32초에 중복 없이 반환됐다. 메일 발송·이동·삭제는 하지 않았으며 해당 제목의 휴지통 존재는 미확인이다.
- 배포 코드의 공식 autoposting resume dry로 사람인 원래3건의 실제 PDF를 각각484926/387193/315318바이트 확보했다. 텍스트6393/8882/6280자, 대상 이름 일치, 경력 본문, 표지 경고 없음이다. 기존 온라인 작성형 holdout도115136바이트 그대로 통과했다. 조사 소유6탭은 닫고 원래 공유 탭을 about:blank로 복원했다.
- 과거 표지3개의 바이트·Drive 위치·FileData·오류는 보존했다. 해당 Drive appProperty의 완료 영수증만 superseded-cover:<원래장부>로 표시하고, 장부 result에 superseded_source를 기록한 후 잘못된 file_data 완료 연결을 해제했다. 알림 영수증·원본 URL/metadata·attempt_count를 초기화하지 않고 같은 장부를 공식 worker가 재처리한다.
- 첫 장부 a2f861d4는 attempt6, 새 FileData2c749e1d-0e0b-49f6-ac27-9a6cda7dedde/Drive1xGpp9vwCt_JqDU52TzZYw02DQtNnnxEi/Candidate5dbcb431-216c-4112-9bd9-5791856ea4ab로22:36:26 KST db_saved/structured를 마쳤다. 큐·claim·오류는 비었다.
- 두 번째 장부1adfd5d2는 attempt5, 새 FileData6cc328ed-c07c-4ed0-8db6-1b376ef54c32/Drive16F-_RH4BSBO2PGwbdGg2g8LnjfXUia85이다. 세 번째328b654b는 attempt5, 새 FileData8d8c6d51-51c8-4a53-a83e-7ec436fda075/Drive17V3owUG_dxcg3pznOkBfeME_aWSSCXqk이다. 둘 다 수집 done이며 후보자 저장 완료는 계속 확인한다.
- 빈 OperationalError3건에는 원본 FileData의 llm_failure_message를 redaction 후 error_message/description에 보완했다. 원래ID·발생시각·생성시각을 보존했고 알림을 새로 만들지 않았다.
- 비공개 복구 백업은 debug .debug/operational-errors-20260915-before-source-repair.json,0600,1913547바이트,SHA256 ebc1ada35dc29a7ce16759f37cba702751d5a7bc637230a624167b6efd979062다. 장부94개/파일9개/원본 메일/오류3개/Drive 메타데이터가 포함된다.
- 85건의 ID 집합SHA256은479605a733f360ed33c38008c29bda52419525618cd77e4ea6a0919f8a7ea67b다. 인크루트27건/사람인58건을 정확히 잠갔고, 각 사이트의 가장 오래된1건을 공식 다운로드로 검증한 뒤 모두 waiting으로 돌렸다. attempt3과 알림·원문 정보는 보존하고 다른229건은 제외했다. 초 미만 정밀도까지 보존한 직전 장부 백업 .debug/operational-errors-20260915-85-before-retry-exact.json SHA25651e99a130ea9b37a4ad48bfec87a10e64c66f308c6c169243ab2db07238a4cbb,0600도 남겼다.
- 복구 준비용 일회성 명령의 nullable join 잠금 오류와 백업 밀리초/DB 마이크로초 비교 AssertionError는 모두 쓰기 전에 롤백됐다. 잠금 대상을 장부에 한정하고, 백업 직렬화 기준 대조 및 변경 전후 원래 datetime 직접 대조로 정정했다. 제품 새 장애나 실패한 파일로 오인하지 않으며 해당 진단 오류 기록을 삭제하지 않는다.
- 다음: 나머지 사람인2건의 실제 후보자 저장,85건의 수집/추출 결과와 잔여 실패 분리, 기존 성공6건/게시 공고/원문·알림 영수증 불변 확인, 과거 표지3개 보존 확인, 문서/GBrain 기록·push. bk 직원4사이트 개인 인증 등록은 사용자 응답 대기이며 타인/전역 인증을 복사하지 않는다.

## 배포 전 재개 기록 — 다른 조사 결과 검증 및 후속 수정

- 최신 사용자 요청은 다른 에이전트가 제시한 미해결 오류를 확인하고 수정하는 것이다. 이전 커밋·push·운영 배포 승인 범위에서 수행한다. 내용 추출 수정 승인 대기는 이 최신 요청으로 해소됐다.
- 사람인 3건의 실제 상세·첨부 화면 조사는 별도 질문에 사용자가 `3건의 화면 구조 조사 승인`으로 승인했다. 공고 게시·수정·삭제와 새 로그인은 제외한다.
- 기준 판본은 debug detached/prod clean main/GitHub main의 df7d5c8a85b3316fcb015f282690c935a2c4b86a다. 작업 시작 시 제품 worktree는 clean이었다. 이번 변경은 아직 커밋·배포하지 않았다.
- 중요 정정: 사람인 실패3건의 저장 PDF는 유효한 파일이지만 원본 이력서 전체가 아니다. 텍스트591/583/640자에 파일 미리보기는 인쇄되지 않는다는 안내가 있고 상세 경력이 없다. 아래 과거의 파일 크기·Drive 전달 성공은 전체 내용 확보를 뜻하지 않는다.
- 실제 인쇄 화면3건에서 `.resume_file a.file_link` 원본 PDF 링크가 각각1개임을 확인했다. 세 번째는 이력서 PDF와 별도 PPTX 포트폴리오가 있으며 PPTX는 이력서 영역 밖이다. 최초 이탈은 첨부형까지 무조건 화면 PDF로 저장한 수집 계약이다.
- 사람인 수정: 기존 다운로드 함수에서 이력서 영역의 유일한 링크와 지원자 경로 일치를 검사해 첨부 원본을 내려받는다. 온라인 작성형의 기존 공식 인쇄/PDF는 유지한다. 수동 팝업 이벤트 수집과 고정 대기는 공식 팝업/대상 준비 대기로 통합했다. 새 모듈·별도 실행기는 없다.
- 원문 범위 수정: 기존 두 번 시도 함수에 원문 records를 전달하고 같은 범위 검사 오류를 기존 재생성 프롬프트에 돌려준다. 역순을 스크립트가 뒤집지 않으며 반복 실패는 명시적으로 중단한다. 필수 경력 검사와 모델·저장 기준은 그대로다.
- 관측성 수정: FileData의 llm_failure_message를 우선 읽고 실제 값이 없을 때 기존 error_message로 이어간다. HTTP/서비스 오류의 이미 확인된 사용자 ID를 수집기까지 전달하며 다른 사용자의 오류를 같은5분 대표 기록으로 합치지 않는다. 로거가 지연된 사용자 인증을 실행하거나 DB를 읽지 않게 했다.
- 이메일 수정: IMAP 서버에서 제목/보낸사람/본문 검색 후 해당 UID 페이지만 BODY.PEEK로 읽는다. 개별 실패는 incomplete/failed_messages에 표시하고 다음 offset은 시도한 UID 기준이다. 연결 중단은502다. 받은편지함 기본값, 본인 계정 제한, 발송·휴지통 승인 조건은 보존한다.
- 실제 CEO Hermes container의 기존 MCP exdigm_request → email_list 계약 → API 경로로 원래 제목을 배포 전 조회했다. HTTP200,84.09초,INBOX 결과0건이다. Agent key/회사 메일 연결은 현재 정상이다. 휴지통 내 실제 해당 메일 존재는 이 조회만으로 확인되지 않는다.
- 개발 읽기 전용 DB 역할은 exdigm_debug_ro/read_only on이다. 개발 키로 운영 이메일 암호문을 읽으려던 검사는 InvalidToken에서 중단했고 IMAP 연결 전이었다. 운영 인증값을 개발 환경으로 옮기지 않고 위 실제 CEO MCP 경로로 전환했다.
- 현재 테스트: 수집101개 통과(기존85+첨부형16). 오류 수집47개 통과. 이메일10개 통과. 실제 출처 범위 재생성4개 통과. 기존 API 전체검사의 pre_meeting1건, 기존 local_worker의 submit_to_pm 등 기준 데이터9건 실패는 별도로 대조 중이며 검사/기대값을 완화하지 않는다.
- 운영 미완료:3건의 원본 재수집·후보자 저장, 기존 빈 오류 기록3건의 제한 보완,85건 재처리, 실제 새 이메일 경로 검증. 아직 운영 DB 쓰기를 하지 않았다.
- 85건은 전체 실패314건 중 mail/download_link/attempt3이며 Incruit UnicodeDecodeError27건과 Saramin matches=0 58건인 정확한 부분집합이다. 모두 FileData/claim이 없다. 다른229건은 자동 재처리 대상이 아니다. 원본/알림/중복 확인과 비공개 백업 후 공식 worker로만 재개한다.
- 외부 후보자 검색의 대상 직원 bk@exdigm.com은4사이트 개인 자격정보가 모두 없다. 운영 설정 직접 등록 또는 승인된 기존 비밀 저장 경로를 비동기로 요청한 상태다. 전역/타인 자격정보를 임의 복사하지 않으며 기존 LinkedIn15건을 보존한다.
- 조사 브라우저: 운영9223 Chrome PID1753602, 기존 profile 재사용. 사람인3건 상세/인쇄 탭은 이번 조사 소유다. 자동 인쇄 미리보기의 실제 Cancel 버튼으로 저장 없이 닫았다. 공유 Chrome 자체는 종료하지 않는다.

### 이번 변경의 코드 리뷰 계약

1. 원천: 최신 오류 수정 요청, 기존 운영 반영 승인, 본 문서의 보호 조건과 현재 코드 계약.
2. 상위 역할: 원본 이력서를 정상 추출·후보자 저장에 전달하고, 본인 회사 이메일 조회 및 진단 가능한 오류 기록을 제공한다.
3. 경계: base df7d5c8a부터 이번10파일 diff와 직접 호출/소비자. 사람인 다운로드/공식 uploader, D-map 재생성/변환, 이메일 list/detail/trash, 로그 수집/OperationalError까지다.
4. 입력: 실제 지원자 식별 근거·원본 링크, 직원 본인 계정, 검색/페이지 입력, canonical SourceRecord와 AI JSON. 운영 성공 레코드와 사용자 작업을 기준선으로 보호한다.
5. 절차: 공식 브라우저 다운로드 또는 온라인 인쇄, 기존1회 피드백 재생성, 서버 검색 후 페이지 다운로드, 오류 redaction/5분 사용자별 dedup/수집.
6. 출력: 실제 원본 파일, 유효 범위만 후속 추출에 전달, 명시적인 메일 부분 실패, 사용자·UID·실제 원인이 남은 DB 기록. 새 메시지 발송은 없다.
7. 보호: 성공6건 후보자 저장, 기존 공고2609150004510, 기존 장부/메일 알림 영수증, 계정 소유권, 필수 이력서 항목, 모델과 고정18파일/194검사.
8. 비목표: 필수 경력 생략/추정 생성, 임의 계정 백필, 받은편지함 기본값 확대, 메일 발송/삭제, DB 인프라·Hermes 변경, 다른 프로젝트 작업.
9. 검증: 추가 실패 red/green, 직접 소비자 검사, 변경 전과 같은 기준선 대조, 보호194/Ruff/Django/catalog, 공식 prod 및 같은 실제3건/CEO MCP 경로. 실제 운영 처리 완료와 단위 검사 성공을 분리한다.

## 21:14 재개 기록 — 코드 배포 정리·파일 수집 9건 완료, 후속 내용 추출 분리

- 주인님은 20:52 현황 보고 뒤 `승인, 진행해`로 기존 사람인3건 재처리를 승인했다. 이어 다른 작업의 미커밋·미배포 변경을 확인하고 필요한 마무리·커밋·푸시·운영 배포를 모두 진행하도록 요청했다.
- 점검 범위는 Exdigm의 제품 코드, 연결된 조정실 문서, 관련 최근 작업이다. 다른 프로젝트의 미완성 코드, 확정되지 않은 UI 시안의 신규 구현, 계정·메일함 정책 변경은 포함하지 않는다.
- 21:14 기준 원격 worktree는 운영 main과 debug detached 두 개이며 모두 clean df7d5c8a다. GitHub main과 실제 운영 app/SSE/notification도 같은 판본이다. 미커밋/미푸시/미배포 제품 코드는 없다. 시작 시 미전송이던 조정실 문서 커밋85abf8e·3962773은 모두 현재 문서 main에 포함되어 있다. 문서 전송은 공식 controlroom push 후 실제 원격 main과의 일치로 판정한다.
- 인크루트는 원래 실패한 동일 프로젝트 a78a7663-9502-4f39-9b1f-ea7d2f97d705에서20:48:37 실제 게시 성공했다. SiteRun b5b7f3bf-9ba4-40ec-a106-774f2695ad2d, run20260915204821, submitted/published true, 공고2609150004510이다. PostingSite의 현재 성공 연결과 빈 last_error를 확인했으며 재게시하지 않는다.
- 사람인 기존3건과 다른 작업이 같은 인쇄 지연 원인으로 확인한 신규492ac3cf-df57-4a7f-95d3-f525ee669918 한 건을 기존 운영 worker로 재처리했다. 첫건의 실제 Drive 저장을 확인한 뒤 나머지3건을 대기열로 돌렸으며21:05:29 KST까지4건 모두 수집 done이다.
- 기준선: 인크루트4건·잡코리아1건 done 유지. 사람인4건 failed/no FileData/no claim, 기존3건의 원본 해시 유지. 추가건 source hash8a2faf95403d6c36ee03db7b602bbad88905c57577a085d3258b4a88051c241e. 현재 판본에서 browser/uploader85개 재통과, 이전 비공개 백업0600과SHA256 일치.
- 보호: 원본 URL·metadata·기존 알림 영수증·성공 파일·실제 게시 공고·사용자 작업을 보존한다. 실패 장부의 최소 재대기 필드만 바꾸고 기존 worker가 공식 다운로드/PDF/Drive/FileData/메일 집계를 수행한다. 새 우회 실행기·추출 모델·게시·메일 발송은 추가하지 않는다.
- 사람인4건과 메일4건의 재처리 직전 백업은 `.debug/operational-errors-20260915-saramin-before-retry.json`,0600,SHA256 `9624743960490f05f26a9521b576bf2a04ae76770bafd41ce9af530aa8472561`이다. 기존8건 백업도0600과원래SHA256이 일치한다. 민감 원문은 채팅·문서·Git·GBrain에 복사하지 않는다.
- 9건 모두 실제 Drive GET으로 크기·MD5·MIME·휴지통 아님·정확한 대상 폴더·원래 장부 appProperty를 확인했다. FileData/Drive/result 연결, 장부 error 공백/no claim, 메일 처리완료와 실패0도 확인했다. 원본 URL/metadata·생성자·메일 연결·후보자 힌트·알림 발송시각/시도/실패시각은 백업과 동일하다.
- 기존5건은 attempt4와 원래 FileData를 유지했다. 인크루트4건의 메일 uploaded_count는 각각1, 잡코리아는 기존 다른 첨부를 포함해2다. 사람인4건은 각각1이며 알림 영수증을 초기화하지 않았다.
- 현재 판본에서 browser/uploader85개와 고정 보호194개(보호18파일 원본 유지)를 다시 통과했다. 서비스5개1/1, worker/support11개active, HTTPS200을 재확인했다. 새 제품 수정이 없으므로 같은 판본을 불필요하게 다시 배포하지 않았다.
- **수집 done은 후보자 저장 완료와 다르다.** 기존5건에 추가 사람인1건이21:20:26 KST structured/후보자 연결/db_saved를 마쳐 총6건 저장 완료다. 나머지 사람인3건은21:20:48 조회에서 knowledge_validation 실패/no claim/대기열false다. 아래 실패 원인을 수집 수정의 실패나 전체 완료로 혼동하지 않는다.
- GBrain 운영 맥락·배포 절차·수집 incident3페이지에 현재 판본과 수집/내용 추출 경계를 갱신했다.21:19 KST에 재조회하여 새 절의 존재와 기존 본문 전체 보존을 확인했다.
- 문서 정리 fabb1a2는 기존85abf8e·3962773을 포함해 공식 controlroom push로 원격 main에 전송됐다. 마지막 Venture 동기화의 미커밋 경고는 Exdigm 문서 push 실패가 아니다. Venture의 HEAD/원격은 같으며 미커밋 파일을 자동 stage하지 않고 보존했다.
- 다음: 별도 비동기 질문으로 새 내용 추출 범위(원문/AI 결과 대조, 필요한 추출 코드 수정·검증, 대상 파일의 후보자 저장)를 이번 작업에 포함할지 확인했다. 답 전에는 모델/프롬프트/검증 계약 변경이나 새 운영 재처리를 하지 않는다. CEO 메일 진단·직원 개인 연결도 남아 있다.

### 다른 작업·보관 변경 감사

- `인크루트 자동 게시 오류 진단`: df7d5c8a와 사람인 fa524969가20:12:21 공식 prod로 배포됐다. 그 작업의 문서 커밋2개만 미전송이었다.20:48 실제 게시 성공으로 과거 dry-only 대기 기록을 갱신했다.
- `이력서 업로드 오류 확인`: 코드 변경 없는 진단 작업이다. 같은 원인으로 찾은 사람인 추가1건을 이번4건 복구에 포함했다.
- `정렬 및 조건 선택 UI 변경`: 이미지 시안만 요청·제공된 작업이다. 미완성 제품 코드나 배포 대기 변경으로 보지 않으며 임의로 새 UI를 구현하지 않는다.
- `헤르메스 기본 모델 확인`:9d046ead 배포와 직원6개 실제 도구응답 검증 기록을 확인했고 현재 main에 포함됐다. 이번에는 Hermes 모델·fleet을 변경하거나 재배포하지 않았다.
- 최근 DB 오류 기록·게시 실패·후보자 이름 복구 작업도 완료 기록과 현재 main의 포함 관계를 대조했다. 모든 과거 대화의 미확정 제안을 구현한 것은 아니다.
- 원격 stash bf49f100은 이미 커밋·배포된 합동 변경의 복구용 백업이다.4f14aa7은9월11일 보존본이며 사람인 마감 검증은 후속 a78a42a0에 통합됐고 옛 화면 조건은 후속 승인된 게시 동작으로 대체됐다. 둘 다 대기 코드를 다시 적용하지 않고 보존했다.
- 조정실 동기화에 연결된 별도 Venture 저장소는 미커밋 작업을 보존한다.21:15 기준 HEAD와 실제 원격 main은89e8202b로 같아 전송할 별도 커밋은 없다. 이 프로젝트의 코드는 이번 Exdigm 변경 범위가 아니다.

### 사람인 실제 수집 결과와 새 추출 오류

| 원래 장부 | 수집 결과 | 실제 파일 검증 | 후속 내용 추출의 마지막 관측 |
|---|---|---|---|
| a2f861d4-49b8-420f-80da-d89ed791a904 |21:02:37 done, attempt5 |PDF74370바이트, FileData42165720-8855-4458-96b9-ecd6ad8e8274 | `invalid inclusive source_id range: S0027..S0004` |
|1adfd5d2-d618-48e4-b008-151b740f8202 |21:04:36 done, attempt4 |PDF55347바이트, FileDataaf4904e7-ade5-40c3-80f0-3fc5242ef543 | `extracted_content 필수 항목 누락: 경력` |
|328b654b-7009-45ee-89a0-61cbf138b08a |21:05:03 done, attempt4 |PDF79737바이트, FileData69826458-4349-478b-a3ef-5e6e88dfdc7b | `invalid inclusive source_id range: S0024..S0004` |
|492ac3cf-df57-4a7f-95d3-f525ee669918 |21:05:29 done, attempt4 |PDF115136바이트, FileData0978d1d7-64d0-473a-8b9f-6d58e91f9269 |21:20:26 후보자 정보 저장 완료 |

- 연속 질문: 왜 내용 추출이 멈췄나 → 역순 원문 범위2건/필수 경력 누락1건이 검증에서 거절됨(운영 FileData 확인). 왜 검증에서 거절됐나 → 기존 recommendation_resume_ssp의 범위·필수 항목 계약이 이를 금지함(코드 확인). 왜 그러한 결과가 생산됐나 → 원문 구성과 AI 응답의 단계별 대조 전이라 미검증.
- 버드뷰: 공식 수집 → 원문 읽기 → extract_resume_knowledge_from_text → validate → 후보자 저장. 수집과 텍스트 추출은 통과했으며 새 관측은 그 뒤 knowledge_validation이다. runners.text_to_pipeline_result가 해당 ValueError를 구체적 실패 사유로 기록한다.
- 결과 대조는 기존 수집 사건에서 후속 별도 실패로 확장됨이다. 상세 생산 원인을 확정하지 않았다. 범위를 거꾸로 뒤집는 수동 보정, 경력 임의 생성, 필수 검사 완화로 성공 처리하지 않는다.
- 추가 내용 추출은 기존 파일/추출 증거를 재사용해 조사해야 한다. 수집 장부9건이나 이미 성공한 인크루트 게시를 다시 실행하지 않는다.

## 20:12 배포 직후 재개 기록 — 사람인·인크루트 보완 배포 완료, 인크루트 운영 dry 통과

- 운영은 df7d5c8a85b3316fcb015f282690c935a2c4b86a다. 기존 합동 배포 d5d7ad78 위에 사람인 보완 fa524969와 인크루트 보완 df7d5c8a를 공식 prod로 fast-forward 배포했다. 아래 이전 시점의 미배포 기록은 경과 기록이다.
- 승인된 이력서 8건 중 인크루트4건과 잡코리아1건이 done이다. 5건 모두 실제 Drive 메타데이터의 파일 크기·MD5·정확한 대상 폴더·원래 장부 식별자를 확인했다. 원본 링크/메타데이터 해시 및 기존 알림 영수증은 유지됐다.
- 사람인3건은 미완료다. 첫 장부 a2f861d4-49b8-420f-80da-d89ed791a904는 attempt4 인쇄 준비 단계 실패, 나머지2건은 원래 attempt3을 유지한다. 성공한5건은 재실행하지 않는다.
- 사람인 추가 보완2파일은 fa524969aaba4aa1fd0686eeb8e69d02fc03daf4로 운영 배포됐다. 관련85개·보호194개·Django/Ruff/catalog/diff 검사 통과, 같은 base8c2ea773와 합동 계약의 재리뷰에서 승인 finding0개다. 사람인 실패 장부3건은 이번 실행에서 재처리하지 않았다.
- 인크루트 근무지역 UI 대응3파일은 df7d5c8a85b3316fcb015f282690c935a2c4b86a로 운영 배포됐다. 현재 버튼·선택 칩·실제 전송 코드를 한 경로에서 확인하고, 예전 rgn_cd/rgn_det 경로는 제거했다.
- 운영의 두 실패 payload는 site run ID를 제외한 정규화 SHA-256이 b5803522937e0d36b3cbc24b212961419519d29f4f611380dff755a46f68182f로 같다. 두 번째 실패 원본 payload로 공식 인크루트 dry를 실행해 경기와 성남시3개 구 선택·적용·최종 코드를 확인했다.
- 다음: 인크루트 실게시는 외부 공고를 만드는 별도 효과이므로 별도 확정 뒤 실패 사이트만 재시도한다. 사람인3건 재처리도 이번 실행 범위 밖이며 성공한5건은 재실행하지 않는다.
- CEO 메일502 상세 원인과 사용자 식별 기록 보완, bk 직원의 네 사이트 개인 연결은 이 배포로 해결되지 않았다. 운영 DB 인프라·Hermes·메일 검색 범위는 변경하지 않았다.
- 직접 실행했던 숨은 Chrome은 종료했고, 개발 runserver의 소유 프로세스 그룹3029160을 확인해 종료했다. 프로세스와8443리스너가 없음을 재확인했으며 운영 작업자11개는 active/jobs0/drain off, 서비스5개는1/1을 유지한다. 사용자 기존 운영 Chrome 탭은 열어 둔 채 원래 URL로 복원했다.

## 2026-09-15 합동 배포 승인 및 재개 상태

- 주인님은 검증된 오류 수정의 운영 배포와 실패 이력서 링크 8건의 재처리 제안에 `진행해`로 승인했다.
- 이어서 `같이 배포해`로 프로젝트 목록 정렬 변경 커밋 89fccbef45bb92ff09ea973b97bd896dc544e36c도 함께 배포하도록 승인했다. 아래 초기 기록의 보드 변경 제외 및 배포 승인 대기는 이 결정으로 대체한다.
- 배포 기준 base는 운영 main 8c2ea773723757da4422e698ac8b34c86a0fd2f8이다. 배포 대상은 정렬 변경 6파일과 오류 수정 8파일이다. 운영 DB 인프라, Hermes 설정, 계정 연결, 메일 검색 범위는 변경하지 않는다.
- 합동 검사: 오류 관련 235개와 프로젝트 화면 56개 통과, 프로젝트 화면 12개 실패 및 4개 setup 오류. 같은 공식 테스트를 기존 운영 판본에서도 실행해 동일한 16개 실패와 54개 통과를 확인했다. 새 정렬 검사 2개는 통과했다. 기존 실패를 삭제하거나 기대값을 완화하지 않는다.
- 기준선 비교 중 오류 수정은 stash bf49f10073093c66cd533747753f706a72929044에 보관했다가 89fccbef 위에 복원했다. 전후 전체 diff SHA-256은 140e39d2dd617a4aa02a7ed559b5a24fcbb79a421e7b6992f7b8c12673294d85로 일치한다. 복구용 stash는 보존 중이다.
- 실제 화면 검증: SSH 개발 서버 세션 28715, DB/역할 exdigm_debug, 메모리 이메일 backend, dev.exdigm.com, Windows 숨은 Chrome exdigm-errors-deploy, 1440x1000. 현재 정렬 후 필터/보기 전환의 상태 보존을 확인 중이다.
- 아직 제품 커밋 추가, push, 운영 배포, 실패 장부 상태 변경은 하지 않았다.

### 합동 코드 리뷰 계약

1. 원천: 오류 수정 요청과 본 문서의 보호 조건, 정렬 커밋 89fccbef 및 주인님의 함께 배포 승인.
2. 역할: 기존 이력서 수집의 실패 원인을 제거하고, 프로젝트 목록에서 선택한 기준과 방향대로 허용된 프로젝트를 표시한다.
3. 경계: base 8c2ea773부터 정렬 6파일과 오류 8파일 및 직접 호출/소비자. 정렬은 project_list → get_project_kanban_cards → 목록/보드 partial → 사용자 필터·보기 전환까지 확인한다.
4. 입력: 기존 URL/파일/사이트 입력 계약에 더해 허용 정렬 기준 3개, 방향, 사용자 권한, 검색·고객사·담당자·상태 필터.
5. 절차: 기존 수집 절차는 보존. 정렬은 GET 요청으로 보드와 URL을 갱신하고 후속 필터/보기 요청에서도 현재 선택을 전달한다. 업무 데이터 쓰기는 없다.
6. 출력: 실제 이력서 파일과 기존 장부 연결, 정상 문서만 수집; 화면은 선택한 기준·방향으로 정렬되고 필터와 접근 범위를 보존한다.
7. 보호: 본 문서의 수집 보호 조건, 직원별 프로젝트 접근 제한, 기존 검색/고객사/상태/보기 선택, 다른 작업의 커밋과 수정 내용.
8. 비목표: 정렬 UI 재설계, 기존 무관한 게시·기준 데이터 검사 실패 수정, DB 인프라/Hermes/인증 정책 변경, 메일함 검색 확대.
9. 검증: 동일 공식 테스트의 base 대조, 정렬·필터 실제 Chrome 상호작용, 보호 계약 194개, 배포 이미지 계약, 운영 Git/이미지/서비스/작업자 일치, 원래 장부 8건의 공식 작업자 결과.

리뷰 관점은 변경 diff와 직접 소비 관계이다. 기존 16개 검사 실패는 base에서도 재현됐으므로 이번 diff의 finding으로 승인하지 않는다. 실제 화면 결과와 운영 재처리는 개발 테스트 통과로 대체하지 않는다.

### 합동 리뷰 finding 및 수정 검증

- 승인 finding 1개: 정렬 HTMX 응답이 보드만 교체하므로, 보드 밖 필터/상태/보기 폼이 이전 sort_by·sort_open·sort_closed를 제출한다. 실제 Chrome에서 elapsed 선택 후 closed 전환이 document_deadline으로 돌아감을 재현했다.
- 연속 질문: 정렬 초기화는 이전 값 제출 때문(확인), 이전 값 제출은 바깥 폼 미갱신 때문(확인), 미갱신은 partial의 상태 전달 계약 누락 때문(확인). 버드뷰의 정렬 선택 → 보드/주소 → 후속 필터 요청과 대조해 최초 이탈이 일치함을 확인했다.
- 최소 구현 4단계: 기존 HTMX OOB 기본 기능을 사용한다. project_list의 partial 응답에 sync_sort_state를 설정하고 view_board에서 정렬 hidden input 3종만 OOB 갱신한다. 새 JS/라이브러리/저장 상태 없이 해당 책임에 통합했다.
- 기존 정렬 테스트에 검사를 추가해 red 1개를 확인한 뒤 수정하여 정렬 2개가 통과했다. 실제 Chrome에서 세 정렬 기준, 방향, 검색어, 진행/종료, 카드/리스트 전환의 선택 보존을 확인했다. 데스크톱/390px 화면 PNG를 직접 확인했고 가로 overflow는 없었다.
- 최종 같은 확대 검사: 291 passed, 기존과 동일한 12 failed 및 4 errors. 보호 계약 194 passed, 보호 파일18개 동일, check/Ruff/catalog/diff 검사 통과. 추가 회귀는 없다.
- 알려진 개발 화면 별도 항목: nginx Basic 인증에 따른 PWA manifest 401, 전체 이동 시 heartbeat 취소; 정렬 XHR과 실제 CSS는 200이며 정렬 조작 중 새 오류는 없었다. 이 범위를 이번 정렬 코드 결함으로 처리하지 않는다.
- 재리뷰: base 8c2ea773, 합동 14파일 및 직접 소비자를 같은 계약으로 재확인. 승인 finding 0개, 계약 변경이 필요한 열린 질문 없음. 정렬 상태 전달의 재발 경로는 실제 개발 화면 검증 범위에서 닫힘이다.
- 운영 사전 조회: 원래 링크 8건 모두 failed/attempt3/FileData 없음/claim 없음/실패 알림 발송 완료. source URL·metadata 해시를 기록했다. 프로젝트74개, 오류 기록14개이며 초기 조사 이후 추가 기록은 원래 11건과 구분한다.
- 배포 후 UI 확인에는 현재 로그인된 사용자 Chrome의 Exdigm 프로젝트 탭(1386260259)을 사용한다. 기존 금융 탭과 개인 인증은 이동·복사·종료하지 않는다.

### 18:16 운영 반영과 실제 이력서 재처리

- d5d7ad7869384a12eec37c8748474c1a99455503 공식 prod 배포가 18:16:04 KST exit0으로 완료됐다. debug/origin/main/운영 main/실행 app·SSE·notification 모두 같은 판본, 서비스5개1/1, 작업자11개active/read-write/drain off, HTTPS200이다.
- 실제 Chrome 운영 화면에서 새 정렬 기준이 표시되고, 경과일수 선택 후 종료 전환에도 선택이 유지됐다. 원래 URL `?sort_open=desc&status_view=open`로 복원했다. 이 전환은 프로젝트 내용 수정이나 게시를 하지 않는다.
- 원래 장부8개와 관련 메일기록8개를 `/home/chaconne/exdigm-debug/.debug/operational-errors-20260915-before-retry.json`에 0600으로 백업했다. SHA256 fa6c30af63d2b7fd26f57e3b76c4e668ab419d0629556108ee52ef222e261408. 민감한 원문 URL/메일 내용은 채팅·Git·GBrain에 기록하지 않는다.
- 정확한 ID·source URL/metadata 해시·failed/attempt3/FileData 없음/claim 없음 조건을 확인하고 원래 장부를 waiting으로 돌렸다. 기존 운영 resume-uploader가 claim과 모든 다운로드·업로드를 수행한다. attempt budget과 이전 알림 영수증은 초기화하지 않았다.
- 인크루트4개는 attempt4에서 done. 실제 Drive 메타데이터의 크기(75564/89888/34732/59470바이트), MD5, 대상 폴더, 장부 appProperty가 모두 일치하고 각각 메일 uploaded_count1이다.
- 잡코리아1개도 attempt4에서 done, 186610바이트, FileData a09b609d-aa12-4d75-9c04-98508f5df61e, Drive 1WLY1k4DM6fwJ309wD1WSgEiBbpvDGDag. 기존 성공 첨부를 포함한 해당 메일 uploaded_count는2다.
- 사람인 첫건 a2f861d4-49b8-420f-80da-d89ed791a904는 지원자 선택을 통과했으나 인쇄 버튼 검사에서 실패했다. 나머지 사람인2개는 재시도하지 않고 보존했다. 8개 중5개 완료이며 사람인3개는 아직 미완료다.

### 사람인 인쇄 단계 재개 게이트

- 현상 잠금: 정확한 지원자의 공식 PDF를 Drive/FileData/메일 결과로 전달해야 한다. 첫 운영 재처리에서 `Saramin resume print control is not unique`가 발생해 완료 조건을 축소하지 않고 조사를 재개했다.
- 연속 질문1: 왜 인쇄 확인이 실패하는가? 버튼/대화상자가 표시되기 전 count가0이다(확인: 실제 원격 공유 브라우저9223).
- 연속 질문2: 왜 표시 전에 검사하는가? 고정3.27초 시점에 인쇄 버튼이0이고,4.30초에1개가 된다. 버튼 클릭 직후 대화상자도0이었다가 visible 대기 후1개다(확인).
- 연속 질문3: 왜 고정시간/즉시 count를 사용하는가? 상세 버튼의 준비 계약이 없고, 대화상자는 기존 wait_for보다 count를 먼저 실행한다(확인: Saramin.download_resume).
- 버드뷰: 목록→지원자 선택→상세→인쇄 대화상자→공식 print popup/PDF의 단계별 생산 구조에서 상세/대화상자 준비 확인이 최초 이탈이다. 결과 대조는 일치. 선택한 인쇄 사유는 즉시 표시됐으므로 이 부분은 바꾸지 않는다.
- 최소 구현2단계: 기존 wait_for를 재사용한다. 인쇄 버튼은 표시 후 유일성 확인, 대화상자는 기존 표시 대기를 유일성 확인 앞으로 이동한다. 후보 신원/경력/인쇄 사유/PDF 검증 및 기존 로그인 대기는 유지한다. 새 분기·의존성·실행 경로는 없다.
- 기준선: d5d7ad78 clean에서 브라우저42개 통과. 기존 테스트를 목록/인쇄버튼/대화상자의 즉시·지연 표시8조합으로 확장해 red6개/성공2개 확인. 수정 후 전체 browser+uploader85개 통과. 보호194·Ruff·catalog·diff 검사 및 같은 base8c2ea773의 합동 리뷰를 수행한다.
- 재리뷰 계약은 기존 합동 계약을 유지한다. 이번 차이는 Saramin.download_resume와 기존 브라우저 테스트2파일뿐이며 이미 배포된 나머지 파일과 복구된5개 장부는 보호한다. 추가 운영 반영/사람인 재처리 전 상태다.

### 인크루트 근무지역 UI 변경 문제해결

#### 현상 잠금

- 17:50 실행59443dee-c2af-4eaa-9728-e03e134c2033과18:03 재실행 ac3e3ba5-9920-451f-9162-3f3f034d126d는 같은 HR Manager 공고다.
- 둘 다 로그인·공고 입력·업종 선택 뒤 근무지역에서 `country-missing: 경기`로 멈췄다.
- 입력은 `경기`와 `성남시 분당구·수정구·중원구`다. 외부 공고 ID와 URL은 비어 있어 인크루트 게시 효과는 없다.
- 필수 결과는 현재 인크루트 화면에서 세 지역을 정확히 선택하고, 게시 전 최종 전송 코드까지 확인하는 것이다.

#### 연속 질문

1. 왜 두 실행이 멈췄는가? `_select_region`이 `경기` 컨트롤을 찾지 못했다. 두 운영 장부와 증거 JSON에서 확인했다.
2. 왜 `경기` 컨트롤을 찾지 못했는가? 코드는 제거된 `rgn_cd/rgn_det` 입력만 조회했다. 현재 공식 JS에는 해당 입력이 없고 대분류·소분류 버튼을 쓴다.
3. 왜 제거된 입력을 계속 조회했는가? 9월10일 성공 당시 JS 계약과 코드·필드맵은 일치했으나, 9월15일 인크루트 JS 변경 뒤 직접 지역 DOM 회귀 검사가 없었다.

#### 버드뷰

- 정상 흐름은 성남 근무지 의미 결정 → 필드맵 라벨 검증 → 인크루트 현재 지역 데이터에서 코드 확인 → 대분류·소분류 버튼 선택 → 선택 칩 생성 → 적용 → 최종 `selRegion` 코드 확인 → 게시다.
- 의미 결정과 필드맵은 정상이다. 최초 이탈은 필드맵에서 현재 화면 버튼으로 이어지는 기계적 연결이다.
- 오류는 그 뒤의 관측 지점이며, 작업자·로그인·업종·다른 사이트는 원인이 아니다.

#### 결과 대조

- 연속 질문과 버드뷰는 일치한다. 같은 입력의 두 실행이 같은 지점에서 멈췄고, 9월10일 구형 화면 성공과 9월15일 신형 공식 JS의 차이가 연결된다.

#### 근본 원인 판정

- 인크루트가 지역 선택 DOM을 `rgn_cd/rgn_det` 입력에서 `JB_RegionData` 기반 대분류·소분류 버튼과 `selRegionLayer` 칩으로 교체했다.
- Exdigm의 지역 선택기와 필드맵이 구형 DOM 계약에 남아 있어 정상 지역 입력이 현재 화면까지 전달되지 않았다.
- 같은 코드를 변경 없이 재실행한 18:03 알림은 이 결함의 반복이며 별도 원인이 아니다.

#### 해결책

- 공식 경로 `manage.py autoposting → AutoPosting.main → Incruit.run → Incruit._select_region`은 유지했다.
- 기존 선택기를 현재 `JB_RegionData`와 버튼·칩 계약으로 교체했다. 우회 실행기나 구형 fallback은 추가하지 않았다.
- 적용 전 회사 주소·재택 기본값을 비우고, 적용 후 라벨뿐 아니라 부모·상세 전송 코드까지 정확히 일치해야 성공으로 인정한다.

#### 적용·검증

- 수정 전 현재 UI 계약 검사3건이 실패했고, 수정 후 모두 통과했다. 국내 원래 사례와 해외 변형을 포함한다.
- 최종 커밋 df7d5c8a에서 직접 확대 검사297건, 인크루트 명칭 검사46건, 보호 계약194건이 통과했다.
- Django check, Ruff, code knowledge catalog, diff 검사가 통과했다. 최종 코드 리뷰 승인 finding과 열린 계약 질문은0개다.
- 자동 게시 전체419건 중399건은 통과했다. 나머지20실패·1정리 오류는 개발 테스트 DB의 고아 FK, 개발 서버 게시 금지 설정, 최근 별도 변경의 기존 기대값 불일치이며 이번3파일과 인과가 없다.
- 전체 검사 뒤 개발 테스트 DB에 남은 `boss_u` 1개와 연결 객체1개만 `exdigm_debug_test`에서 삭제했다. 운영 DB는 변경하지 않았다.
- 주인님의 `진행해` 승인 뒤 2026-09-15 20:12:21 KST 공식 `scripts/deploy/deploy.sh prod`가 `prod ok df7d5c8a`, exit0으로 완료됐다. debug detached HEAD·origin/main·운영 clean main·실행 app/SSE/notification의 source commit이 모두 df7d5c8a와 일치한다.
- 새 이미지 `exdigm_app:20260915201057`의 보호 파일18개와 DOC/DOCX/PDF 실제 전달 계약을 통과한 뒤 서비스가 교체됐다. 서비스5개1/1, worker/support11개 active/jobs0/drain off, HTTPS200이다. DB 인프라와 Hermes는 배포하지 않았다.
- 운영 공유 브라우저를 재사용해 공식 `manage.py autoposting --site incruit --mode dry`를 원래 두 번째 실패 payload로 실행했다. run_id `20260915201436`, exit0, result `ok=true`, `submitted=false`, `published=false`, error 없음이다.
- 현재 인크루트 화면에서 경기 부모코드18과 분당구1822·수정구1845·중원구1846을 선택했다. 선택 칩, 적용 뒤 최종 `selRegion` 항목, 원래 요구값이 모두 정확히 일치했다. 증거는 운영 `runtime/auto_posting/evidence/incruit/20260915201436`에 있다.
- 공식 shell-readonly에서 `exdigm_debug_ro`, transaction_read_only on을 확인했다. 원래 두 AutoPostingSiteRun은 failed 상태와 같은 오류를 보존하고 외부 ID·URL은 계속 비어 있다. dry 뒤 새 사이트 실행 장부나 외부 게시 효과는 없다.

#### 재발 판정

- 개발 범위는 닫힘이다. 원래 `경기/성남3개 구`, 해외 변형, 기존 자동 게시 소비 경로가 최종 코드에서 통과했다.
- 운영의 같은 원인 재발 경로는 dry 범위에서 닫힘이다. 배포된 df7d5c8a와 실제 로그인 세션, 원래 실패 payload, 현재 인크루트 UI를 사용한 공식 경로에서 지역 선택부터 최종 전송 코드 확인까지 통과했다.
- 실제 외부 게시 결과는 미검증이다. 이번 dry는 등록을 제출하지 않았으며, 실패 사이트 실게시 재시도는 별도 확정 전까지 실행하지 않는다.

## 목표와 승인 범위

- 주인님 요청: DB에 남은 오류 내역에 문제해결 게이트를 적용해 수정한다.
- 원천 기록: projects.OperationalError 12건 중 배포 테스트 1건을 제외한 11건.
- 구현·격리 검증·리뷰·작업 문서화는 현재 요청 범위다. 운영 배포·실패 장부 재처리·외부 계정 연결 변경은 실제 영향과 검증 결과를 보고하고 별도 확정한다.
- 서버: chaconne@49.247.202.197. 수정 위치: /home/chaconne/exdigm-debug.
- 기준선: 운영 clean main 및 개발 clean detached HEAD 모두 8c2ea773723757da4422e698ac8b34c86a0fd2f8.
- 조정실 controlroom도 변경 시작 시 clean이었다.

## 현상 잠금

| 묶음 | 기록 수 | 필수 결과 | 원래 실패 지점 |
|---|---:|---|---|
| 인크루트 링크 | 4 | 메일의 이력서 원본을 내려받아 기존 Drive 업로드 장부에 연결 | HTTP 이동 주소 UTF-8 해석 오류 |
| 사람인 링크 | 3 | 메일이 지목한 정확한 지원자의 이력서 파일 확보 | 후보자 행 0개 |
| 잡코리아 링크 | 1 | 지원 이력서 공식 다운로드 | 다운로드 버튼의 개수/표시 상태 검사 |
| Drive 임시 파일 | 1 | 실제 문서만 처리하고 임시 소유자 파일은 수집 대상에서 제외 | 162바이트 ~$ DOCX를 문서로 처리 |
| 외부 검색 | 1 | 허용된 본인 연결로 사이트별 검색, 기존 성공 결과 보존 | 네 사이트 개인 로그인 정보 없음 |
| CEO 회사 메일 | 1 | 본인 메일함 검색을 정상 목록/빈 목록으로 반환 | /agent/email/messages/ HTTP 502 |

## 연속 질문과 직접 근거

### 인크루트

1. 왜 다운로드가 멈췄는가? HTTP 이동 주소를 읽는 중 UTF-8 해석이 실패했다. 확인: 원래 실패 링크에서 동일 바이트 0xb9와 position 217 재현.
2. 왜 HTTP 이동 주소를 읽는 중 UTF-8 해석이 실패했는가? Requests가 Location 헤더의 원래 바이트를 UTF-8로 가정한다. 확인: sessions.get_redirect_target → to_native_string 호출 경로.
3. 왜 Requests가 Location 헤더의 원래 바이트를 UTF-8로 가정하는가? 라이브러리 기본 정책이며 현재 다운로드 통합 경계가 비 UTF-8 이동 주소를 URL 표기로 보존하지 않는다. 확인: allow_redirects=False여도 다음 요청 준비 중 예외가 발생한다.

### 사람인

1. 왜 후보자 행 0개로 실패했는가? 비동기 후보자 목록이 나타나기 전에 행을 센다. 확인: 페이지 틀은 0.37초, 이름 컨트롤은 약 5.04초에 표시.
2. 왜 비동기 후보자 목록이 나타나기 전에 행을 세는가? 고정 2.5초 대기 후 count를 즉시 호출한다. 확인: Saramin.download_resume.
3. 왜 고정 2.5초 대기 후 count를 즉시 호출하는가? 이 단계에 실제 목록 준비 계약이 없다. 확인: 같은 선택자는 준비 뒤 20명과 세 실패 대상의 이름을 반환한다.

### 잡코리아

1. 왜 다운로드 버튼을 확정하지 못했는가? 버튼 개수 또는 표시 상태가 즉시 검사에서 맞지 않았다. 확인: 저장된 오류를 발생시키는 분기.
2. 왜 버튼 개수 또는 표시 상태가 즉시 검사에서 맞지 않았는가? 고정 2초 뒤 준비 대기 없이 검사한다. 확인: 현재 버튼은 하나이며 표시 완료를 기다리면 존재한다. 당시 정확한 화면은 미검증.
3. 왜 고정 2초 뒤 준비 대기 없이 검사하는가? 페이지 틀과 다운로드 컨트롤 준비를 구분하는 계약이 없다. 코드 결함은 확인, 원래 사건의 유일 원인 여부는 미검증.

### Drive

1. 왜 DOCX에서 텍스트가 없었는가? 읽기 대상은 162바이트의 ~$ Office 임시 파일이다. 확인: 운영 FileData의 이름·크기·처리 상태.
2. 왜 읽기 대상은 162바이트의 ~$ Office 임시 파일인가? Drive 수집이 확장자/MIME을 근거로 문서 처리 대상으로 넘겼다. 확인: drive_batch 경로와 build_file_row.
3. 왜 Drive 수집이 확장자/MIME을 근거로 문서 처리 대상으로 넘겼는가? 공통 분류에 Office 임시 파일 제외 조건이 없다. 확인: sync_drive_changes → build_file_row → decision_for_row → FileData/manifest.

### 외부 검색

1. 왜 네 사이트 검색이 시작되지 않았는가? 사용자 로그인 정보가 없다. 확인: 저장된 site result.reason.
2. 왜 사용자 로그인 정보가 없는가? 해당 사용자에게 ExternalSiteCredential이 등록되지 않았다. 확인: 운영 조회 0행.
3. 왜 해당 사용자에게 ExternalSiteCredential이 등록되지 않았는가? 등록 경위는 확인하지 못했다. 코드의 타인 계정 대체 근거는 없다. 미검증/사용자 연결 필요.
- LinkedIn 성공 후보자 15명은 보존한다. 전체 검색을 재실행하거나 다른 직원 인증을 복사하지 않는다.

### CEO 메일

1. 왜 요청이 502로 끝났는가? 회사 메일 서비스가 읽기 실패를 반환했다. 확인: nginx/app 로그와 응답 분기.
2. 왜 회사 메일 서비스가 읽기 실패를 반환했는가? 당시 상세 예외는 저장되지 않아 미확인이다. 로그인/검색/개별 메일 fetch를 구분해야 한다.
3. 왜 당시 상세 예외는 저장되지 않아 미확인인가? 응답으로 변환된 예외는 일반 502 로그가 되고 현재 수집기는 method/path/status만 보존한다. 확인: company_email 및 OperationalErrorHandler.
- 실패 요청은 12:52:33 KST, query는 Webinar 제목이며 88.461초 소요. 12:54:30의 limit=1 요청은 200, 65.991초.
- 주인님 확인: CEO가 Telegram Hermes에 요청했고 대상 메일은 휴지통에 있었을 가능성이 있다.
- 현재 API는 INBOX를 기본으로 고정한다. 휴지통 미검색과 502 원인은 분리한다. 검색 결과 없음의 정상 출력은 빈 목록이다.
- CEO 메일 및 BOSS AgentProfile은 active이며 연결 user_id가 일치한다. 오류의 사용자 번호 누락은 수집 항목 누락으로 확인했다.
- 개발 조회 전용 환경은 운영 메일 비밀번호를 복호화할 수 없어 InvalidToken으로 외부 조회 전에 중단됐다. 운영 인증 장애의 증거가 아니다.

## 버드뷰

- 메일 링크 → 공통 다운로드 → 사이트 이력서 파일화 → Drive → FileData/Resume/Candidate의 기존 책임을 유지한다.
- 임시 파일은 원천의 대상 판별 단계에서 제외해야 한다. 추출 실패를 숨기거나 가짜 텍스트로 바꾸지 않는다.
- 검색은 사용자별 인증이 필수이고 성공 사이트 결과를 보존한다.
- 메일 API는 사용자에 연결된 메일함만 읽는다. 사용자 번호/에이전트 번호 누락과 실제 연결 실패는 같지 않다.

## 결과 대조와 근본 원인 판정

- 인크루트·사람인·Drive: 연속 질문과 버드뷰가 일치한다.
- 잡코리아: 준비 확인 결함은 확인. 당시 증거가 없어 유일 원인 판정은 보류한다.
- 검색: 로그인 정보 부재 확인. 사용자 등록이 필요한 경계다.
- 메일: 요청 시간·CEO 계정·휴지통 가설로 분석이 확장됐다. 상세 실패 원인은 미확정이다.

## 변경 범위와 최소 구현

- data_extraction/services/resume_uploader.py: Requests 응답 훅으로 Location의 원래 바이트를 percent-encoding. UTF-8/CP949와 기존 percent escape를 함께 보존하며 기존 수동 redirect 상한·허용 주소·내부망 차단 유지.
- auto_posting/sites/saramin.py 및 jobkorea.py: 기존 로그인/추가 인증 전환 대기는 보존하고, 선택 직전에 기존 컨트롤의 표시 대기를 추가. 이름/경력·유일성·원본 검증 보존.
- data_extraction/services/drive_inventory.py: 기존 분류에 Office 임시 파일 제외 추가. 실제 이력서와 지원 포맷 보존.
- 기존 관련 테스트 파일에 실제 실패 조건·변형·정상 사례를 추가한다.
- 최소 구현 게이트: 화면/분류 2단계 재사용, HTTP 주소 5단계 설치된 Requests response hooks 및 표준 urllib.parse. 새 라이브러리/서비스/실행 경로 없음.

## 보호 불변조건과 기준선

- 원본 파일, 업무 DB, 기존 장부, 후보자 신원 매칭, 현재 프로필, 정상 사이트 수집 및 기존 API 권한을 보존한다.
- 운영 DB는 shell-readonly의 exdigm_debug_ro/read_only on으로만 조사했다.
- 수정 전 공식 검사: uploader/incruit/jobkorea/search/operational records 83 passed.
- 추가 기준선: browser resume/Drive inventory/sync_drive_changes 27 passed.
- 실패 재현 검사 추가 → 원래 코드 실패 → 최소 수정 → 같은 검사 통과 → 전체 diff 리뷰 순서로 검증한다.

## 재개 정보

- 상태: 위 제품 코드 4개와 기존 테스트 4개 수정. 확대 검사 235 passed, 차이 축소 뒤 브라우저 경로 42 passed, 보호 계약 194 passed. 코드 리뷰 승인 finding 없음.
- 개발 worktree에 다른 작업의 프로젝트 보드 변경 6개 파일이 새로 나타났다. 해당 파일은 수정·스테이징·커밋 대상에서 제외한다.
- CEO Hermes의 기존 container 로그에서 12:52 요청이 인증 뒤 `company_email_read_failed / 메일 목록을 완전하게 불러오지 못했습니다.`로 끝난 사실을 확인했다. `_fetch_message`가 None을 반환한 분기까지 특정했으나 서버 거절과 UID 소실은 현재 기록으로 구분할 수 없다.
- 오류의 사용자·프로필 ID 기록 보완은 별도 비동기 질문으로 확인 중이다. 아직 해당 코드는 수정하지 않았다.
- 외부 검색 미등록 계정은 bk@exdigm.com이다. 해당 직원의 /accounts/settings/extension/에서 네 사이트 개인 연결이 필요하다. 기존 LinkedIn 성공 결과 15명은 그대로 둔다.
- 다음: 별도 승인 후 운영 반영 및 실패 링크 8건의 공식 장부 재처리를 진행한다. 메일은 읽기 거절/조회 도중 UID 소실을 구분할 운영 근거가 더 필요하다. 휴지통 검색 확장은 별도 제품 범위로 두고 임의 적용하지 않는다.
- 운영 배포, 장부 재처리, 인증 변경은 실행하지 않았다.

## 적용·검증 증거

| 대상 | 수정 전 실패 확인 | 수정 후 확인 |
|---|---|---|
| 사람인 | 지연 표시 조건에서 matches=0, 즉시 표시 성공 | 두 조건 모두 공식 download_resume의 PDF 결과 확인 |
| 잡코리아 | 숨겨진 버튼 조건에서 not unique, 즉시 표시 성공 | 두 조건 모두 공식 download_resume의 암호 ZIP 결과 확인 |
| 인크루트 | CP949 두 변형 UnicodeDecodeError, UTF-8 주소 이중 인코딩; ASCII 성공 | 실제 Requests 처리 계층을 거쳐 업로드 장부 DONE, FileData 연결, 업로드 1회 |
| Drive | 분류 변형 5개 및 공식 sync 단계가 임시 파일을 생성/대기열에 넣음 | 임시 파일 제외, 실제 문서 FileData/manifest 생성, 커서 정상 전진 |

- `scripts/debug_workspace.sh test tests/test_auto_posting_incruit_waits.py tests/test_auto_posting_jobkorea_references.py tests/test_resume_uploader.py tests/test_candidate_sourcing.py tests/test_operational_error_records.py tests/test_auto_posting_browser_runtime.py tests/test_inventory_01db_drive_tree.py tests/test_update_candidates_results.py --tb=short`: 235 passed, 7 warnings, 46.09초.
- 운영 원래 인크루트 링크 4개를 개발 코드의 `_download_resume_link`로 읽기만 재검증했다. UnicodeDecodeError는 사라졌고 인증 경로에 도달했다. 이후 개발/운영 복호화 키 분리로 InvalidToken에 막혔으므로 실제 파일 전달까지 통과했다고 판정하지 않는다.
- 운영 장부 상태를 바꾸거나 운영 비밀번호를 개발에 공급하지 않았다.
- 마지막 코드에서 `scripts/debug_workspace.sh test tests/test_auto_posting_browser_runtime.py --tb=short`: 42 passed.
- `scripts/debug_workspace.sh contracts`: 고정 기준 6ac315e930880406f6f7f9d489322b07b85e380f의 보호 파일 18개 유지, 194 passed.
- `scripts/debug_workspace.sh check`, `uv run --locked python -m tools.code_knowledge catalog_update`, 변경 8개 파일 `ruff check`, `git diff --check`: 모두 통과. 목차 파일 변경 없음.

## 코드 리뷰 계약

1. 원천: 주인님의 DB 오류 수정 요청, 본 문서의 현상 잠금·보호 불변조건, 현재 AGENTS.md 및 해당 기능 스킬.
2. 역할: 메일 이력서 링크를 기존 수집 장부에 전달하고, Drive에서 실제 문서만 기존 처리 대기열에 전달한다.
3. 경계: base 8c2ea773723757da4422e698ac8b34c86a0fd2f8에서 제품 코드 4개와 테스트 4개의 로컬 diff. 직접 호출자인 process_resume_upload/AutoPosting.main/sync_drive_changes 및 직접 분류 소비자까지만 확인한다.
4. 입력: 허용 사이트 HTTPS URL, HTTP Location 원래 바이트, 공식 사이트 페이지와 후보자 힌트, Drive 파일명·확장자·폴더 메타데이터. 기존 인증 필수조건은 유지한다.
5. 절차: 주소 검사 후 수동 제한 이동, 인증 전환 후 대상 유일성 검사와 공식 파일화, Drive 분류 후 기존 DB/manifest 및 커서 처리 순서 보존.
6. 출력: 지원 형식의 실제 이력서 바이트와 이름/MIME, 기존 장부 상태·FileData 연결; 정책 제외 파일은 새 처리 대상이 되지 않고 정상 문서는 계속 전달된다.
7. 보호: 내부 주소 차단, 10MB/이동 상한, 중복 업로드 방지, 정확한 지원자 선택, 기존 인증/추가 인증, 사용자 작업·업무 데이터·기존 오류 기록.
8. 비목표: 메일함 검색 범위 확대, 운영 계정 연결 변경, 운영 배포/장부 재처리, 프로젝트 보드 변경, 전체 이메일 성능 재설계.
9. 검증: 위 재현 테스트와 기존 성공 검사, 공식 debug_workspace 테스트 및 contracts/check, diff 검사. 외부 사이트 인증 뒤 실파일 전달과 실제 메일 서버의 실패 응답 구분은 아직 확인하지 못했다.

리뷰 관점은 변경 diff와 1차 호출/소비 관계다. 변경으로 보호 조건이 깨지는 실행 가능한 사례만 finding으로 승인한다. 알려진 운영 인증 경계와 502의 미확정 원인은 별도 미검증 항목이며 코드 리뷰 통과로 대체하지 않는다.

### 코드 리뷰 결과

- 승인된 finding이 없다. 계약을 바꿔야 하는 열린 리뷰 질문도 없다.
- Requests 응답 훅 이후에도 매 이동 전 HTTPS/주소 검사와 상한이 유지된다. 파일 업로드 장부·중복 처리 경로는 변경하지 않았다.
- 사이트 이름/경력 매칭·유일성·공식 파일 형식 검증은 유지했다. 로그인·추가 인증 관련 기존 대기도 보존했다.
- Drive의 신규 목록 수집과 변경분 수집이 같은 분류를 재사용한다. 제외 표시는 기존 decision_for_row 및 manifest 소비자로 이어진다.
- 운영 인증 뒤 실파일 전달과 당시 IMAP 응답의 상세 실패 조건은 검증하지 못했다.

## 재발 판정과 종료 경계

| 경로 | 판정 | 근거와 남은 일 |
|---|---|---|
| 한글 Location 처리 | 검증 범위 닫힘 | 원래 운영4건이 공식 worker로 실제 Drive/FileData/메일 및 후보자 저장 완료. 원본과 기존 알림 보존 |
| 사람인 준비 대기 | 수집 경로 검증 범위 닫힘 | 원래3건과 같은 원인의 추가1건 모두 공식 worker의 PDF/Drive/FileData/메일 집계 완료. 내용 추출 오류는 아래 별도 경로 |
| 잡코리아 준비 대기 | 수집 경로 검증 범위 닫힘 / 과거 단일 원인 미검증 | 원래 운영1건의 실제 파일 전달과 후보자 저장 완료, 지연 변형 회귀 통과. 당시 화면 상태의 유일 원인은 단정하지 않음 |
| 인크루트 근무지역 | 검증 범위 닫힘 | df7d5c8a 운영 배포와 동일 payload dry 통과. 원래 프로젝트에서20:48 실제 submitted/published true 및 현재 PostingSite 연결 확인. 공고2609150004510 |
| Drive Office 임시 파일 | 신규 유입 경로 개발 범위 닫힘 | 공식 sync 단계에서 임시 파일 제외와 정상 문서 DB/manifest 전달 검증. 기존 실패 기록은 보존 |
| 새 사람인 내용 추출 | 열림 / 근본 원인 미검증 | 실제 파일 수집 후3건의 knowledge_validation 실패를 확인. 원문·AI 결과 대조와 별도 추출 수정 범위 확인 필요. 추가1건은21:20:26 후보자 저장 완료 |
| 외부 검색 | 열림 | 해당 직원의 네 사이트 계정 등록 필요. 다른 직원 인증을 대체 사용하지 않음 |
| CEO 메일 502 | 열림 | 인증 이후 개별 메일 fetch의 None 분기 확인. IMAP 거절과 UID 소실은 기록만으로 구분 불가 |

- 전체 오류 해결은 미완료다. 승인된 미배포 변경 감사와 파일 수집9건 복구, 원래 인크루트 실제 게시 확인은 끝났다. 새 사람인 내용 추출, 사용자 연결, CEO 메일 상세 원인이 남아 있으며 수집 완료를 후보자 저장 완료로 확대하지 않는다.
- 제품은 df7d5c8a까지 push·운영 배포됐고 운영 main은 clean이다. DB 인프라·Hermes·메일 검색 범위는 변경하지 않았다.
- 주인님이 당시 요청은 CEO Telegram/Hermes라고 답한 사실은 반영했다. 봇 연결 실패는 확인되지 않았고 과거 오류 DB의 사용자 식별자를 추정해 소급 기입하지 않는다. 앞으로 식별자 기록을 보완하는 코드와 메일 상세 진단은 아직 미구현이다.

## 이력서 번역·PDF 배치 근거 최신 완료 — 2026-09-16 15:32 KST

### 현상과 근본 원인

- 실제 PDF 화면에서는 `희망연봉`과 `회사내규에 따름`이 같은 카드 안에 명확히 배치돼 있었다.
- 기존 PDF 텍스트는 값을 먼저, 라벨을 나중에 반환했다. 텍스트와 source id만 받은 분류 단계는 둘의 같은 카드 관계를 잃어 희망연봉 값을 현재·최종 급여로 오인할 수 있었다.
- 문제는 번역이 아니라 PDF의 시각 배치 정보가 source id 계약에서 사라진 기본 데이터 추출 보조정보 결함이다. 기존 텍스트 추출 순서·원문·분류 구조는 유지해야 한다.

### 최소 구현과 SSP

- 기존 PyMuPDF 추출 결과에 새 파서나 새 AI 호출을 더하지 않았다.
- 기존 `page.get_text()`의 텍스트와 순서는 그대로 두고, 같은 호출에서 얻는 페이지 번호·정규화 좌표·그려진 박스 영역만 metadata로 기록했다.
- 기존 L source id에 해당 배치 참조를 붙이고, 국문 기준자료가 여러 원문 행을 합치면 그 참조도 함께 승계한다.
- 배치 정보는 번역·언어 판정·신원 판정에는 전달하지 않고 기존 source-map 분류 호출에만 전달한다. 한 기록의 같은 페이지·영역 좌표는 하나의 합집합 상자로 압축한다.
- 기존 PDF 캐시는 `pdf_layout_v1` 계약이 없으면 한 번 다시 추출한다. 지식 fingerprint에는 실제 배치 metadata를 포함해 텍스트가 같아도 배치가 달라지면 이전 지식을 재사용하지 않는다.
- 원문 의미 보존 번역, 국문 완성 뒤 영문 1회 번역, Gemini 번역 호출 1회의 medium 추론은 앞서 승인된 범위 그대로 함께 반영했다. 기본 항목 분류·추출 순서·모델·자기소개 요약은 바꾸지 않았다.

### 실제 원본 결과 대조

- 문제 원본 PDF SHA-256은 `8e113105060d56da852bcd9160b9c2e22eb5be2796bd6fe87af4f1c4bf63bc10`이다.
- 변경 전후 추출 텍스트 SHA-256은 모두 `09605dc7cb34a70b868a4a77a1ad4827b33830522c307302de3079ec419fe26a`로 같아 텍스트·순서가 바뀌지 않았다.
- 값 `회사내규에 따름`과 라벨 `희망연봉`은 모두 1쪽의 같은 `P001R007` 영역으로 연결됐다.
- 실제 전체 생성 경로 결과에서 현재·최종조건에는 직함만, 직전조건에는 직함만 남고, 희망조건에 `희망연봉 = 회사내규에 따름`이 들어갔다.
- 영문도 같은 국문 결과만 번역해 `Desired Salary = Subject to company policy`로 생성됐다.
- 배치정보를 모든 AI 호출에 노출한 초기 시도는 번역 입력을 불필요하게 키워 마지막 행 계약 실패를 만들었다. 이를 폐기하고 source-map에만 전달하는 현재 SSP로 축소한 뒤 같은 원본 전체 경로가 통과했다.

### 검증·리뷰·배포

- PDF 배치·지식 계약 검사 72개, 공용 LLM 검사 28개, 번역 추론 범위 검사 21개가 통과했다.
- 보호 파일 18개와 공식 계약 검사 216개가 새 승인 기준에서 통과했다.
- 직접 영향 확대 검사는 237개 통과했다. 9개 실패는 공유 개발 테스트 DB에 `submit_to_pm` ActionType이 없는 기존 기준선 실패로 이번 변경과 무관하다.
- `data_extraction/tests.py` 41개, Django check, Ruff, code knowledge catalog, diff 검사가 통과했다.
- Direct Review에서 승인 finding과 열린 코드 계약 질문은 0개다.
- 커밋 `20355d8afacb236cb910364e0f9c81d560eaa267`을 `origin/main`에 push하고 2026-09-16 15:29 KST 공식 `scripts/deploy/deploy.sh prod`로 운영 배포했다.
- debug HEAD·origin/main·운영 main·보호 기준 pin·양쪽 보존 ref·실행 app/SSE/notification source commit이 모두 `20355d8a`와 일치한다.
- 서비스 4개가 1/1, worker/support 11개가 active/jobs 0/drain off, 운영 HTTPS가 HTTP 200이다. DB 인프라와 Hermes는 배포하지 않았다.

### 재발 판정과 남은 경계

- 희망연봉 오분류의 같은 원인 재발 경로는 실제 원본을 사용한 공식 전체 생성 경로와 배포된 동일 커밋 범위에서 닫힘이다.
- 기존 후보자·이력서 DB를 덮어쓰거나 성공 데이터를 재처리하지 않았다. 따라서 과거 저장 결과의 소급 교정은 실행하지 않았다.
- 별도 기존 성공 PDF를 새로 생성한 한 번의 결과에서는 번역 모델이 학력·면허 원문 여러 행을 하나의 긴 국문 기록으로 합친 뒤 source-map이 일부 사실을 공유하지 못했다. 고정된 같은 국문 기준자료에서 배치정보 유무를 비교하면 결과가 같았으므로 이번 PDF 배치 변경의 회귀는 아니다. 다만 번역·source-map의 기존 확률적 품질 위험은 별도 열린 항목이다.
- 배포 직후 공유 디버깅 worktree에 다른 활성 작업 `잡코리아 자동 게시 오류 확인`이 만든 auto-posting 4개 파일의 미커밋 변경이 나타났다. 이력서 커밋·배포에는 포함하지 않았고 해당 작업에 새 HEAD를 알렸다. 운영 checkout은 clean이다.
