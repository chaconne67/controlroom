/* Synthetic review state only: no fetch, production calls, real identifiers or messages. */
const $ = s => document.querySelector(s);
const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const iconPaths={business:'M3 21V7h11v14M7 7V3h14v18M6 11h4m-4 4h4m7-8h1m-1 4h1m-1 4h1',home:'m3 10 9-7 9 7M5 9v12h14V9M9 21v-7h6v7',today:'M5 4h14v17H5zM8 2v4m8-4v4M8 11l2 2 5-5M8 17h7',companies:'M3 21V7h8v14M11 3h10v18M6 11h2m-2 4h2m7-8h3m-3 4h3m-3 4h3',sms:'m3 4 18 8-18 8 4-8-4-8Zm4 8h14',tm:'M4 13v-1a8 8 0 0 1 16 0v1M4 12H2v6h4v-6H4m16 0h2v6h-4v-6h2m0 6v3h-7',audio:'M9 4a3 3 0 0 1 6 0v8a3 3 0 0 1-6 0V4Zm-4 7v1a7 7 0 0 0 14 0v-1m-7 8v3m-4 0h8',history:'M4 4h16v17H4zM8 8h8m-8 5h8m-8 4h5',sales:'M4 3v18h18M7 15l5-5 4 3 5-7'};
const icon = k => `<span class="nav-icon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="${iconPaths[k]||iconPaths.companies}"/></svg></span>`;
const channelName={funding:'기업자금',mortgage:'모기지'};
const menus=[['today','오늘 할 일'],['companies','대상 회사'],['sms','문자 발송'],['tm','TM 상담'],['audio','통화 녹음'],['history','TM 진행 내역'],['sales','영업 현황']];
const companies=[
 {id:'hotel',name:'주식회사 예시호텔',rep:'김○○',region:'경기 수원시',industry:'호텔업',address:'경기 수원시 · 예시로 120',phone:'031-000-0000',mobile:'010-••••-1200',contact:'재무 담당자',sms:true,relation:'법인 소유 확인',status:'verified',properties:['hotel-building','hotel-land'],agent:'김담당',sales:'이영업',revenue:'48.2억원',credit:'BB+',memo:'운영자금 확보를 위한 담보 조건 비교 희망',sourceDate:'2026.09.17'},
 {id:'trust',name:'주식회사 예시리조트',rep:'박○○',region:'인천 중구',industry:'휴양 숙박업',address:'인천 중구 · 샘플해안로 38',phone:'032-000-0000',mobile:'010-••••-2300',contact:'재무 담당자',sms:true,relation:'위탁자 확인',status:'verified',properties:['trust-building'],agent:'김담당',sales:'정영업',revenue:'63.4억원',credit:'BB',memo:'신탁 계약과 우선수익권 조건 확인 필요',sourceDate:'2026.09.16'},
 {id:'draft',name:'주식회사 예시스테이',rep:'이○○',region:'서울 강서구',industry:'숙박업',address:'서울 강서구 · 예시길 27',phone:'02-000-0000',mobile:'010-••••-3400',contact:'회사 담당자',sms:true,relation:'주소로 연결',status:'draft',properties:['draft-building','draft-land'],agent:'박담당',sales:'미배정',revenue:'19.7억원',credit:'확인 자료 없음',memo:'공유자와 공동담보목록 원문 검토 필요',sourceDate:'2026.09.17'},
 {id:'unit',name:'주식회사 예시산업',rep:'최○○',region:'서울 금천구',industry:'제조업',address:'서울 금천구 · 샘플산업로 12',phone:'02-000-0001',mobile:'010-••••-4500',contact:'재무 담당자',sms:true,relation:'법인 소유 확인',status:'verified',properties:['unit-room'],agent:'박담당',sales:'미배정',revenue:'82.1억원',credit:'BBB-',memo:'전유부분과 대지권의 담보 범위 확인',sourceDate:'2026.09.15'},
 {id:'empty',name:'주식회사 예시관광',rep:'정○○',region:'경기 가평군',industry:'숙박업',address:'경기 가평군 · 예시호수길 8',phone:'031-000-0002',mobile:'',contact:'회사 일반전화',sms:false,relation:'연결 확인 전',status:'missing',properties:[],agent:'김담당',sales:'미배정',revenue:'자료 수집 대기',credit:'자료 수집 대기',memo:'문자 수신 연락처 확인 필요',sourceDate:''},
 {id:'linked',name:'주식회사 예시파트너스',rep:'윤○○',region:'경기 시흥시',industry:'숙박업',address:'경기 시흥시 · 샘플중앙로 44',phone:'031-000-0003',mobile:'',contact:'회사 일반전화',sms:false,relation:'주소로 연결',status:'verified',properties:['linked-building'],agent:'박담당',sales:'미배정',revenue:'12.6억원',credit:'B+',memo:'회사와 등기 소유자의 관계 확인 필요',sourceDate:'2026.09.14'}
];
const properties={
 'hotel-building':{code:'A-01',type:'건물',name:'예시호텔 본관',address:'경기 수원시 · 예시로 120',kind:'숙박시설 · 지상 5층 / 지하 1층',area:'연면적 1,842.60㎡',owner:'주식회사 예시호텔',share:'단독 소유 · 1/1',acquired:'2021.06.18',receipt:'2021.07.02',price:'48억원',priceScope:'본관 + 대지 2개 물건의 전체 계약금액',right:'공동담보',amount:'36억원',group:'공동담보 A',date:'2023.04.12',pages:8,source:'2026.09.17'},
 'hotel-land':{code:'A-02',type:'토지',name:'예시호텔 대지',address:'경기 수원시 · 예시동 120-1',kind:'대 · 소유권',area:'토지면적 622.40㎡',owner:'주식회사 예시호텔',share:'단독 소유 · 1/1',acquired:'2021.06.18',receipt:'2021.07.02',price:'48억원',priceScope:'본관 + 대지 2개 물건의 전체 계약금액',right:'공동담보',amount:'36억원',group:'공동담보 A',date:'2023.04.12',pages:6,source:'2026.09.17'},
 'trust-building':{code:'B-01',type:'건물',name:'예시리조트 본관',address:'인천 중구 · 샘플해안로 38',kind:'숙박시설 · 신탁 부동산',area:'연면적 2,610.20㎡',owner:'가상신탁 주식회사',share:'등기명의자 · 수탁자',acquired:'2025.07.21',receipt:'2025.07.22',price:'확인 자료 없음',priceScope:'최근 소유권 변동 원인: 신탁',right:'신탁',amount:'60억원',group:'공동 1순위',date:'2025.07.21',pages:28,source:'2026.09.16',trust:true},
 'draft-building':{code:'C-01',type:'건물',name:'예시스테이 건물',address:'서울 강서구 · 예시길 27',kind:'숙박시설',area:'검토 대기',owner:'검토 대기',share:'공유지분 검토 필요',acquired:'검토 대기',receipt:'검토 대기',price:'검토 대기',priceScope:'가격과 대상 범위 미확정',right:'검토 대기',amount:'검토 대기',pages:10,source:'2026.09.17',draft:true},
 'draft-land':{code:'C-02',type:'토지',name:'예시스테이 대지',address:'서울 강서구 · 예시동 27-3',kind:'대',area:'검토 대기',owner:'검토 대기',share:'공유지분 검토 필요',acquired:'검토 대기',receipt:'검토 대기',price:'검토 대기',priceScope:'가격과 대상 범위 미확정',right:'검토 대기',amount:'검토 대기',pages:7,source:'2026.09.17',draft:true},
 'unit-room':{code:'D-01',type:'호실',name:'예시산업센터 501호',address:'서울 금천구 · 샘플산업로 12, 501호',kind:'집합건물 · 업무시설',area:'전유면적 61.78㎡',owner:'주식회사 예시산업',share:'전유부분 1/1',acquired:'2017.09.28',receipt:'2017.09.29',price:'확인 자료 없음',priceScope:'이 원문에서 거래금액 확인 불가',right:'건물만 담보',amount:'5.04억원',group:'본 호실만',date:'2017.09.29',pages:3,source:'2026.09.15',unit:true},
 'linked-building':{code:'E-01',type:'건물',name:'주소로 연결된 건물',address:'경기 시흥시 · 샘플중앙로 44',kind:'숙박시설',area:'연면적 921.00㎡',owner:'개인 소유자 이○○',share:'단독 소유 · 1/1',acquired:'2008.04.12',receipt:'2008.04.16',price:'확인 자료 없음',priceScope:'현재 회사의 취득으로 확인되지 않음',right:'근저당권',amount:'9억원',group:'본건 부동산',date:'2020.05.20',pages:5,source:'2026.09.14',linked:true}
};
function initChannel(mode){const ids=mode==='mortgage'?companies.map(c=>c.id):['hotel','unit','empty','linked'];return {page:'companies',company:ids[0],property:companies.find(c=>c.id===ids[0]).properties[0],companyTab:mode==='funding'?'overview':'estate',propertyTab:'summary',search:'',filter:'all',selected:[],stages:Object.fromEntries(ids.map(id=>[id,mode==='mortgage'&&id==='hotel'?'상담 중':mode==='mortgage'&&id==='trust'?'영업 전달':'미접촉'])),smsSent:mode==='mortgage'?['hotel','trust']:[],campaigns:[],calls:mode==='mortgage'?[{id:'hotel',result:'연결',reaction:'관심 있음',memo:'금리 조건 비교를 희망하여 현재 이용 금융기관과 만기 자료를 요청했습니다.',next:'담보 관련 서류 확인',due:'2026-09-18T14:00',stamp:'09.17 10:30',done:false}]:[],deals:mode==='mortgage'?[{id:'trust',status:'진행중',sales:'정영업',memo:'신탁 조건 확인 후 금융기관 협의',properties:['trust-building']}]:[],forms:{},showCancelled:false,tmLane:'normal',special:null};}
let state={mode:'mortgage',openGroup:'mortgage',channels:{funding:initChannel('funding'),mortgage:initChannel('mortgage')}};
const current=()=>state.channels[state.mode], company=()=>companies.find(c=>c.id===current().company), property=()=>properties[current().property];
const list=()=>companies.filter(c=>state.mode==='mortgage'||['hotel','unit','empty','linked'].includes(c.id));
const badge=(label,type='')=>`<span class="badge ${type}">${esc(label)}</span>`;
const dataBadge=c=>badge(c.status==='verified'?'확인 완료':c.status==='draft'?'검토 대기':'미수집',c.status==='verified'?'ok':c.status==='draft'?'warn':'');
const btn=(label,act,attrs='',classes='')=>`<button class="btn ${classes}" data-act="${act}" ${attrs}>${label}</button>`;
const heading=(title,desc,actions='')=>`<div class="breadcrumb"><b>${channelName[state.mode]}</b><span>/</span><span>${esc(title)}</span></div><div class="heading"><div><h1>${esc(title)}</h1>${desc?`<p>${esc(desc)}</p>`:''}</div><div class="heading-actions">${actions}</div></div>`;
const notice=(title,body,type='')=>`<div class="notice ${type}"><div><strong>${title}</strong><p>${body}</p></div></div>`;
const fact=(label,value,note='',source=false,wide=false)=>`<div class="fact${wide?' wide':''}"><dt>${label}</dt><dd>${value}${source?btn('원문','source',`data-title="${label}"`,'text'):''}${note?`<small>${note}</small>`:''}</dd></div>`;
function renderNav(){ $('#nav').innerHTML=['funding','mortgage'].map(mode=>`<div class="nav-group"><button class="group-toggle ${state.mode===mode?'current':''}" data-act="group" data-mode="${mode}" aria-expanded="${state.openGroup===mode}" aria-controls="nav-${mode}">${icon(mode==='funding'?'business':'home')}${channelName[mode]}<span class="chevron" aria-hidden="true">›</span></button><div id="nav-${mode}" class="nav-sub" ${state.openGroup===mode?'':'hidden'}>${menus.map(([key,label])=>`<button data-act="nav" data-mode="${mode}" data-page="${key}" class="${state.mode===mode&&(current().page===key||(current().page==='detail'&&key==='companies'))?'active':''}" ${state.mode===mode&&(current().page===key||(current().page==='detail'&&key==='companies'))?'aria-current="page"':''}>${icon(key)}${label}${key==='today'&&state.channels[mode].calls.some(c=>!c.done)?`<span class="nav-count">${state.channels[mode].calls.filter(c=>!c.done).length}</span>`:''}</button>`).join('')}</div></div>`).join('');$('#mobile-channel').textContent=channelName[state.mode];}
function updateHash(){const c=current();history.replaceState(null,'',`#${state.mode}/${c.page}${c.page==='detail'?'/'+c.company+'/'+c.propertyTab:''}`);}
function navigate(mode,page,id){state.mode=mode;state.openGroup=mode;const c=current();c.special=null;c.page=page;if(id){c.company=id;c.property=companies.find(x=>x.id===id).properties[0];c.propertyTab='summary';c.companyTab=mode==='funding'?'overview':'estate';}$('.prototype').classList.remove('nav-open');$('.mobile-bar button').setAttribute('aria-expanded','false');render();$('#screen').focus({preventScroll:true});}
function render(){renderNav();const c=current();let html='';if(c.special==='loading')html=heading('대상 회사','회사와 부동산 정보를 불러오고 있습니다.')+`<div aria-live="polite"><div class="skeleton"></div><div class="skeleton"></div></div>`+btn('불러오기 완료 상황 보기','recover');else if(c.special==='error')html=heading('대상 회사','')+`<div class="empty-state" role="alert"><h3>목록을 불러오지 못했습니다</h3><p>검색 조건은 유지됩니다. 다시 시도해 주세요.</p>${btn('다시 불러오기','recover','','primary')}</div>`;else {const views={companies:renderCompanies,sms:renderSMS,detail:renderDetail,tm:renderTM,audio:renderAudio,history:renderHistory,sales:renderSales,today:renderToday,manage:renderManage};html=(views[c.page]||renderCompanies)();}$('#screen').innerHTML=html;updateHash();}
function filters(){return `<div class="toolbar"><label>회사 검색<input class="input" id="company-search" type="search" value="${esc(current().search)}" placeholder="회사명·지역·대표자 검색" aria-label="회사명·지역·대표자 검색"></label><label>${state.mode==='mortgage'?'등기 자료':'업무 상태'}<select class="select" id="status-filter"><option value="all">전체</option>${(state.mode==='mortgage'?[['verified','확인 완료'],['draft','검토 대기'],['missing','미수집']]:[['미접촉','미접촉'],['상담 중','상담 중'],['영업 전달','영업 전달']]).map(([v,t])=>`<option value="${v}" ${current().filter===v?'selected':''}>${t}</option>`).join('')}</select></label>${btn('조건 초기화','clear-filters','','small reset-filters')}</div>`;}
function filtered(){const c=current();const q=c.search.trim().toLowerCase();return list().filter(x=>(!q||[x.name,x.region,x.rep].join(' ').toLowerCase().includes(q))&&(c.filter==='all'||(state.mode==='mortgage'?x.status:c.stages[x.id])===c.filter));}
function renderCompanies(){return heading('대상 회사',state.mode==='mortgage'?'회사별 진행 상황과 연결된 부동산을 확인합니다.':'기업자금 대상 회사와 진행 상황을 확인합니다.',btn('선택 회사 문자 예약','sms-modal',current().selected.length?'':'disabled','primary'))+filters()+renderCompanyTable()+`<p class="footnote">${state.mode==='mortgage'?'연결 물건은 소유 여부와 별개입니다. 채권최고액과 신탁 한도는 물건 상세에서 범위를 확인합니다.':'대표이사 연락처와 회사 정보를 기준으로 기업자금 업무를 진행합니다.'}</p>`;}
function renderCompanyTable(sms=false){const rows=filtered();const c=current();const mortgage=state.mode==='mortgage';return `<div class="list-meta"><b>${rows.length}개 회사</b><span class="selection">${c.selected.length?c.selected.length+'개 선택':''}</span><span class="right">${mortgage?'회사별 목록 · 금액은 상세에서 확인':'기업자금 대상만 표시'}</span></div>${!rows.length?`<div class="empty-state"><h3>조건에 맞는 회사가 없습니다</h3><p>검색어나 필터를 바꾸어 다시 확인하세요.</p>${btn('조건 초기화','clear-filters','','primary')}</div>`:`<table class="data-table responsive"><thead><tr><th class="check-cell"><label class="check-label"><input type="checkbox" data-act="select-all" aria-label="현재 목록의 문자 가능 회사 전체 선택" ${rows.filter(x=>x.sms&&c.stages[x.id]!=='제외').length&&rows.filter(x=>x.sms&&c.stages[x.id]!=='제외').every(x=>c.selected.includes(x.id))?'checked':''}></label></th><th>회사 / 지역</th><th>${sms?'문자 수신 연락처':mortgage?'연결 부동산':'최근 연매출'}</th><th>${sms?'발송 상태':mortgage?'회사와의 관계':'신용등급'}</th><th>${mortgage?'등기 자료':'TM 담당자'}</th><th>진행 / 다음 행동</th></tr></thead><tbody>${rows.map(x=>`<tr><td class="check-cell"><label class="check-label"><input type="checkbox" data-act="select" data-id="${x.id}" aria-label="${x.name} 선택" ${c.selected.includes(x.id)?'checked':''} ${x.sms&&c.stages[x.id]!=='제외'?'':'disabled'}></label></td><td class="company-cell">${btn(x.name,'company',`data-id="${x.id}"`,'link company-name')}<small>${x.region} · ${x.industry}</small></td><td data-label="${sms?'문자 수신 연락처':mortgage?'연결 부동산':'최근 연매출'}">${sms?(x.sms?esc(x.mobile):badge('연락처 확인 필요','warn')):mortgage?(x.properties.length?`<b>${x.properties.length}개 물건</b>`:'연결 확인 전'):esc(x.revenue)}<small>${sms?esc(x.contact):mortgage?(x.properties.length?x.properties.map(id=>properties[id].type).join(' · '):'회사 주소만 확보'):''}</small></td><td data-label="${sms?'발송 상태':mortgage?'회사와의 관계':'신용등급'}">${sms?(c.smsSent.includes(x.id)?badge('발송 완료','ok'):badge('미발송')):mortgage?esc(x.relation):esc(x.credit)}</td><td data-label="${mortgage?'등기 자료':'TM 담당자'}">${mortgage?dataBadge(x):esc(x.agent)}${mortgage?`<small>${x.sourceDate||'원문 없음'}</small>`:''}</td><td data-label="업무 진행">${badge(c.stages[x.id],c.stages[x.id]==='영업 전달'?'blue':'')}<small>${x.sms?esc(x.agent):'회사전화로 연락처 확인'}</small></td></tr>`).join('')}</tbody></table>`}`;}
function renderSMS(){const c=current();return heading('문자 발송','이 업무의 대상·수신 연락처를 확인한 뒤 예약합니다.',btn('문자 예약하기','sms-modal',c.selected.length?'':'disabled','primary'))+(c.campaigns.length?`<section class="panel" style="margin-bottom:20px"><h3>예약 현황</h3>${c.campaigns.map((r,i)=>`<div class="plain-row"><div class="grow"><b>${esc(r.name)}</b><small>${r.ids.length}개 회사 · ${r.status==='scheduled'?'예약 '+esc(r.date):'발송 완료 예시'}</small></div>${badge(r.status==='scheduled'?'예약됨':'발송 완료',r.status==='scheduled'?'blue':'ok')}${r.status==='scheduled'?btn('발송 완료 상황 보기','sms-delivered',`data-index="${i}"`,'small'):''}</div>`).join('')}</section>`:notice('예약된 발송이 없습니다','아래 회사에서 문자 수신 연락처를 확인하고 대상을 선택하세요.'))+filters()+renderCompanyTable(true)+`<p class="footnote">회사 일반전화만 있는 대상은 문자 예약에서 제외됩니다. ${state.mode==='mortgage'?btn('연락처 확인 대상 보기','contact-tm','','text'):''}</p>`;}
function renderDetail(){const c=company();const s=current();return heading(c.name,'',btn('회사 목록','nav',`data-mode="${state.mode}" data-page="companies"`)+btn('TM 상담','tm-for-company',`data-id="${c.id}"`,'primary'))+`<div class="company-meta"><span>${c.region} · ${c.industry}</span><span>대표 ${c.rep}</span><span>TM ${c.agent}</span><span>${badge(s.stages[c.id])}</span></div><div class="tabs" role="tablist" aria-label="회사 상세">${[['overview','회사 정보'],...(state.mode==='mortgage'?[['estate','부동산']]:[]),['activity','활동 이력']].map(([k,v])=>`<button role="tab" aria-selected="${s.companyTab===k}" class="${s.companyTab===k?'active':''}" data-act="company-tab" data-tab="${k}">${v}${k==='estate'?`<span>${c.properties.length||'확인 전'}</span>`:''}</button>`).join('')}</div>${s.companyTab==='overview'||state.mode==='funding'&&s.companyTab==='estate'?renderOverview(c):s.companyTab==='activity'?renderActivity(c):renderEstate(c)}`;}
function renderOverview(c){return `<div class="panel"><h3>회사 기본정보</h3><dl class="facts">${fact('회사명',esc(c.name))}${fact('대표자',c.rep)}${fact('업종',c.industry)}${fact('소재지',c.address)}${fact('최근 연매출',c.revenue,'결산 기준: 2025년 · 시안 데이터')}${fact('신용등급',c.credit,'평가 기준일: 2026.06.30 · 시안 데이터')}${fact('회사 전화',c.phone,'회사 대표번호')}${fact('문자 수신 연락처',c.mobile||'확인 필요',c.sms?c.contact:'일반전화를 대표 휴대전화로 사용하지 않음')}</dl></div>`;}
function renderActivity(c){const s=current();return `<div class="panel"><h3>${channelName[state.mode]} 활동 이력</h3><ul class="timeline">${s.calls.filter(x=>x.id===c.id).map(x=>`<li><div class="date">${esc(x.stamp)} · ${c.agent}</div><h4>TM ${esc(x.result)}</h4><p>${esc(x.memo)}</p><p>다음 행동: ${esc(x.next||'미정')}</p></li>`).join('')||'<li><h4>아직 통화 기록이 없습니다</h4><p>이 업무에서 기록한 활동이 여기에 쌓입니다.</p></li>'}${s.smsSent.includes(c.id)?'<li><div class="date">09.16</div><h4>문자 발송 완료</h4><p>이 업무의 발송 이력</p></li>':''}<li><div class="date">09.15</div><h4>대상 회사 등록</h4></li></ul></div>`;}
function renderEstate(c){if(!c.properties.length)return notice('아직 연결된 등기 원문이 없습니다','회사 주소는 확보되어 있습니다. 물건 식별과 소유 관계를 확인하면 여기에 표시됩니다.')+`<div class="empty-state"><h3>부동산 정보를 확인할 차례입니다</h3><p>‘자료 없음’은 부동산이 없다는 뜻이 아닙니다.</p>${btn('확인할 자료 보기','checklist','','primary')}</div>`;
 const p=property()||properties[c.properties[0]];const s=current();return (c.status==='draft'?notice('등기 원문은 확보됐으며 판독 결과를 검토 중입니다','공유지분·권리·목록의 누락을 확인하고 있습니다. 미확정 금액과 소유 정보는 요약·집계에 사용하지 않습니다.','warn'):c.relation==='주소로 연결'?notice('회사 주소로 연결된 부동산입니다','등기명의자는 회사와 다릅니다. 회사 소유·대표자 소유·임차 관계는 별도 확인이 필요합니다.','warn'):'')+`<div class="estate-layout"><div class="property-list"><div class="section-label"><b>연결된 물건 ${c.properties.length}개</b><span>토지·건물 별도 등기</span></div>${c.properties.map(id=>{const x=properties[id];return `<button class="property-card ${s.property===id?'active':''}" data-act="property" data-id="${id}" aria-pressed="${s.property===id}">${badge(x.type)}<h3>${x.name}</h3><p>${x.address}</p><p style="margin-top:8px">${x.draft?'검토 대기':x.trust?'신탁 · 위탁자 관계 확인':x.linked?'소유 관계 확인 필요':'법인 소유 확인'}</p><span class="property-code">물건 ${x.code} · 등기 열람 ${x.source}</span></button>`}).join('')}</div><section class="property-panel" aria-label="선택 물건 상세"><div class="property-heading"><div><small>${p.type} · 물건 ${p.code}</small><h2>${p.name}</h2><p>${p.address}</p></div><div>${dataBadge(c)}<p>열람 ${p.source}</p></div></div><div class="tabs" role="tablist" aria-label="물건 상세">${[['summary','요약'],['rights','권리·담보'],...(p.trust?[['trust','신탁']]:[]),['timeline','변동 이력'],['documents','원문']].map(([k,v])=>`<button role="tab" aria-selected="${s.propertyTab===k}" class="${s.propertyTab===k?'active':''}" data-act="property-tab" data-tab="${k}">${v}</button>`).join('')}</div>${p.draft&&s.propertyTab!=='documents'?renderDraft(p):s.propertyTab==='rights'?renderRights(p):s.propertyTab==='timeline'?renderTimeline(p):s.propertyTab==='documents'?renderDocuments(p):s.propertyTab==='trust'?renderTrust(p):renderSummary(p)}</section></div>`;}
function renderDraft(p){return `<div class="empty-state"><h3>원문 검토 후 요약이 표시됩니다</h3><p>소유지분과 담보 범위가 아직 확정되지 않았습니다.<br>원문 ${p.pages}쪽을 먼저 확인할 수 있습니다.</p>${btn('확보한 원문 보기','property-tab','data-tab="documents"','primary')}</div><div class="subsection"><h3>확인이 필요한 내용</h3><div class="plain-row"><span class="grow">공유자 전원과 각 지분</span>${badge('미확정','warn')}</div><div class="plain-row"><span class="grow">설정·변경·말소 권리 누락 여부</span>${badge('미확정','warn')}</div><div class="plain-row"><span class="grow">본문에 참조된 공동담보목록</span>${badge('추가 원문 필요','warn')}</div></div>`;}
function renderSummary(p){return `<dl class="facts">${fact('등기명의자',p.owner,p.share,true)}${fact('회사와의 관계',p.trust?'위탁자':p.linked?'주소만 일치 · 소유 미확인':'법인 소유 확인',p.trust?'수탁자를 회사 소유자로 바꾸어 표시하지 않음':'',true)}${fact('용도·규모',p.kind,p.area,true)}${fact('최근 소유권 변동',p.acquired,p.trust?'원인: 신탁 · 접수 '+p.receipt:'원인: 매매 · 접수 '+p.receipt,true)}${fact('등기에 기재된 거래금액',p.price,p.priceScope,true)}${fact(p.trust?'우선수익 한도':'현재 채권최고액',p.amount,p.trust?'공동 1순위 3기관 · 잔액 아님':p.group+' · 실제 대출 잔액 아님',true)}${p.unit?fact('대지권', '13.76 / 3,221.2', '토지 지분과 전유면적은 별도 · 근저당은 건물만에 관한 것',true,true):''}</dl><div class="subsection"><h3>상담 전에 확인할 내용</h3>${p.group==='공동담보 A'?`<div class="group-reference"><div><b>본관과 대지를 하나의 담보 묶음으로 확인</b><br>두 등기에 같은 36억원이 기재되어 있습니다.</div>${btn('담보 묶음 보기','collateral','','small')}</div>`:p.trust?notice('신탁 계약의 수익순위·처분 조건 확인','등기 표제와 신탁원부를 함께 확인해야 합니다.'):p.unit?notice('담보 범위: 건물만','대지권이 있어도 해당 근저당의 담보 범위에 자동으로 포함하지 않습니다.','warn'):notice('소유자와 회사의 관계 확인','현재 원문만으로 이 부동산을 회사 보유 자산으로 판단할 수 없습니다.','warn')}<div class="plain-row"><div class="grow"><b>실제 대출 잔액 · 금리 · 만기</b><small>은행 자료 또는 확인된 상담 증빙 필요</small></div><span class="muted">확인 자료 없음</span></div></div>`;}
function renderRights(p){if(p.trust)return notice('이 물건은 신탁원부도 확인해야 합니다','우선수익 한도·순위와 계약 조건은 신탁 탭에 따로 표시합니다.')+btn('신탁 내용 보기','property-tab','data-tab="trust"','primary');const s=current();return `<div class="section-label"><span>등기 열람일 현재 관측 · ${p.source}</span><label><input type="checkbox" data-act="cancelled" ${s.showCancelled?'checked':''}> 말소된 권리 포함</label></div><article class="right-card"><div class="right-title"><h3>근저당권 · 을구 3번 ${badge('현재','ok')}</h3><span class="amount">${p.amount}</span></div><dl class="facts">${fact('권리자','가상은행')}${fact('채무자',company().name)}${fact('담보 범위',p.unit?'건물만':p.linked?'본건 부동산':'토지 + 건물')}${fact('설정 원인일',p.date)}${fact('접수일',p.date)}${fact('채권최고액',p.amount,'실제 대출 잔액 아님')}</dl>${p.group==='공동담보 A'?`<div class="group-reference"><span><b>공동담보 A</b> · 확인 물건 2개</span>${btn('연결 물건 확인','collateral','','small')}</div>`:''}${btn('설정 근거 원문 보기','source','data-title="근저당권 설정"','text')}</article>${s.showCancelled?`<article class="right-card muted-card"><div class="right-title"><h3>이전 근저당권 · 을구 1번 ${badge('말소')}</h3><span class="amount">12억원</span></div><p class="muted">2021.07.02 설정 → 2023.04.12 말소</p><small>당시 채권최고액을 보존하며 현재 담보에 합산하지 않습니다. 말소를 실제 잔액 0원으로 해석하지 않습니다.</small></article>`:''}<p class="footnote">가압류·압류·전세권·처분금지 등은 권리 종류별로 표시합니다. 원문에 없는 우선순위를 자동 판단하지 않습니다.</p>`;}
function renderTimeline(p){const rows=p.unit?[['2022.09.14','담보 범위 확인','2017년 설정 근저당은 건물만에 관한 것이라는 부기. 새 대출로 세지 않습니다.'],['2022.09.14','대지권 기록','대지권 비율과 목적 토지를 별도로 관측했습니다.'],[p.date,'근저당권 설정',p.amount+' · 채권최고액'],[p.acquired,'소유권 이전','매매 원인일 · 접수 '+p.receipt]]:p.trust?[[p.receipt,'신탁 등기','등기명의자: 가상신탁 주식회사'],[p.acquired,'신탁 계약','위탁자와 공동 순위 우선수익자를 원부에서 확인']]:[[p.date,'근저당권 설정',p.amount+' · '+p.group],[p.date,'이전 근저당권 말소','현재 담보 집계에서 제외'],[p.receipt,'소유권 이전 접수','매매 원인일 '+p.acquired+' · '+p.priceScope]];return `<p class="footnote" style="margin-top:0">원인일과 등기 접수일을 구분한 이력입니다. 이기·분할 전 기록이 없으면 이전 이력은 확인 전으로 남깁니다.</p><ul class="timeline">${rows.map(([date,title,body])=>`<li><div class="date">${date}</div><h4>${title}</h4><p>${body}</p>${btn('근거 보기','source',`data-title="${title}"`,'text')}</li>`).join('')}</ul>`;}
function renderTrust(p){return notice('신탁 기간과 대출 만기는 다른 정보입니다','아래 기간은 원부의 신탁 계약 기간입니다. 실제 대출 만기는 별도 자료가 필요합니다.')+`<dl class="facts">${fact('위탁자',company().name,'회사와 부동산의 관계',true)}${fact('수탁자','가상신탁 주식회사','등기명의자',true)}${fact('신탁 기간','2025.07.21 ~ 2027.07.11','연장 조건 있음 · 대출 만기로 사용하지 않음',true,true)}</dl><div class="subsection"><h3>공동 1순위 우선수익자</h3>${[['가상금융 A','30억원'],['가상금융 B','12억원'],['가상금융 C','18억원']].map(([name,amount])=>`<div class="plain-row"><span>${badge('공동 1순위')}</span><b class="grow">${name}</b><span class="amount">${amount}</span></div>`).join('')}<p class="footnote">한도 합계 60억원 · 우선수익 한도이며 실제 대출 잔액이 아닙니다.</p></div><div class="subsection"><h3>계약 조건</h3><div class="plain-row"><div class="grow"><b>기간 연장 조건</b><small>채무가 남아 있을 때의 연장 조건은 원부 해당 조항 확인</small></div>${btn('원문','source','data-title="신탁 기간 연장 조건"','text')}</div><div class="plain-row"><div class="grow"><b>비용 부담 및 처분 대금 배분</b><small>기관별 부담과 배분 순서는 계약 근거로 확인</small></div>${btn('원문','source','data-title="비용과 처분 조건"','text')}</div></div>`;}
function renderDocuments(p){return `<div class="plain-row"><div class="grow"><b>등기사항전부증명서</b><small>열람 ${p.source} · ${p.pages}쪽 · ${p.draft?'원문 확보 / 판독 검토 대기':'원문 검토 완료 예시'}</small></div>${btn('원문 보기','source','data-title="등기사항전부증명서"','small')}</div>${p.group==='공동담보 A'?`<div class="plain-row"><div class="grow"><b>공동담보목록 A</b><small>본문 참조와 연결 · 본관 / 대지 2개 물건</small></div>${btn('목록 보기','collateral','','small')}</div>`:''}${p.trust?`<div class="plain-row"><div class="grow"><b>신탁원부</b><small>등기 본문과 연결된 계약 원문 포함</small></div>${btn('원부 보기','source','data-title="신탁원부"','small')}</div>`:''}${p.draft?notice('미수령 참조가 있습니다','본문의 공동담보목록 일부가 아직 확보되지 않았습니다. 원문이 없는 참조는 확인 자료 없음으로 유지합니다.','warn'):''}<div class="subsection"><h3>원문과 판독 이력</h3><div class="plain-row"><span class="grow">${p.source} 열람본</span>${badge(p.draft?'검토 대기':'확인 완료',p.draft?'warn':'ok')}</div><p class="footnote">새 판독이 생겨도 이전 원문과 판독 버전은 보존합니다. 원문 접근은 회사 권한에 따라 제한됩니다.</p></div>`;}
function eligibleTM(){return list().filter(c=>current().tmLane==='contact'?!c.sms:current().smsSent.includes(c.id)&&!['영업 전달','제외'].includes(current().stages[c.id]));}
function renderTM(){const s=current();const eligible=eligibleTM();if(!eligible.some(c=>c.id===s.company)&&eligible.length)s.company=eligible[0].id;const c=company();const draft=s.forms[c.id]||{};return heading('TM 상담','통화 대상과 부동산 자료를 함께 보고 기록합니다.')+(state.mode==='mortgage'?`<div class="tabs" role="tablist" aria-label="상담 대상 구분"><button role="tab" aria-selected="${s.tmLane==='normal'}" class="${s.tmLane==='normal'?'active':''}" data-act="tm-lane" data-lane="normal">문자 발송 후 상담</button><button role="tab" aria-selected="${s.tmLane==='contact'}" class="${s.tmLane==='contact'?'active':''}" data-act="tm-lane" data-lane="contact">연락처 확인 <span>제안 분기</span></button></div>`:'')+(!eligible.length?`<div class="empty-state"><h3>현재 상담할 대상이 없습니다</h3><p>문자 발송이 완료된 회사가 상담 목록에 나타납니다.</p>${btn('문자 발송으로 이동','nav',`data-mode="${state.mode}" data-page="sms"`,'primary')}</div>`:`<div class="toolbar"><label>상담할 회사<select class="select" id="tm-company">${eligible.map(x=>`<option value="${x.id}" ${x.id===c.id?'selected':''}>${x.name}</option>`).join('')}</select></label></div><div class="split"><section><div class="contact-box"><small>${c.name} · ${s.tmLane==='contact'?'연락처 확인':'TM 상담'}</small><h3 class="phone">${s.tmLane==='contact'?c.phone:c.mobile}</h3><small>${s.tmLane==='contact'?'회사 일반전화 · 문자 수신 담당자 확인':c.contact+' · 확인된 수신 연락처'}</small></div>${s.tmLane==='contact'?notice('문자 수신 연락처를 확인하는 선행 통화','이 분기는 시안 제안입니다. 번호를 확인하기 전에는 문자 발송 대상으로 바꾸지 않습니다.','warn'):''}<div class="panel"><h3>${state.mode==='mortgage'?'상담에 필요한 부동산':'회사 정보'}</h3>${state.mode==='mortgage'?`<div class="plain-row"><span class="grow">연결 물건</span><b>${c.properties.length||'확인 전'}${c.properties.length?'개':''}</b></div><div class="plain-row"><span class="grow">회사와의 관계</span><b>${c.relation}</b></div><div class="plain-row"><span class="grow">등기 자료</span>${dataBadge(c)}</div>`:`<dl class="facts">${fact('연매출',c.revenue)}${fact('신용등급',c.credit)}</dl>`}${btn(state.mode==='mortgage'?'부동산 상세 보기':'회사 정보 보기','company',`data-id="${c.id}"`,'text')}<div class="signal"><b>확인할 내용</b><p>${esc(c.memo)}</p></div></div></section><section class="panel"><h3>통화 기록</h3><form id="call-form"><div class="form-grid"><label class="field"><span>통화 결과</span><select name="result" class="select">${['연결','부재','통화중','결번'].map(v=>`<option ${draft.result===v?'selected':''}>${v}</option>`).join('')}</select></label><label class="field"><span>반응도</span><select name="reaction" class="select">${['선택 안 함','관심 있음','추후 검토','거절'].map(v=>`<option ${draft.reaction===v?'selected':''}>${v}</option>`).join('')}</select></label><label class="field wide"><span>통화 메모</span><textarea name="memo" placeholder="자금 목적, 보유·임차 관계, 확인할 자료">${esc(draft.memo||'')}</textarea></label><label class="field wide"><span>다음 할 일</span><input class="input" name="next" value="${esc(draft.next||'')}" placeholder="서류 요청 / 재통화 / 전문가 연결"></label><label class="field wide"><span>예정 일시</span><input class="input" type="datetime-local" name="due" value="${esc(draft.due||'')}"></label></div><p id="call-error" class="error-text" role="alert"></p><div class="form-footer"><button class="btn primary" type="submit">기록 저장</button></div></form></section></div>`);}
function renderHistory(){const s=current();return heading('TM 진행 내역','통화 기록과 다음 행동을 확인하고 영업 담당자에게 전달합니다.')+(s.calls.length?`<div class="deal-list">${s.calls.slice().reverse().map(r=>{const c=companies.find(x=>x.id===r.id);return `<article class="deal"><div class="deal-top"><div><h3>${c.name}</h3><p>${r.stamp} · ${c.agent} · ${esc(r.result)} / ${esc(r.reaction)}</p></div>${badge(s.stages[r.id])}</div><p>${esc(r.memo)}</p><div class="deal-bottom"><span>다음: ${esc(r.next||'미정')}${r.due?' · '+r.due.replace('T',' '):''}</span><div>${btn('회사 보기','company',`data-id="${r.id}"`,'small')}${btn('영업자에게 전달','handoff-modal',`data-id="${r.id}" ${['영업 전달','제외'].includes(s.stages[r.id])?'disabled':''}`,'primary small')}</div></div></article>`}).join('')}</div>`:`<div class="empty-state"><h3>아직 통화 기록이 없습니다</h3><p>이 업무에서 저장한 상담만 표시됩니다.</p>${btn('TM 상담으로 이동','nav',`data-mode="${state.mode}" data-page="tm"`,'primary')}</div>`);}
function renderSales(){const s=current();return heading('영업 현황','전달받은 상담과 부동산 자료를 기준으로 진행합니다.')+`<div class="summary-strip">${[['전체',s.deals.length],['진행중',s.deals.filter(x=>['수신','진행중'].includes(x.status)).length],['성사',s.deals.filter(x=>x.status==='성사').length],['무산',s.deals.filter(x=>x.status==='무산').length]].map(([k,v])=>`<div><small>${k}</small><b>${v}</b></div>`).join('')}</div>`+(s.deals.length?`<div class="deal-list">${s.deals.map((d,i)=>{const c=companies.find(x=>x.id===d.id);return `<article class="deal"><div class="deal-top"><div><h3>${c.name}</h3><p>담당 ${d.sales} · ${channelName[state.mode]}</p></div>${badge(d.status,d.status==='성사'?'ok':'blue')}</div><p>${esc(d.memo)}</p>${state.mode==='mortgage'?`<div class="signal"><b>상담 대상 부동산 ${d.properties.length}개</b><p>${d.properties.map(id=>properties[id].name).join(' · ')||'물건 범위 확인 전'} / ${c.status==='draft'?'자료 검토 대기':'전달 당시 자료 상태 확인'}</p></div>`:''}<div class="deal-bottom"><span>다음 행동과 결과를 업무별로 기록합니다.</span><div>${btn('자료 보기','company',`data-id="${c.id}"`,'small')}${btn('진행 업데이트','sales-modal',`data-index="${i}"`,'primary small')}</div></div></article>`}).join('')}</div>`:`<div class="empty-state"><h3>전달된 영업 건이 없습니다</h3><p>TM 진행 내역에서 영업 담당자에게 전달하면 표시됩니다.</p>${btn('TM 진행 내역','nav',`data-mode="${state.mode}" data-page="history"`,'primary')}</div>`);}
function renderToday(){const s=current();const tasks=s.calls.filter(c=>!c.done&&c.next);return heading('오늘 할 일','이 업무의 예정된 재통화·자료 확인을 확인합니다.')+(tasks.length?`<div class="deal-list">${tasks.map(t=>`<article class="deal"><div class="deal-top"><div><small>${t.due?t.due.replace('T',' '):'일시 미지정'}</small><h3>${esc(t.next)}</h3><p>${companies.find(c=>c.id===t.id).name}</p></div>${btn('완료','task-done',`data-index="${s.calls.indexOf(t)}"`,'small')}</div>${btn('상담 열기','nav',`data-mode="${state.mode}" data-page="tm" data-id="${t.id}"`,'text')}</article>`).join('')}</div>`:`<div class="empty-state"><h3>남은 할 일이 없습니다</h3><p>상담에서 다음 행동과 시간을 기록하면 여기에 표시됩니다.</p>${btn('대상 회사 보기','nav',`data-mode="${state.mode}" data-page="companies"`,'primary')}</div>`);}
function renderAudio(){return heading('통화 녹음','현재 업무의 상담 대상과 녹음 파일을 연결합니다.')+`<div class="panel"><h3>${channelName[state.mode]} 녹음 올리기</h3><form id="audio-form"><div class="form-grid"><label class="field"><span>회사</span><select class="select" name="audio-company">${list().map(c=>`<option>${c.name}</option>`).join('')}</select></label><label class="field"><span>녹음 파일</span><input class="input" type="file" accept="audio/*" name="audio-file"></label></div><p class="footnote">이 시안에서는 파일을 서버로 보내지 않습니다. 파일 선택과 요약 검토 상태만 확인합니다.</p><div class="form-footer"><button type="submit" class="btn primary">요약 검토 예시 보기</button></div></form></div>`;}
function renderManage(){return heading('관리','계정은 공통으로 관리하고 업무별 대상 배정은 구분합니다.')+`<div class="tabs"><button class="active">담당자 관리</button><button data-act="manage-note" data-title="가입 승인">가입 승인</button><button data-act="manage-note" data-title="발신 설정">발신 설정</button></div><div class="panel"><h3>대상 배정 업무</h3><div class="tabs">${['funding','mortgage'].map(m=>`<button class="${state.mode===m?'active':''}" data-act="nav" data-mode="${m}" data-page="manage">${channelName[m]}</button>`).join('')}</div><table class="data-table"><thead><tr><th>TM 담당자</th><th>현재 배정</th><th>배정 비율</th></tr></thead><tbody><tr><td>김담당</td><td>${list().filter(c=>c.agent==='김담당').length}개 회사</td><td>50%</td></tr><tr><td>박담당</td><td>${list().filter(c=>c.agent==='박담당').length}개 회사</td><td>50%</td></tr></tbody></table><p class="footnote">실제 구현은 기존 배정 입력·확인 절차를 재사용하며 보호 대상과 배정 권한을 유지합니다.</p></div>`;}
function modal(title,body,footer='',subtitle=''){const d=$('#modal');$('#modal-content').innerHTML=`<div class="modal-head"><div><h2 id="modal-title">${title}</h2>${subtitle?`<small>${subtitle}</small>`:''}</div><button class="icon-btn" data-act="close" aria-label="대화상자 닫기">×</button></div><div class="modal-body">${body}</div><div class="modal-footer">${footer||btn('닫기','close')}</div>`;if(!d.open)d.showModal();}
function toast(msg){$('#toast').textContent=msg;$('#toast').classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>$('#toast').classList.remove('show'),4200);}
function openSMS(){const ids=current().selected.filter(id=>companies.find(c=>c.id===id).sms&&current().stages[id]!=='제외');if(!ids.length){toast('문자 수신 연락처가 확인된 회사를 선택해 주세요.');return;}modal(channelName[state.mode]+' 문자 예약',`<div class="notice"><div><strong>${ids.length}개 회사에 예약</strong><p>${ids.map(id=>companies.find(c=>c.id===id).name).join(' · ')}</p></div></div>${state.mode==='mortgage'&&ids.includes('hotel')?notice('다른 업무의 최근 연락','예시호텔: 기업자금 문자 09.16 발송 이력 예시. 중복 연락 여부를 확인하세요.','warn'):''}<form id="sms-form"><div class="form-grid"><label class="field wide"><span>예약 이름</span><input class="input" name="name" required value="${channelName[state.mode]} 상담 안내"></label><label class="field wide"><span>발송 내용</span><textarea name="message" required placeholder="사용할 발송 문구를 입력하세요.">안녕하세요. ${channelName[state.mode]} 상담 안내를 드립니다. 관심이 있으시면 담당자에게 연락 부탁드립니다.</textarea></label><label class="field wide"><span>예약 일시</span><input class="input" name="date" type="datetime-local" value="2026-09-18T10:00" required></label></div><p class="footnote">수신 연락처·수신거부·중복 연락·발신 정보는 실제 발송 전에 다시 확인합니다.</p></form>`,`${btn('취소','close')}<button class="btn primary" type="submit" form="sms-form">예약 예시 저장</button>`,'시안 동작 · 실제 문자 발송 없음');}
function openHandoff(id){current().company=id;const c=company();modal(channelName[state.mode]+' 영업 전달',`<form id="handoff-form"><p><b>${c.name}</b>의 상담을 전달합니다.</p><div class="form-grid"><label class="field wide"><span>영업 담당자</span><select class="select" name="sales"><option>이영업</option><option>정영업</option></select></label><label class="field wide"><span>상담 요약</span><textarea name="memo" required>${esc(current().calls.filter(x=>x.id===id).at(-1)?.memo||c.memo)}</textarea></label></div>${state.mode==='mortgage'?`<div class="subsection"><h3>상담 대상 부동산</h3>${c.properties.map(pid=>`<label class="plain-row"><input type="checkbox" name="property" value="${pid}" checked><span class="grow">${properties[pid].name}</span>${dataBadge(c)}</label>`).join('')||'<p class="muted">연결된 물건 없음 · 물건 범위 확인 전으로 전달</p>'}${c.status==='draft'?notice('검토 대기 상태도 함께 전달됩니다','금액과 소유권이 확정된 것으로 표시하지 않습니다.','warn'):''}</div>`:''}</form>`,`${btn('취소','close')}<button class="btn primary" form="handoff-form">전달 예시 저장</button>`,'상담 기록과 참조한 자료 상태를 함께 연결');}
function openSource(title){const p=property()||properties[company().properties[0]];if(!p){toast('확보한 원문이 없습니다.');return;}modal('원문 근거',`<p><b>${p.name}</b> · 열람 ${p.source} · 본문 ${title.includes('신탁')?'부속 원부':'2쪽'}</p><div class="paper-document"><h3>등기 원문 연결 예시</h3><p style="text-align:center;font-size:12px">시안용 합성 문서 · 실제 등기부가 아닙니다</p><table><tr><th>구분</th><th>확인할 내용</th></tr><tr class="highlight"><td>${esc(title)}</td><td>${p.draft?'검토가 필요한 원문 항목을 이 위치에서 확인합니다.':p.trust?'신탁 계약·당사자·공동 순위·한도와 조건':p.owner+' / '+p.amount+' / '+p.group}</td></tr><tr><td>자료 연결</td><td>선택한 물건 · 열람일 · 판독 버전 · 해당 쪽</td></tr></table></div><p class="footnote">실제 구현에서는 이 자리에 인증된 PDF의 해당 쪽을 엽니다. 원본 다운로드는 이미 배포된 회사 권한 검사 경로에 연결합니다.</p>`,btn('닫기','close'),'시안의 원문 연결 동작');}
function collateral(){modal('공동담보 A',`<dl class="facts">${fact('채권최고액','36억원','묶음에 한 번 표시 · 실제 잔액 아님')}${fact('권리자 / 채무자','가상은행 / 예시호텔')}${fact('설정 접수','2023.04.12','목록과 각 등기 접수를 대조한 상태 예시')}${fact('확인된 담보 물건','2개')}</dl><div class="subsection"><h3>같은 담보에 연결된 물건</h3>${['hotel-building','hotel-land'].map(id=>`<div class="plain-row"><div class="grow"><b>${properties[id].name}</b><small>물건 ${properties[id].code} · ${properties[id].type}</small></div>${btn('물건 보기','linked-property',`data-id="${id}"`,'small')}</div>`).join('')}</div><p class="footnote">두 등기에 36억원이 반복되어도 72억원으로 합산하지 않습니다. 주소·은행·금액만 같은 경우에는 동일 담보로 묶지 않습니다.</p>`);}
document.addEventListener('click',e=>{const el=e.target.closest('[data-act]');if(!el||el.disabled)return;const a=el.dataset.act,s=current();switch(a){case'group':state.openGroup=state.openGroup===el.dataset.mode?null:el.dataset.mode;renderNav();break;case'nav':navigate(el.dataset.mode,el.dataset.page,el.dataset.id);break;case'company':navigate(state.mode,'detail',el.dataset.id);break;case'company-tab':s.companyTab=el.dataset.tab;render();break;case'property':s.property=el.dataset.id;s.propertyTab='summary';render();break;case'property-tab':s.propertyTab=el.dataset.tab;render();break;case'mobile-nav':$('.prototype').classList.toggle('nav-open');el.setAttribute('aria-expanded',$('.prototype').classList.contains('nav-open'));break;case'clear-filters':s.search='';s.filter='all';render();break;case'select':s.selected=el.checked?[...new Set([...s.selected,el.dataset.id])]:s.selected.filter(id=>id!==el.dataset.id);render();break;case'select-all':{const ids=filtered().filter(c=>c.sms&&s.stages[c.id]!=='제외').map(c=>c.id);s.selected=el.checked?[...new Set([...s.selected,...ids])]:s.selected.filter(id=>!ids.includes(id));render();break;}case'sms-modal':openSMS();break;case'close':$('#modal').close();break;case'sms-delivered':{const camp=s.campaigns[+el.dataset.index];camp.status='delivered';camp.ids.filter(id=>s.stages[id]!=='제외').forEach(id=>{if(!s.smsSent.includes(id))s.smsSent.push(id);if(s.stages[id]==='미접촉')s.stages[id]='문자 발송';});render();toast(channelName[state.mode]+'의 발송 완료 예시를 반영했습니다. TM 상담에서 확인하세요.');break;}case'tm-for-company':{
 const target=companies.find(x=>x.id===el.dataset.id);
 if(['영업 전달','제외'].includes(s.stages[target.id])){
  modal('상담 대상 상태 확인', '<p>'+esc(target.name)+' · '+esc(s.stages[target.id])+'</p><p>선택한 회사는 현재 TM 상담 대상에서 빠져 있습니다. 다른 회사의 상담으로 바꾸지 않습니다.</p>');
 }else if(!target.sms&&state.mode==='mortgage'){s.tmLane='contact';navigate(state.mode,'tm',target.id);}
 else if(s.smsSent.includes(target.id)){s.tmLane='normal';navigate(state.mode,'tm',target.id);}
 else{modal('문자 발송 후 상담할 수 있습니다','<p>'+esc(target.name)+'는 아직 문자 발송이 완료되지 않았습니다.</p>',btn('닫기','close')+btn('문자 발송 보기','company-sms',`data-id="${target.id}"`,'primary'));}
 break;
}case'company-sms':s.selected=[el.dataset.id];$('#modal').close();navigate(state.mode,'sms');break;case'contact-tm':s.tmLane='contact';navigate(state.mode,'tm');break;case'tm-lane':s.tmLane=el.dataset.lane;render();break;case'cancelled':s.showCancelled=el.checked;render();break;case'source':openSource(el.dataset.title||'등기 원문');break;case'collateral':collateral();break;case'linked-property':$('#modal').close();navigate(state.mode,'detail','hotel');current().property=el.dataset.id;render();break;case'handoff-modal':openHandoff(el.dataset.id);break;case'sales-modal':{const d=s.deals[+el.dataset.index];modal('영업 진행 업데이트',`<form id="sales-form" data-index="${el.dataset.index}"><div class="form-grid"><label class="field wide"><span>진행 상태</span><select class="select" name="status">${['수신','진행중','성사','무산'].map(v=>`<option ${v===d.status?'selected':''}>${v}</option>`).join('')}</select></label><label class="field wide"><span>진행 메모</span><textarea name="memo" required>${esc(d.memo)}</textarea></label></div></form>`,`${btn('취소','close')}<button class="btn primary" form="sales-form">변경 예시 저장</button>`);break;}case'task-done':s.calls[+el.dataset.index].done=true;render();toast('이 업무의 할 일을 완료했습니다.');break;case'manage':navigate(state.mode,'manage');break;case'manage-note':modal(el.dataset.title,'<p>기존 공통 관리 화면을 유지합니다. 대상 배정·업무별 설정은 기업자금과 모기지를 구분합니다.</p>');break;case'recover':s.special=null;render();toast('목록을 다시 불러왔습니다.');break;case'checklist':modal('부동산 확인 자료','<ol><li>회사 주소와 실제 영업장 주소 확인</li><li>토지·건물·호실별 등기 식별</li><li>회사와 소유자·위탁자의 관계 확인</li><li>등기 본문과 참조 목록·원부 확보</li></ol><p class="muted">시안에서는 열람 신청이나 결제를 실행하지 않습니다.</p>');break;case'scenario':{const v=el.dataset.value;state.mode='mortgage';state.openGroup='mortgage';if(['loading','error'].includes(v)){current().special=v;render();}else{navigate('mortgage','detail',({portfolio:'hotel',draft:'draft',trust:'trust',unit:'unit',empty:'empty'})[v]);if(v==='trust'){current().propertyTab='trust';render();}}$('.prototype').scrollIntoView({block:'start'});break;}case'reset':state={mode:'mortgage',openGroup:'mortgage',channels:{funding:initChannel('funding'),mortgage:initChannel('mortgage')}};render();toast('시안의 가상 데이터를 초기화했습니다.');break;case'mobile-preview':modal('모바일 화면 미리보기',`<iframe title="390px 모바일 시안" src="index.html?embedded=1${location.hash}" style="display:block;width:390px;max-width:100%;height:620px;border:1px solid var(--line);margin:auto;border-radius:8px"></iframe>`,'','실제 390px 브라우저 폭에서 반응형 화면을 표시합니다.');break;}});
document.addEventListener('input',e=>{if(e.target.id==='company-search'){const start=e.target.selectionStart;current().search=e.target.value;render();const input=$('#company-search');input.focus();input.setSelectionRange?.(start,start);}if(e.target.closest('#call-form'))current().forms[company().id]=Object.fromEntries(new FormData($('#call-form')));});
document.addEventListener('change',e=>{if(e.target.id==='status-filter'){current().filter=e.target.value;render();}if(e.target.id==='tm-company'){current().company=e.target.value;render();}if(e.target.closest('#call-form'))current().forms[company().id]=Object.fromEntries(new FormData($('#call-form')));});
document.addEventListener('submit',e=>{e.preventDefault();const f=e.target;const d=new FormData(f),s=current();if(f.id==='sms-form'){const ids=s.selected.filter(id=>companies.find(c=>c.id===id).sms&&current().stages[id]!=='제외');s.campaigns.push({name:d.get('name'),date:d.get('date').replace('T',' '),ids,status:'scheduled'});s.selected=[];$('#modal').close();navigate(state.mode,'sms');toast(channelName[state.mode]+' 예약 예시를 저장했습니다. 실제 발송은 없습니다.');}if(f.id==='call-form'){if(!String(d.get('memo')).trim()){$('#call-error').textContent='통화 메모를 입력해 주세요.';f.elements.memo.focus();return;}if(d.get('due')&&!String(d.get('next')).trim()){$('#call-error').textContent='예정 일시에 할 일을 입력해 주세요.';f.elements.next.focus();return;}s.calls.push({id:company().id,...Object.fromEntries(d),stamp:'09.17 방금',done:false});s.stages[company().id]=d.get('reaction')==='거절'?'제외':'상담 중';s.forms[company().id]={};navigate(state.mode,'history');toast(channelName[state.mode]+' 상담 기록을 저장했습니다.');}if(f.id==='handoff-form'){const id=company().id;s.deals.push({id,status:'수신',sales:d.get('sales'),memo:d.get('memo'),properties:d.getAll('property')});s.stages[id]='영업 전달';$('#modal').close();navigate(state.mode,'sales');toast(channelName[state.mode]+' 영업 전달 예시를 저장했습니다.');}if(f.id==='sales-form'){Object.assign(s.deals[+f.dataset.index],{status:d.get('status'),memo:d.get('memo')});$('#modal').close();render();toast('이 업무의 영업 진행을 변경했습니다.');}if(f.id==='audio-form')modal('녹음 요약 검토',`<p>${esc(d.get('audio-company'))} · ${channelName[state.mode]}</p><div class="notice"><div><strong>요약 초안 예시</strong><p>담보 조건 비교를 희망하며 현재 대출 관련 자료를 요청했습니다.</p></div></div><p>실제 음성 인식 결과가 아닙니다. 실제 구현에서는 초안을 확인·수정한 뒤 해당 업무의 상담으로 확정합니다.</p>`);});
$('#modal').addEventListener('click',e=>{if(e.target===$('#modal')){const r=e.target.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)e.target.close();}});
const initial=location.hash.slice(1).split('/');if(['mortgage','funding'].includes(initial[0])){state.mode=initial[0];state.openGroup=initial[0];if([...menus.map(m=>m[0]),'detail','manage'].includes(initial[1]))current().page=initial[1];if(initial[1]==='detail'&&companies.some(c=>c.id===initial[2])){current().company=initial[2];current().property=company().properties[0];if(['summary','rights','timeline','documents','trust'].includes(initial[3]))current().propertyTab=initial[3];}}
if(new URLSearchParams(location.search).has('embedded')){$('.review-bar').hidden=true;$('.scenario-bar').hidden=true;$('.notes').hidden=true;}
render();

document.addEventListener('keydown',e=>{
 if(e.key==='Escape'&&$('.prototype').classList.contains('nav-open')){
  $('.prototype').classList.remove('nav-open');$('.mobile-bar button').setAttribute('aria-expanded','false');$('.mobile-bar button').focus();
 }
 const tab=e.target.closest('[role="tab"]');
 if(!tab||!['ArrowLeft','ArrowRight','Home','End'].includes(e.key))return;
 const group=tab.closest('[role="tablist"]'),name=group.getAttribute('aria-label'),tabs=[...group.querySelectorAll('[role="tab"]')];
 let i=tabs.indexOf(tab);i=e.key==='Home'?0:e.key==='End'?tabs.length-1:(i+(e.key==='ArrowLeft'?-1:1)+tabs.length)%tabs.length;
 const wanted=tabs[i].textContent;e.preventDefault();tabs[i].click();
 const updated=document.querySelector('[role="tablist"][aria-label="'+name+'"]');
 [...updated.querySelectorAll('[role="tab"]')].find(x=>x.textContent===wanted)?.focus();
});
