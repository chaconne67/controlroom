# 투자자 프로파일링 문구 최소 수정

작성일: 2026-09-18 KST

> 2026-09-21 후속: 주인님은 개별 문구 치환보다 전체 질문·선택지의 의미와 다섯 단계의 적절성을 확인하라고 취지를 정정했습니다. 이 문서의 네 문구 변경은 당시 작업 이력입니다. 현재 검토와 재개 정보는 [13개 문항·65개 선택지 의미 검토](2026-09-21-investor-questionnaire-semantic-review.md)를 따릅니다. 후속 작업 중 만든 7번 `0%` 초안은 철회·복구했습니다.

상태: 문구 최소 수정·기존 체계 보존 검증·코드 커밋·푸시 완료 / 이번 운영 배포 미실행

실행 정본: `chaconne@49.247.192.127:/home/chaconne/projects/fundkeeper`, `origin/master`

## 1. 승인된 목표와 제외한 제안

후속 RA 메일과 타사 사례의 취지는 알고리즘 투자상품에 맞지 않는 지나친 무손실 표현을 합리적인 선호 표현으로 완화하는 것이다. 전체 13개 문항을 검토했으며, 주인님은 기존 설문·점수·등급 체계를 유지하고 일부 문구만 조금 바꾸라고 명시했다.

- 타사 사례는 문구의 의미를 참고하는 근거다. 예시 문항 번호·손익 비율·배점으로 현재 설문을 교체하지 않는다.
- 새 무손실 요구자 구별, 위험 확인·동의 절차, 추천 제외 분기, 설문 판본 검사, 답변 단계별 신규 등급 상한은 제안에서 철회한다.
- 점수 계산 및 등급 분류는 이번에 수정하지 않는다. 기존 채점으로 해결되므로 배점 조정도 필요 없다.
- 알고리즘 설명서·제출 자료·상품 구성까지 연쇄 변경하지 않는다.

## 2. 작업 시작 상태와 기준선

- main `HEAD`와 `origin/master`: `d0a3ca1df251ed108ff0ce9ee365453f41a771c0`; 코드 작업 트리 clean.
- 조정실 controlroom에는 앞선 작업에서 작성한 이 계획과 docs README 수정만 있다. 다른 변경이 생기면 보존한다.
- `UserProfileView` → `profile.html`의 기존 13개 답변 → `scoring.process_survey` → 기존 검증·합산·기본 분류·분류 상한 → `profile-result.html` → 기존 등급별 상품 파셜 순서다. DB 저장·주문은 호출하지 않는다.
- 원점수 최대 64, 100점 정규화·반올림, 공격형 80 / 적극투자형 60 / 위험중립형 40 / 안정추구형 26 기준을 유지한다.
- `q7=1` 또는 `q8=1`은 안정형 상한, `q1=1`은 안정추구형 상한이다. 겹치면 안정형이 우선한다. 점수 자체는 변하지 않는다.
- `q8=2`만으로는 상한을 추가하지 않는다. `q7=2/3`의 결과도 기존과 같다.
- 변경 전 기존 stdlib 테스트 7개 통과. 현재 앱 이미지의 네트워크 없는 독립 컨테이너에서 읽기 전용 소스와 `python -B`로 실행했다.

## 3. 전체 문항 검토와 최소 문구 변경

| 번호·주제 | 결과 |
|---|---|
| 1 투자목적 | 원금보전 등 기존 목적·배점 유지 |
| 2 연소득 | 유지 |
| 3 총자산 대비 금융투자 비중 | 유지 |
| 4 투자 여유자금 | 유지 |
| 5 투자경험 상품 | 예적금·보험을 모두 안전자산으로 단정하지 않도록 경험 표현만 수정 |
| 6 투자경험 기간 | 유지 |
| 7 손실 감내 | 첫 선택지의 무손실·0% 표현을 원금보전 선호로 완화. 나머지 5%·10%·20% 단계와 값 유지 |
| 8 수익과 위험 태도 | 첫 선택지의 절대 손실 불가를 보전 우선 선호로 완화. 나머지 답변·값 유지 |
| 9 연령 | 유지 |
| 10 투자예정 기간 | 유지 |
| 11 손실 시 반응 | 마지막 답변의 전혀 동요하지 않음을 비교적 차분함으로 완화 |
| 12 금융지식 | 유지 |
| 13 부양가족·단기 지출 | 유지 |

교체할 문구:

- 5번: `예금, 적금, 보험 등 안전자산만` → `예금, 적금, 보험 등의 가입 경험만 있음`.
- 7번 첫 답변: `손실을 전혀 감내할 수 없음 (0%)` → `원금보전을 선호하며 손실 위험을 최소화하고 싶음`.
- 8번 첫 답변: `원금보전이 최우선이며 손실은 절대 불가` → `낮은 수익을 감수하더라도 원금보전을 우선함`.
- 11번 마지막 답변: `손실에 전혀 동요하지 않고 장기 관점을 유지함` → `손실에도 비교적 차분하게 대응하며 장기 관점을 유지함`.
- 설문 안내, 웹 배점표의 같은 문구, 안정형 분류 이유·성향 설명도 위 뜻에 맞춰 교체한다. 새 안내 단계나 컨트롤은 만들지 않는다.

## 4. 변경 범위와 최소 구현

기존 5개 파일의 표시 문구만 바꾼다.

- `userprofile/templates/userprofile/profile.html`
- `userprofile/templates/userprofile/table.html`
- `userprofile/templates/userprofile/profile-result.html`
- `userprofile/scoring.py`: 분류 이유 문자열만 변경
- `userprofile/tests.py`: 해당 이유 문자열 기대값만 함께 변경. 점수·등급 기대값은 그대로 유지

최소 구현 게이트: 2단계에서 멈춤 — 기존 문항·상한·결과 경로 재사용. `git grep`로 현재 문구와 `process_survey`, `UserProfileView` 및 파셜 연결을 확인했다. 새 함수·클래스·상태·입력·의존성 없음. HTML 구조·CSS·JS도 그대로다.

범위 밖: 알고리즘 설명서, `gen_appendix.py`, PPTX/PDF 생성·덮어쓰기, Drive/포털 제출, 상품 위험도·상품 구성·추천 링크, 운영 DB·환경설정·예약·주문·결제·실제 메일.

이번 문구 변경의 운영 배포는 별도 요청 전 실행하지 않는다. 2026-09-16의 이전 배포 승인은 당시 완료된 변경의 이력이다. 현행 main의 제품별 Compose 배포만 사용할 수 있으며 옛 Swarm 스크립트는 금지한다.

## 5. 보호 불변조건과 검증

- 문항 수·번호·선택지 숫자 값·배점·점수 구간·기존 분류 상한과 겹침 우선순위 유지.
- 기존 테스트 7개의 점수·등급 기대값을 변경 전후 동일하게 검증.
- `scoring.py`는 이유 문자열을 제외한 Python 구문 구조가 기존과 동일한지 비교.
- 설문 HTML의 입력·label 연결·버튼·CSS 클래스·스크립트 구조가 동일한지 비교.
- 네트워크·운영 DB와 분리한 Django 환경에서 실제 뷰 GET/POST·배점표 렌더·오류 흐름 검증.
- Tailwind 빌드와 숨은 조정실 Chrome에서 390px·480px·1024px 문항·결과 확인. 새 CSS를 임의로 주입하지 않는다.
- 실행 계약을 바꾸지 않는 일반 문구 변경이므로 프로젝트 지침의 저위험 예외에 따라 코드 리뷰 루프는 생략하고 범위·diff를 직접 확인한다.
- 사용자 파일·인증·데이터·제품 AI와 기존 외부 효과 보존. 검증 불가 항목은 성공으로 보고하거나 미검증 상태로 커밋하지 않는다.

## 6. 재개 정보

- 주인님 최신 지시로 앞선 광범위한 계획 A~D를 폐기했다. 이 문서의 문구 최소 수정만 실행한다.
- 최종 코드: main `HEAD`와 `origin/master` 모두 `c77316e683b4b3ab369011931a6719aef49af1e6`, clean. 5개 파일의 문구 14줄만 변경했다.
- 검증 결과: 변경 전후 기존 테스트 7개 통과. 이유 문자열 한 곳을 제외한 채점 AST 동일, 625개 답변 조합의 점수·등급·보수 성향 플래그 동일. 3개 HTML의 태그·속성·스크립트 동일.
- 독립 Django 검사와 실제 뷰 GET·배점표·POST 7사례·답변 누락 오류 통과. 운영 DB 대신 dummy 엔진, 비밀값 없는 설정, 읽기 전용 소스, signed-cookie 세션을 사용했다.
- Tailwind 빌드 통과. 기존 이미지의 설치된 Tailwind와 검증 이미지의 Playwright 내장 Node를 사용하고 빌드 파일은 검증 컨테이너의 임시 영역에만 생성했다. 제품 의존성 추가·운영 CSS 수정 없음.
- Windows 공용 숨은 Chrome `fundkeeper-main-profiling-wording`, run-id `20260918`에서 390·480·1024px 실제 문항·결과 확인 및 13문항 선택·제출 통과. 390px q7=1 → 안정형94, 480px q8=1 → 안정형94, 1024px q8=2 → 공격형95. 이전·다음 이동 시 선택 유지, 다시 검사하기, 가로 넘침 없음, 콘솔 오류·실패 요청 없음.
- 화면 증거: `C:\Users\chaconne\.hidden-browser\evidence\fundkeeper-main-profiling-wording\20260918`. 앱 CSS 응답의 빌드 해시 `156cdafe423f1d0f9934adc89ce4b9225e5ca09226a0afc34b482015262c7415`와 계산된 스타일 확인. 입력 데스크톱·전경 창 유지.
- 검증 서버는 main의 내부 전용 Docker 망(외부 통신 차단), 컨테이너8000 → SSH 내부 연결 → Windows127.0.0.1:18773으로만 접근했다. 단일 요청 서버의 브라우저 대기 연결 문제는 검증용 표준 멀티스레드 서버로 바꿔 재검증했으며 제품 코드에는 반영하지 않았다.
- 검증 후 전용 Chrome·서버·SSH 터널·전용 Docker 망 제거 완료. 임시 빌드 파일도 컨테이너 종료와 함께 제거했다. 위 개발 URL은 종료됐다.
- GitHub push에서 기존 의존성 보안 경고(410건)가 표시됐다. 이번에는 의존성을 변경하지 않았으며 보안 점검은 이번 문구 검증 범위 밖이다.
- 다음 행동: 운영 배포 요청을 받으면 현행 main의 Coconut 제품별 Compose 정의와 scoped 배포·복구 경로를 확인한 뒤 이번 커밋만 반영하고 실제 운영 설문을 확인한다. 아직 미실행은 이번 운영 배포뿐이다.
