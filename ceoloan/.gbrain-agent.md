# CEO Loan GBrain

전역 `~/.gbrain-agent.md`의 공용 `default` 소스와 공식 조회 명령을 먼저 따른다. 이 프로젝트에서는 아래 맥락을 추가로 읽는다.

1. 작업 전 `project/ceoloan-operating-context`를 읽는다.
2. `ceoloan <작업 기능·화면·모델·오류>`로 공용 GBrain을 검색하고 작업에 관련된 프로젝트 개요·구조·배포 런북을 확인한다.

GBrain은 과거 맥락이며 현재 코드와 서버가 최종 기준이다.
