# 재무 정보가 있는 모기지 회사 IROS 100개 시도

## 목표와 범위

2026-09-24 주인님은 모기지 대상 회사 중 재무 정보가 있는 회사 100개를 선정해 인터넷등기소 자료 추출을 실행하도록 지시했다. 주인님의 추가 답변에 따라 완료 PDF 100개를 채우기 위한 더 큰 후보 풀이 아니라 **정확히 100개 회사를 각 한 번씩 시도**한다. 부동산 검색에서 한 건으로 확정되지 않거나 사이트가 실패하면 유료 열람 수와 최종 PDF 수는 100보다 적을 수 있다. 결제 한도는 열람 한 통당 700원, 최대 100건·70,000원이다. 공식 SSP는 검증한 원본 PDF·구조 JSON을 회사에 연결해 중앙 DB에도 저장한다. 별도 의미 판독·공개 확정이나 웹 배포는 이 배치에 포함되지 않는다.

## 기준선과 대상 선정

- 코드 정본: main `chaconne@49.247.192.127:/home/chaconne/projects/ceoloan`, `ceoloan/main`의 `532f090`. Windows 실행본은 `C:\cretop-agent`의 `iros_ssp.py`, `iros_batch.py`, `iros_dom.py`다. 배치는 `iros_ssp.py collect`로만 실행한다.
- 이전 `20260919_iros_mortgage_100_001`은 91건 저장·확정 결제 63,700원, 결제 불명 0건, 선불 잔액 부족 중단이다. 그 manifest 500개 회사·주소 전부와 다른 실행에서 PDF/구조 JSON이 검증된 주소를 새 배치에서 제외했다. 이전 명단·승인·상태는 변경하지 않았다.
- 9월 24일 운영 `company_main`을 읽기 전용으로 조회했다. `mortgage_mortgagetarget.status=detail_pending`, 신뢰도 1·검토 불필요 본점 주소, 중앙 등기자료 미연결, 모기지 화면의 `financial_report.has_data`와 같은 결산연도·숫자값·지원 단위·계정 조건을 충족한 회사는 1,233개였다. 과거 후보와 검증 성공 주소를 제외하면 1,086개였고, 모기지 대상 생성 시각·ID 순서에서 중복 없는 회사와 주소 100개를 고정했다. 이 100개에는 재무 결산일이 2007~2025년인 회사가 포함된다. 재무 정보의 최신 연도 제한은 추가하지 않았다.
- 배치 정본: `C:\iros-agent\batches\20260924_iros_finance_100_001`의 `manifest.json`, `approval.json`, `state.json`. 명단 SHA-256은 `7a86a37f982d0abc70f10724f652de1ac5ab2b1c916106fd702b5cc38a5112a8`. 회사명·주소·고유번호는 공유 문서에 옮기지 않는다. `selection-audit.json`에는 집계와 조회 스냅샷 해시만 있다.
- 공식 `validate`와 `status`는 후보 100개·목표 100개·승인 70,000원, 시작 상태 0건을 확인했다. 실행 직전 현재 DB에서 후보 100개의 회사·주소·재무 값·모기지 상태·중앙 등기 미연결을 다시 조회했고 전부 일치했다.

## 마지막 성공본 선정과 검증

Windows `iros_dom.py`는 9월 19일 22:44 수정됐고, 같은 Windows 배치 경로에서 9월 20일 12:01까지 91번째 PDF 검증·DB 저장에 성공했다. 서버의 당시 파일 변경은 9월 19일 21:29였으므로, 주인님 지시에 따라 Windows의 마지막 성공본을 기준으로 삼았다. 원본 SHA-256 `67b72dfa0686ede94a284fd3327229ccd364508f37d6aab3fb219489981bdbc6`은 새 배치 폴더의 `iros_dom.last-success-original.py`에 보존했다.

그 성공본의 CAPTCHA 이미지 판독을 공식 서버 파일에 반영했다. 검색 중 세션이 끊겼을 때 강제 로그아웃·반복 재로그인하는 가지는 단일 세션 계약에 맞춰 중단하도록 바꿨다. 숫자 이미지가 없는 정상 로그인도 유지한다. 서버 코드 두 파일만 커밋해 `ceoloan/main`에 push했고 Windows 실행본을 같은 해시로 맞췄다. 변경 전 IROS 검사 80개, 변경 후 82개와 Ruff가 통과했다. 코드 리뷰에서 승인된 finding은 없었다. 운영 웹 배포는 요청받지 않아 실행하지 않았다.

## 실행과 중단 증거

9월 24일 13:59:25 숨김 창에서 공식 `iros_ssp.py collect`를 시작했다. 처음 후보의 `home` 단계에서 인터넷등기소가 **서비스 점검중** 화면을 반환했다. 저장된 화면은 `C:\iros-agent\screenshots\dom_home_failure_20260924_140007.png`이고, 화면의 점검 시간은 **9월 24일 07:00 ~ 9월 26일 02:40**이다. 작업이 일찍 끝나면 앞서 개시할 수 있다고 표시한다.

14:00:07의 공식 `status`는 `stopped`/`workflow_failed`, 완료 0, 중앙 DB 저장 0, 확정 결제 0, 결제 불명 0, 결제 전 실패 1, 미시도 99, 확정 비용 0원이다. 첫 후보의 실패 단계는 `home`, 결제 상태는 `none`이다. 수집 프로세스와 전용 Chrome은 종료됐다. 실행 후 20초 관측에서 입력 데스크톱은 `Default` 전후 동일, 전경 변화 0회, 도구·데스크톱 간섭 0회였다. 이 관측은 수집 종료 후의 구간이므로 실행 전체의 비간섭을 입증하지 않는다.

## 재개 조건과 다음 행동

사이트 점검 종료 또는 실제 정상 홈 확인 전에는 같은 조건으로 반복하지 않는다. 재개 전에 현재 `state.json`과 PDF·결제 기록을 읽고, 결제 불명·진행 중인 다른 IROS 수집·기존 배치와의 중복이 없는지 확인한다. 고정 manifest/approval은 그대로 두고 공식 `iros_ssp.py collect --resume-stopped`만 사용한다. `unpaid_failed`로 기록된 첫 후보는 건드리지 않으며, 나머지 99개 회사를 순서대로 처리한다. 현재 선불 잔액은 확인되지 않았다. 결제가 거절되면 부족 사유와 결제 상태를 공식 결과로 확인하고 추가 결제를 멈춘다.

수집 종료 후에는 공식 `status`, 각 성공 PDF·registry JSON의 고유번호/표제부·갑구·을구, DB 저장 영수증, 확정 결제 수와 비용, 결제 불명 0건을 대조한다. 100개 후보가 모두 끝나도 PDF가 100개 미만이면 사용자에게 실제 성공 수와 제외 이유를 보고한다. 실행 예약 여부는 주인님 답변을 기다린다.
