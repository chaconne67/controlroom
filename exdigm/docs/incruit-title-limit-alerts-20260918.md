# 자동게시 간결 제목·익명 소개20자·사이트 제약 수정

## 최종 결과·검증 범위

2026-09-18 **15:07:49 KST 공식 운영 반영 완료**. 최종 코드 `0b237350c87e06e288614ec807b0a6a344fa199e`는 익명 회사 소개의 제품 한도를15자에서 **20자**로 변경한다. 긴 원본 회사 설명을 제목에 붙이는 공통 생산 원인도 수정해, 기존 익명 소개 생성·검증 경로의 업종/근거 있는 기업규모+정리된 핵심 직무로 제목을 만든다. 원래109자 사례는 “화학소재 대기업-설비관리자”14자로 생성됐다.

여섯 사이트의 기존 입력 경로에 공통 최종 제목/현재HTML 제약 검사를 연결했다. 생성값과 최종 실제 값이 다르거나 길이/문자 제약을 어기면 성공으로 반환하지 않는다. 활성5사이트에서 공식 비제출 dry로 실제 제목 일치를 확인했다. 최신20자 코드에서도 인크루트 `20260918151127`의 최종 verify와 실제 화면에서14자 제목이 유지됐다. 다른4사이트의 입력 경로와 확정 제목은20자 조정으로 변경되지 않았다.

공통 변경의 전체549pass/4skip 및 보호216pass, 최종20자 조정의 producer/직접 소비자182pass·같은 소개 집중6pass(20/21경계)·보호216pass를 확인했다. 현재 실행중 앱/SSE/알림 서비스의 동일 커밋과 상수20, 서비스5개1/1·작업자/지원11 active/jobs0/drainoff·HTTPS200을 직접 확인했다. 원본10행은 동일하고 새 검증run/site_run DB행은0개다.

실제 게시·서버 최종 수락·공개 공고 생성은 검증하지 않았다. 과거4건 실패·회사 원본·본문·인증·비활성 피플앤잡 설정·미배포 마크다운 변경은 보존했다. 피플앤잡 실제 폼/서버 한도와 HTML 길이 미선언 사이트의 서버 한도는 미확인이다. 별도 기존 화면 기준선5실패도 그대로 남아 있다. 아래 단계별 승인·검증 기록은 당시 상태이며, 현재 상태는 이 절과 마지막 재개 정보를 우선한다.

## 승인·목표·범위

주인님이 인크루트 실패 알림과 13:37 NotSupportedError 조사 결과를 보고 “수정해”로 승인했다. 인크루트의 실제 모집제목 60자 제한을 기존 제목 검사·AI 재작성에 연결하고, 등록 완료를 확인하지 못했을 때 현재 제출에서 나온 사이트 안내를 알림과 공고 화면에 전달한다. 우리 백업 준비 조회에서 발생한 NotSupportedError에는 확인된 처리 결과만 덧붙인다.

코드 수정·검증·리뷰·기록과 공식 운영 반영을 수행한다. 실제 게시 재시도·마감·삭제, 과거 게시 실패 기록의 성공 처리, 다른 사이트 규칙, 인증·공유 브라우저·권한·DB 인프라·Hermes·이력서 수정은 범위 밖이다. 실제 사이트 검증은 게시하지 않는 공식 dry 경로로 한정한다.

## 시작 상태와 증거

- 2026-09-18 운영 clean main과 개발 clean detached HEAD 모두 `4795d1ece34cf64502f4642295bdccf34da5e0bf`. 기존 미배포 마크다운 변경 `4ac2f0b26027c58c96fda142e8c7e690eb340539`는 `preserve/resume-verification-markdown-20260918`에 보존한다.
- 코드 위치: `chaconne@49.247.202.197:/home/chaconne/exdigm-debug`. 운영 원본 `/home/chaconne/exdigm`은 직접 수정하지 않는다.
- controlroom의 기존 다른 작업 `projects/ceoloan/docs/mortgage-workspace-design-20260917.md` 수정은 보존한다.
- 프로젝트 `c7d6d740-d7c6-4c30-a751-f9163a7b26e4` “아산공장 설비관리”. 제목은 109자이며 기존 `_posting_title_defects('incruit', title)`는 길이 결함을 반환하지 않았다.
- 공식 저장 증거의 실제 안내: “모집제목은 60자 이내로 입력해주세요.”. 14:01·14:06의 `20260918140058`/`20260918140523/result.json`을 직접 확인했다. 다른 작업 에이전트가 14:11·14:12의 `20260918141029`/`20260918141215/result.json`도 같은 109자 제목·같은 안내임을 확인했다. 제목 SHA-256 `a77c2f071f358c16e70bf54ed2e3c75fce5a8a3374c06c6dcae45da64e7b65d2`.
- 제출 전부터 공고번호가 있었고 `JobPostRegYn=Y`는 코드가 설정한다. 이 값들을 게시 성공 근거로 사용하지 않는다. 원래 실패 행·payload·evidence는 보존한다. 외부 공개 상태는 미확인이다.
- `OperationalError 3182bdb8-c37f-4749-1ddd-7959cd1a52fb`: 우리 관리용 백업 준비 조회에서 nullable outer join 전체를 FOR UPDATE해 PostgreSQL이 거절했다. 조회를 `select_for_update(of=('self',))`로 고친 후 백업과 이력서 보완 안내는 완료됐다. 이 오류는 기록상 아직 pending·처리 이력 없음이다.

## 기준선 잠금·최소 구현·공식 경로

보호: 다른 사이트의 확인된 가중 길이 규칙, 정상 제목의 무변경·불필요한 AI 호출 없음, 공개 고객사 실명 차단, 등록 완료·공개 확인의 기존 성공 판정, 현재 제출 이전 대화상자의 분리, 원래 오류와 게시 이력·원본 데이터·다른 미배포 작업을 유지한다.

최소 구현 게이트: 2단계에서 멈춤. 실제 검색 `AUTO_POSTING_TITLE_WEIGHT_LIMITS`, `_posting_title_defects`, `_site_posting_title`, `_publish_current_form`, `_accept_dialog`, `auto_posting_failure_reason`, `record_processing_result`로 기존 책임을 확인했다. 새 의존성·러너·모듈·별도 AI 경로를 만들지 않는다. 제목 제한은 기존 가중치 표에 문자 수 단위를 함께 표현해 검사와 AI 지시를 공유한다. 사이트 안내는 기존 대화상자 기록을 재사용하고 전체 DOM 상태를 사람에게 던지는 실패 설명을 대체한다.

실행 순서: 기존 워커 `build_auto_posting_payload` → `_site_posting_title` → `_posting_title_defects`/필요한 경우 기존 `call_llm_json`의 최대 2회 재작성 → `manage.py autoposting` → `AutoPosting.main` → `Incruit.run` → 입력 검증 → `_publish_current_form`의 현재 제출 대화상자 기록 → 기존 워커 실패 저장·알림 및 `auto_posting_failure_reason` 화면 표시. dry에서는 입력 검증에서 끝나며 제출하지 않는다.

추가 성공조건: 인크루트 60자는 AI 없이 유지, 61자 이상은 의미를 보존하는 기존 AI 재작성 후 60자 조건 검사, 계속 초과하면 브라우저 실행 전 명시적 실패. 현재 제출의 사이트 안내만 실패 사유에 남고 이전 로그인 등 안내는 섞이지 않으며 일반 실패·기존 성공 판정은 유지된다. 실제 운영 AI 출력과 공식 dry 입력 결과를 확인한다.

## 수정 전 검증

- 공식 `scripts/debug_workspace.sh test`로 payload·인크루트 대기·워커·오류 처리 기록 검사: **238 passed, 5 warnings, 79.04초**.
- 추가 게시 수정·프로젝트 화면 관련 검사: **106 passed, 5 failed, 52 deselected**. 변경 전부터 발생한 실패: 목록 처리 중 문구, 게시 버튼 수(7/6), 두 곳의 처리 중/게시 중 문구, CRLF 저장 응답(200/302). 기존 코드·화면·검사 기대값은 건드리지 않는다. 실패 사유 번역 9개 기존 검사는 이 실행에서 통과했다. 수정 후 같은 범위로 비교한다.
- 운영 읽기 전용 역할 `exdigm_debug_ro`, DB `exdigm`, transaction_read_only on 확인. 원래 14:01·14:06 SiteRun 및 PostingSite 전체 행 해시를 잠갔다. NotSupportedError 행 전체 해시 `960469c8950fffc80fb76fa41123fb2aa0f408add7e2abcbe9491253de28eda5`.

## 1차 수정 중 재개 기록 — 당시 상태

기준선과 새 검사에서 결함 재현 후 기존 6개 파일만 수정했다. 새 검사 최초 7 failed/2 passed, 수정 후 제목 경계·재작성·payload·현재 대화상자·안전한 화면 표시·기존 번역 **18 passed**. 기존 238개와 추가 회귀 8개를 같은 명령으로 실행해 **246 passed, 77.60초**. Ruff·diff check 통과. 보호 검사와 실제 운영 입력의 기존 제품 AI 호출을 진행 중이다.

NotSupportedError 1건은 실제 해결 영수증과 대상 알림을 확인한 뒤 기존 `record_processing_result`로 succeeded 처리 이력 1개를 추가했다. 백업 `runtime/approved-incruit-title-fix-20260918/not-supported-error-backup.json`(0600, 부모0700)은 위 원래 행 해시와 같다. 바뀐 필드는 처리 상태·처리 이력·갱신 시각뿐이며 원래 오류 전 필드는 그대로다. 실제 인크루트 게시가 성공했다는 처리를 하지 않았다.

아직 코드 커밋·배포·실게시 재시도 없음. 다음: 보호 검사·기존 화면 실패 기준선 비교·목차 갱신·메인 코드 리뷰 → 검증된 단일 커밋 공식 배포 → 기존 payload 생성 함수와 공식 dry → 원래 4건 게시 실패 및 프로젝트/게시 연결/부모 실행 원본 해시 대조.

## 코드 리뷰 계약

1. 원천: 주인님의 조사 결과에 대한 “수정해”, 위 승인·범위, AGENTS 및 auto-posting/agent-script-role/prompt-guide/code-review-loop, 변경 전 `4795d1ec`의 공식 경로.
2. 역할: 사이트에 들어가기 전에 유효한 익명 제목을 생산하고, 등록 완료를 확인하지 못한 실제 안내를 기존 기록·알림·화면에 전달한다.
3. 경계: base `4795d1ece34cf64502f4642295bdccf34da5e0bf` 대비 6개 파일(`auto_posting/sites/incruit.py`, `projects/models.py`, `projects/services/auto_posting_workflow.py`, 제목 payload·인크루트 대기·프로젝트 화면 기존 검사 파일)과 직접 호출자·LLM·실패 저장/알림·화면 소비자. 별도 운영 오류 처리 이력은 기존 서비스의 승인된 단일 행 효과로 확인한다.
4. 입력: 기존 프로젝트 회사 설명·직무, 현재 사이트 선택, 정규화된 제목, 기존 AI JSON, Playwright의 현재 제출 대화상자 문자열, 기존 실패 상태·직원 조회 권한. 제목의 사실을 AI 답으로 취급하지 않으며 스크립트가 의미를 임의로 잘라 선택하지 않는다.
5. 절차: 제목 결함 검사 → 필요할 때만 최대2회 기존 AI 재작성과 재검사 → privacy 검사 → payload/공식 엔진. 제출 시작 이후의 안내만 분리하고 기존 전환·관리 목록·공개 검증을 유지한다. 실패 저장·원래 기록/증거 보존, dry 비제출을 유지한다.
6. 출력: 인크루트 최대60자·다른 사이트 기존 가중 길이 제목 또는 명시적 생성 실패, 현재 제출 안내를 담는 실패 예외와 원문을 안전하게 표시하는 화면/알림. 실패 안내만으로 성공 상태를 만들지 않는다.
7. 보호: 위 보호 조건, 기존 다른 사이트·Unicode 정규화·JSON 재시도·완료 판정·권한·비밀값 차단·화면 escaping·216개 보호 계약·미배포 마크다운 작업·기존 데이터.
8. 비목표: 기존 화면 문구/버튼/CRLF 기준선 5실패, 실제 게시 재시도·마감·삭제·과거 게시 실패 성공 처리, DB/Hermes/인증/사이트 추가·새 러너·새 의존성·운영 폴더 직접 수정.
9. 검증 근거: 전체 diff와 직접 호출자/피호출자/소비자, 새 검사에서 실제 결함 재현, 246개 공식 직접 검사·화면 사유와 escaping 검사·고정 보호·Django/Ruff/catalog, 실제 제품 AI와 공식 dry, 원본10행 및 관리 오류 백업/변경 필드 대조.

리뷰 관점은 변경 diff와 1차 영향범위다. 질문: 사이트별 길이 단위/정상 무AI/2회 실패가 보존되는가, 실제 제출 안내만 관측 예외에 연결되는가, 실패가 기존 저장·알림·화면에 도달하고 원문이 안전하게 표시되는가, 기존 완료 판정과 dry 경계가 유지되는가. 범위 밖의 기존 문제를 finding으로 승인하지 않는다.

## 수정 후 검증·리뷰

- 제목/payload·인크루트 대기·워커·오류 기록: **246 passed**. 새 경계·현재 안내·화면 원문 안전 표시와 기존 번역 집중 검사 **18 passed**.
- 기존 화면 기준선과 같은 확장 검사: **107 passed, 5 failed, 52 deselected**. 실패 5개는 수정 전과 정확히 같은 사례·같은 기대값 차이다. 이번 화면 실패 사유 검사는 추가 통과했고 새 회귀는 관측하지 않았다. 이 5개를 전부 통과했다고 보고하지 않는다.
- 고정 보호 `6dd40643`의 18파일은 동일, 보호 검사 **216 passed**. Django check·Ruff·diff check 통과. catalog_update: state current, local.valid true, broken_references 없음, 추적 목차 변경 없음.
- 운영 데이터는 공식 조회 전용 경로로 읽어 실제 회사 설명·직무를 기존 제품 AI(`codex_cli`, `gpt-5.5`)에 전달했다. 109자 입력 → **“그룹계열 글로벌 첨단소재 전문기업-아산공장 설비관리”**, **28자**, 결함 없음, 10.34초. 운영 데이터·게시 상태는 쓰지 않았다. 비공개 직접 증거 `.debug/incruit-title-limit-20260918/live-title-baseline.json`(0600/0700)에 원본10행 해시와 결과를 보관했다.
- 메인 에이전트의 code-review 단일 패스: 전체 6파일 diff, 변경 상수의 사용처, payload→privacy→엔진→예외 저장/알림 및 화면 소비자를 직접 확인했다. 다른 사이트 가중치/AI 규칙, 정상 제목의 무AI, 두 번의 엄격 재검사, 현재 제출 안내 범위, escaping, 기존 등록 완료/공개 검증과 dry 비제출 계약을 검사 증거와 대조했다. **승인된 finding 및 열린 제품 계약 질문 없음**. 과거 화면 5실패는 이번 diff로 생긴 문제가 아니므로 수정하지 않았다.

## 1차 운영 반영·최종 dry 증거의 한계

공식 `scripts/deploy/deploy.sh prod`는 **2026-09-18 14:31:14 KST prod ok/exit0**으로 완료됐다. 수정 커밋 `c91fd73772584619c8b4c27f8b7a83949832ef6b`, 이미지 `exdigm_app:20260918142951`, 이미지 ID `sha256:c1740b603abfbc9f5c00398a950b0b9482b82f705480ea86a202897ca51aa9a3`. 배포 전 고정 보호18파일 및 실제 DOC/DOCX/PDF 전달 계약을 통과했다. 서비스5개1/1, 작업자·지원11 active/jobs0/drainoff, HTTPS200 확인.

공식 조회 전용 경로에서 저장하지 않은 임시 run/site_run 객체를 기존 `build_auto_posting_payload`에 전달했다. 새 UUID의 검증 파일만 생성했고 DB행·알림·과거 payload는 쓰지 않았다. 제목은 “그룹 계열 첨단 산업용 소재 기업-아산공장 설비관리”, 28자. 실제 명령 `manage.py autoposting --site incruit --mode dry --payload <new path>`는 `20260918143157` ok/exit0으로 반환했다. 하지만 최종 `incruit.fill.verify.postingTitle`과 dry.verify.postingTitle은 **빈 문자열**이었다. 첫 입력 report.len28은 있었으나 최종 값 일치를 검사하지 않았다. 따라서 명령 성공을 실제 제목 전달 성공·완료로 판정하지 않는다. 입력 성공이라고 설명한 진행 응답을 정정했다. 원본 증거는 `runtime/auto_posting/evidence/incruit/20260918143157/result.json`, 화면 `03-after-fill.png`다. 실게시 재시도는 없었다.

## 주인님 추가 지시 — 공통 제목 생산·다른 사이트 제약 검토

주인님이 “비슷한 유형의 문제가 발생하지 않도록 다른 사이트의 제약사항들도 검토 … 불필요하게 제목을 길게 만드는 것 자체가 문제”라고 추가 지시했다. 위 60자 제한만 채우는 수정에서 범위를 확장한다.

- 현재 모든 사이트는 긴 `Client.description`을 제목 답처럼 결합하고 한도를 초과할 때만 AI 재작성한다. 기존 `_exdigm_company_descriptor`는 업종+기업규모의 짧은 익명 소개를 만들지만 제목에서는 설명이 빈 경우에만 호출한다. 긴 소개를 제목에 붙이는 공통 생산 원인을 수정한다.
- 최초부터 짧은 익명 기업 소개+직무로 생성한다. 기존 익명 소개 생성·검증·repair를 재사용하고 고객사 설명·본문·직무 사실은 보존한다. 제목용 기업 소개는 업종/규모만 담도록 하며 길고 불필요한 연혁·시장 설명은 붙이지 않는다.
- 여섯 사이트의 제목 길이 단위·문자/공백·브라우저 입력 변화·최종 일치 검증을 검토한다. 확인된 사이트 규칙만 등록하고 미확인 길이를 추측하지 않는다. 일반 제목도 최종 실제 값이 확정 제목과 다르면 게시·dry 성공으로 인정하지 않는다. 인크루트에서 확인한 비게시 검증의 빈틈도 닫는다.
- 실제 게시/마감/삭제·원래 4건 실패 기록·인증/사이트 활성 설정·공고 본문·회사 원본 정보·다른 미배포 변경은 계속 보존한다.

새 코드 기준선은 clean detached/운영 main **c91fd737**다. 수정 전 공식 `scripts/debug_workspace.sh test tests/test_auto_posting*.py --tb=short`는 **527 passed, 4 skipped, 35 warnings, 160.61초**다. 건너뛴 4개는 기존 조건부 실제 연동 검사다. 기존 화면5실패·보호 pin18파일은 앞 절의 동일 기준을 유지한다. 1차 제한/실패 안내 수정은 운영 반영했지만 공통 제목 생성과 최종 실제 입력 검증은 이 추가 지시에서 계속 진행한다.

### 추가 구현 잠금

제목 생산은 기존 `_exdigm_company_descriptor`를 모든 사이트의 공통 생산 단계에서 호출하고 기존 `_structured_position`의 핵심 직무를 사용한다. 익명 소개는 제품의 간결성 기준 **20자 이내**로 검증·재작성한다(최초15자에서 주인님의 후속 지시로20자로 조정). 원래 회사 설명·프로젝트명·본문은 변경하지 않는다. 생성한 소개는 `posting_spec.company.public_description`에 확정 출력으로 따로 보관하며 원래 description 사실을 대체하지 않는다. 홈페이지 Company 필드도 이 확정 출력을 재사용해 같은 소개를 두 번 생성하는 경로를 없앤다.

최소 구현 게이트: 제목 생산은 **2단계**에서 멈춤. `_exdigm_company_descriptor`·`_repair_posting_selection`·`_structured_position`을 실제 검색·본문으로 확인했다. 기존의 raw Client.description/빈 값 fallback과 홈페이지의 중복 생성을 대체한다. 일반 유효 제목에 불필요한 제목 재작성은 하지 않는다. 60/70가중치·Unicode·두 번 제목 repair는 유지한다. 제목 source를 Client.description+Project.title로 가정한 검사는 최신 지시로 의도적으로 바뀐 계약을 반영하되, 실제 초과·재검사·중단과 신원/직무 보존 기대값은 유지한다.

실제 입력 검증은 기존 공통 `observe_form`을 재사용하고 HTML 입력란의 maxLength·value.length·patternMismatch를 관찰 항목으로 추가한다. 사이트별 최종 입력 단계가 공통 `verify_posting_title`을 호출하도록 연결한다. 기존 `posting_form_snapshot`은 업데이트 비교용 정규화 값만 반환하며 `observe_form`에는 native 길이/문자 제약이 없고 인크루트/BusinessPeople/PeopleNJob/사람인은 최종 제목의 비어 있음만 보거나 검사 자체가 없음을 직접 확인했다. **공통 검증 함수는 4단계(브라우저·Playwright의 기본 관찰)에서 멈춤**: Python 문자열 검사만으로는 현재 HTML 규칙·JS가 바꾼 입력을 확인할 수 없다. 새 모듈·의존성·러너를 만들지 않고 기존 공통 파일·관찰·스크롤 도구에서 문자 그대로 일치와 native 제한을 검사한다. 역할 증가는 확인한 false positive를 닫고 여섯 사이트의 같은 입력 계약을 한 번 구현하기 위해 필요하며, 같은 수정에서 제목 생산 fallback/중복 AI 책임을 제거한다.

인크루트 제목 입력은 현재 bulk 입력 초반에서 사이트 선택·급여 처리 뒤로 이동하고 기존 `_fill_input`을 재사용한다. 초기 report.len28 이후 최종 값이 빈 증거에 따라 최종 입력과 검사 위치를 맞춘다. 어떤 후속 선택이 값을 지웠는지는 아직 확정하지 않는다. 게시 단계의 시험 표기/최종 재입력·기존 성공 판정은 유지한다.

공식 경로: 공통 익명 소개(캐시 검증/필요시 기존 AI repair) → 핵심 직무 → 기존 사이트 제목 정규화/검사/필요시2회 repair → payload(원본 사실/확정 소개·제목 구별) → `manage.py autoposting` → 각 사이트 기존 입력 → 공통 최종 제목/native 제약 검사 → 기존 dry/게시 계약. 검증은 기존 자동게시 전체·고정 보호·실제 DOM 지움/잘림/native 제약 사례·실제 모델과 활성 사이트별 공식 dry다. 현재 규정이 확인되지 않은 사이트는 추측한 길이값으로 막지 않는다. 비활성/멤버십/보안 조건은 변경하지 않는다.

표준 근거: [MDN maxlength](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/maxlength)는 HTML 길이의 UTF-16 단위와 프로그램 입력만으로 native 길이 검증이 보장되지 않음을 명시한다. [MDN patternMismatch](https://developer.mozilla.org/en-US/docs/Web/API/ValidityState/patternMismatch)는 실제 입력의 문자 패턴 검증 상태를 제공한다. 따라서 관찰된 native 값 길이와 maxLength를 직접 비교하고 문자 패턴 상태도 검사한다.

추가 두 실패를 확인해 준 별도 작업은 이미 보관되어 후속 메시지를 전송하지 못했다. 보관을 해제하거나 그 작업의 파일을 변경하지 않았다. 코드 수정은 이 작업만 담당한다.

### 추가 변경 리뷰 계약

1. 원천: 주인님의 공통 제목 간결화·다른 사이트 제약 검토 지시, 위 추가 구현 잠금, AGENTS 및 적용 스킬. 앞선 제한·실패 안내 배포 c91fd737의 성공 계약은 보호한다.
2. 역할: 원본 회사 소개와 내부 프로젝트 이름을 제목으로 복사하는 공통 생산 원인을 제거하고, 확정 제목이 실제 최종 입력란에 그대로 전달된 것을 공식 엔진에서 확인한다.
3. 경계: base `c91fd73772584619c8b4c27f8b7a83949832ef6b` 대비 공통 도구·여섯 사이트 기존 `_fill`·payload 워크플로 및 기존 payload/실제 DOM 검사 파일. 직접 호출자인 워커와 공통 관찰/선택 도구, payload의 직접 소비자·dry/게시 진입점까지 확인한다.
4. 입력: 원본 회사·공고 사실, 기존 익명 소개 캐시, 정리된 핵심 직무, 사이트별 확정 제목, 현재 보이는 실제 제목 입력란의 값과 native 제약. 원본 사실과 확정 공개 소개는 별도 필드로 구분한다.
5. 절차: 기존 소개 cache 검증/최대3회 repair → 15자 소개+핵심 직무 → 기존 사이트 제목 검사/최대2회 repair → privacy/payload → 기존 엔진 입력 → 공통 최종 값/native 검사 → 기존 dry/게시. 인크루트 제목은 후속 선택 뒤 입력한다. 인증·브라우저·버튼·게시 성공 판정은 기존 경로다.
6. 출력: 짧은 익명 소개와 직무를 담는 제목 및 원본 사실/확정 공개 설명이 분리된 payload, 실제 값/HTML 제한 관찰 증거 또는 설명 가능한 명시적 실패. HTML 제한이 없는 사이트에 임의 제한을 추가하지 않는다.
7. 보호: 원본 회사 설명·프로젝트명·본문·기존 데이터/실패 이력, 정상 유효 제목의 무재작성, 확인된 Unicode/사이트 길이/AI 재검사, 공개 신원 차단, 공유 관찰/행동의 기존 계약, test 표기·게시 완료/공개 검증·dry 비제출, 고정 보호18파일과 다른 미배포 작업.
8. 비목표: 실제 게시·마감·삭제/과거 실패 성공 처리, 사이트 활성/멤버십/인증/보안 변경, 본문 생성·JD·추출·기존 화면5실패·브라우저 런처·새 모듈/의존성.
9. 검증: 수정 전 자동게시527pass/4skip, 같은 전체 검사와 고정 보호216개, 실제 DOM의 지움/잘림/UTF-16/native 길이·패턴·누락/중복 및 인크루트 후속 선택 지움 변형, 실제 제품 AI와 활성 사이트별 공식 dry, 원본10행/관리 오류 백업 대조, catalog/Django/Ruff·전체 diff.

리뷰 관점은 diff와 1차 영향범위다. 질문은 소개 길이/캐시 repair/신원 검사가 함께 유지되는지, 원본 사실이 바뀌지 않는지, 최종 검사와 test 제목이 실제 엔진 입력에 맞는지, 관찰 항목 추가가 기존 선택/행동 계약을 깨지 않는지, 빈값·native 제한 우회가 성공으로 반환되는지다. 실제 조회에서 활성5개/비활성 피플앤잡을 확인했다. 비활성 사이트를 켜거나 로그인하지 않는다.

### 추가 변경 검증·리뷰

수정 후 같은 공식 자동게시 전체 검사: **549 passed, 4 skipped, 35 warnings, 123.16초**. 기존527개와 추가22개를 포함한다. 실제 DOM의 제목 지움/잘림/native 길이·문자 패턴, UTF-16 2단위 글자, 입력란 누락/중복, 인크루트 후속 선택 세 가지 지움 변형을 통과했다. 원본 회사 설명(끝 공백 포함)과 내부 프로젝트명 보존·홈페이지 소개 한 번 생성·긴 소개 캐시 repair/계속 초과 시 중단도 확인했다.

집중 검사 최초 실행은 가상 양식의 제목 입력란 부재 및 새 생산 계약에 맞지 않는 캐시/기대값 때문에 12실패185통과에서 종료했다. 승인된 공통 생산 변경을 반영해 해당 가상 입력을 제공하고 기존 필수값·선택 기대값은 유지했다. 다음 집중 검사 250통과/1실패는 가상 HTML의 invalid pattern 때문에 브라우저가 패턴을 적용하지 않은 사례였다. 유효한 패턴으로 바꾼 뒤 위 전체 검사에서 통과했다. 검사·제품 조건을 완화하거나 공통 제목 검사를 우회하지 않았다.

Django check·Ruff·diff check 통과. catalog_update는 current/local.valid true/broken_references 없음이고 추적 목차 변경 없음이다. 고정 보호18파일은 pin6dd40643과 같고 보호 검사 **216 passed, 7.07초**를 다시 확인했다.

메인 에이전트가 c91fd737 대비 전체10파일 diff와 payload→privacy→여섯 엔진/직접 dry·게시 연결, 공통 관찰→행동/선택 소비자를 단일 code-review 패스로 확인했다. 캐시 변환은 메모리 출력만 변경하며 DB·원본 facts를 쓰지 않는다. 소개 생성/검사·정규화·제목 repair가 한 경로이고 홈페이지가 확정 소개를 재사용한다. 여섯 `_fill`에 최종 값/native 검사가 실제 연결되어 있으며 잡코리아 test 표기는 실제 입력 함수의 동일 결과를 검사한다. 관찰 항목 추가에 따른 상태 비교/기존 선택·행동 검사는 전체 회귀에서 통과했다. **승인된 finding 및 열린 계약 질문 없음**. 실제 사이트의 미확인 서버 제한은 추측하지 않으며 아래 공식 dry 증거로 확인한다.

실제 운영 사례의 기존 producer 호출은 **“화학소재 대기업-설비관리자” 14자**, 익명 회사 소개8자로 생성됐다. 원래109자/회사 설명100자/프로젝트명 “아산공장 설비관리”와 정리된 직무 “설비관리자”를 직접 확인했다. 신규 unsaved 모델로 기존 helper가 검증 파일만 쓰며 운영 DB run/site_run 행은 0개 생성했다. 공통 producer가 확정한 익명 소개는 다른 dry payload의 메모리 캐시로만 재사용한다. 원본 프로젝트 requirements는 저장하지 않는다.

추가 코드 커밋 `d643e9a2dc86d6393dfc607f55831717789f6ce0`은 승인된10파일만 포함한다. 디버깅/운영이 clean임을 확인한 뒤 **2026-09-18 14:55:57 KST** 공식 prod 배포를 시작했고 **14:57:22 KST 성공 종료**했다. origin/main과 운영 checkout에 fast-forward했고 이미지 `exdigm_app:20260918145602`의 DOC/DOCX/PDF 실제 전달 계약은 통과했다. 서비스5개1/1·작업자/지원11 active/jobs0/drainoff·HTTPS200 및 아래 활성5사이트의 공식 dry 증거를 확인했다.

### 주인님 최종 한도 조정 — 15자에서 20자

주인님은 위 15자 기준을 **20자**로 변경하도록 지시했다. 변경 대상은 제목용 익명 회사 소개의 제품 한도 상수와 이에 대한 검사다. 익명 소개+핵심 직무로 제목을 만드는 공식 경로, 확인된 사이트별 한도 및 최종 실제 입력 검사·원본 보존은 유지한다. 최소 구현 게이트는 2단계에서 멈춤: 기존 `AUTO_POSTING_COMPANY_DESCRIPTOR_MAX_CHARS`를 prompt/validator가 공유하므로 이 상수만 변경하고 새 분기·함수·설정을 만들지 않는다.

새 기준선은 clean debug/운영 `d643e9a2` 및 위549/4skip·216pass다. 공식 prod는 **14:57:22 KST 성공 종료**했고 서비스5개1/1·작업자/지원11 active/jobs0/drainoff·HTTPS200이다. 활성5사이트의 최종 실제 제목은 모두 “화학소재 대기업-설비관리자”14자/생성값과 일치이며 submitted=false/published=false다. 현재 native 제약은 인크루트60·비즈니스피플150·사람인60, 홈페이지/잡코리아는 HTML maxlength 미선언이고 pattern은 다섯 곳 모두 미선언이다. 잡코리아70가중/사람인60가중 사전 계약은 별도로 유지한다. 비활성 피플앤잡의 실제 입력/서버 한도는 미확인이다.

공식 조회 전용 최종 대조에서 최초10행 해시가 모두 같고, 새 dry run/site_run DB행은 모두0개다. 우리 NotSupportedError의 상태는 succeeded/처리 이력1개이며 원래 필드는 백업과 같고 처리상태·이력·갱신시각만 달라졌다. 기존 이력서 보완 본문 이메일 링크와 SENT 전달도 보존됐다.

20자 변경은 기존 소개 검사 기준선을 전후 같은 명령으로 확인하고20/21 경계 사례를 추가한다. 해당 producer 소비자를 다시 검사하고 고정 보호·리뷰 후 공식 prod에 반영하며, 같은 기존 payload/공식 인크루트 dry로 최종 입력 보존을 확인한다. 다른 네 사이트의 입력 경로와 제목 값은 바뀌지 않았으므로 위 실제 증거와 변경 범위를 함께 기록한다.

20자 변경의 리뷰 계약은 위9항목을 참조하며 원천은 주인님의 “15 -> 20”, base는 `d643e9a2`, diff 경계는 기존 한도 상수와 기존 소개 검사 두 파일이다. 절차·보호·비목표는 동일하고 출력 조건의 익명 소개 한도만20자로 바꾼다. 확인할 질문은 prompt/validator가 한 상수를 공유하는지,20자는 무재작성·21자는 기존 repair인지, 원본/사이트 한도와 최종 입력 계약이 같은지다. 수정 전 같은 기존 소개 집중 검사는 **4 passed, 176 deselected, 0.58초**다.

20자 조정 후 같은 payload 파일의 producer/직접 소비자 검사는 **182 passed, 52.25초**다. 실제20자·21자 사례를 기존 매개변수 검사에 추가했고20자 무AI/21자 repair 및 계속 초과 시3회 중단을 확인했다. Ruff·diff check·Django check·catalog current/local.valid true 통과. 메인 code-review 단일 패스에서 전체2파일 diff와 상수의 모든 사용처(prompt·validator·오류 설명)를 직접 대조했다. 원본/공개 신원·사이트별 한도·입력 경로는 같고 제품 한도20만 변경됐다. **승인된 finding 및 열린 계약 질문 없음**. 같은 집중 기준선·고정 보호를 확인하고 커밋·공식 반영한다.

같은 집중 기준선 수정 후 **6 passed, 176 deselected, 0.54초**(기존4개와20/21 추가2개), 보호 계약18파일 동일·**216 passed, 6.87초**다. 최종 한도 조정 커밋 `0b237350c87e06e288614ec807b0a6a344fa199e`은 위2파일만 포함하며 **15:06:21 KST** 공식 prod 배포를 시작하고 **15:07:49 KST prod ok/exit0**으로 종료했다. origin/main·운영 checkout은 이 커밋으로 fast-forward했다. 이미지 `exdigm_app:20260918150625`, 이미지 ID `sha256:942ead403ba1527839c29aa5f20a0affbb4ed41dd0915d3e363647d41105b5ef`. DOC/DOCX/PDF 전달 계약·서비스 수렴·11개 작업자/지원 재시작·회수job0을 통과했다.

수정 후 생산 서버/디버깅 HEAD는 모두0b237350이고 운영 main/origin/main·debug detached가 clean이다. 현재 실행중 앱/SSE/알림 컨테이너의 `/app/.source-commit` 및 실제 워크플로 상수를 읽어 세 서비스 모두 동일 커밋/20을 확인했다. 서비스5개1/1, 작업자/지원11 active/jobs0/drainoff, 운영HTTPS200이다. 상태 확인 중 잘못 지정한 브라우저 제어 스크립트 경로는 파일 부재로 거절됐으며 실행·설정 변경은 없었다. 공식 작업자 상태의 auto-posting-browser active 및 기존 공식dry로 실제 동작을 확인했다.

최신20자 코드의 실제 공식 `manage.py autoposting --site incruit --mode dry --payload <기존 신규 검증 payload>`는 **20260918151127 ok/exit0**이다. 공통 `incruit.title.verify`와 최종 `verify.postingTitle` 모두 “화학소재 대기업-설비관리자”14자/생성값과 일치, native_max_length60·pattern미선언·업종/담당자일치다. 실제 `03-after-fill.png`를 별도로 열어 제목14자가 화면에 그대로 있음을 확인했다. submitted=false/published=false다. 다른4사이트에는 입력 경로/검증 함수/확정 제목 변경이 없어 아래 d643e9a2의 공식 증거를 재사용한다.

최종 공식 조회 전용 대조도 DB exdigm·역할exdigm_debug_ro·transaction_read_only on이다. 최초10행의 원본 SHA-256은 모두 같고 새 검증run/site_run 행은0개다. 관리용 NotSupportedError의 status succeeded/처리이력1개와 백업 비교의 변경 필드3개만을 다시 확인했다. 비공개 직접 증거 `.debug/incruit-title-limit-20260918/20-char-title-final-verification.json`(0600)에 현재커밋·20자·최종제목·원본보존을 기록했다.

### 실제 사이트 제약과 검증 증거

| 사이트 | 사전 제목 규칙/현재 입력란 제약 | 공식 dry 증거 | 결과/한계 |
|---|---|---|---|
| 인크루트 | 문자수60, 현재 maxlength60 | 0b237350:20260918151127; d643e9a2:20260918145744 | 제목14자·생성값과 일치, 최종 verify·실제 화면에도 유지, 비제출 |
| 비즈니스피플 | 현재 maxlength150/화면14/150 | 20260918145831 | 제목14자·생성값과 일치, 업종/직무/지역/담당자 필수 확인, 비제출 |
| 잡코리아 | ASCII1·비ASCII2로70단위, 현재 HTML maxlength 미선언 | 20260918145934 | 제목14자·생성값과 일치, 기존 단계/근무지역 확정 확인, 비제출 |
| 사람인 | ASCII1·비ASCII2로60단위, 현재 maxlength60 | 20260918150118 | 제목14자·생성값과 일치, 기존 직무/근무지/필수값 확인, 비제출 |
| 엑스다임 홈페이지 | 현재 HTML maxlength 미선언 | 20260918145808 | Company8자·Position14자 정확히 일치, 비제출. 서버 최대길이는 미확인 |
| 피플앤잡 | 비활성; 현재 실제 길이 미확인 | 공통 생성/문자/값/native 회귀 및 연결 코드 검토 | 설정·멤버십을 변경하지 않아 실제 폼/게시 검증은 미실시 |

현재 다섯 사이트 제목 입력란에는 pattern 속성이 없었다. NFC/유니코드 공백 정규화와 확인된 공통 안전 문자 검사, 기존 AI 엄격 재검사는 유지한다. 실제 HTML 제약은 매 실행마다 공통 관찰로 읽고 최종 문자 그대로 일치/native 길이·패턴을 검사하므로 과거의 문서 숫자만 믿지 않는다. HTML maxlength 미선언을 서버 무제한이라는 뜻으로 해석하지 않는다. 실제 게시를 실행하지 않았으므로 서버 최종 수락·공개 공고 생성은 검증 범위에 포함하지 않는다.

## GBrain 장기 계약 기록

기존 정본의 본문/타입을 확인한 뒤 최신 계약을 기록했다. `incident/exdigm-auto-posting-title-normalization-contract`의 capture는 현재 active schema pack gbrain-base-v2가 page type incident를 허용하지 않아 거절했다. 실제 저장되지 않았음을 get으로 확인했고 오류 원문도 확인했다. 같은 incident 타입의 사람인 문서도 임의로 타입을 바꾸거나 스키마를 수정하지 않고 보존했다.

현재 지원하는 note 타입으로 `project/exdigm-auto-posting-concise-title-contract-20260918`을 새로 기록했다. 여기서 오래된 긴 회사 설명/내부 프로젝트명 생산 방식 및15자 기준을 대체하는20자/공통 최종 입력 계약을 명시한다. 기존 `project/exdigm-auto-posting-process-policy`는 지원되는 project 타입을 유지하며 이 현행 계약과 현재 운영/검증 증거를 앞부분에 연결했고, `incident/exdigm-incruit-summary-resume-missing-work-20260918`는 기존 note 타입/본문을 보존하며 관리용NotSupportedError 성공후속1개와 원본보존을 추가했다. 세 페이지 모두 capture 영수증 이후 별도 get으로 새 본문을 다시 읽어 저장을 확인했다. GBrain 스키마·CLI 업그레이드·개인 기억 폴더는 변경하지 않았다.

## 최종 재개 정보

- 요청한 공통 제목 간결화, 익명 소개20자 조정, 여섯 사이트의 제약/최종 실제 값 검사 연결, 활성5사이트 공식 비제출 입력 확인, 원본 보존 및 기록은 완료했다. 주인님이 추가로 하실 조치는 없다.
- 운영/개발 최종 코드0b237350, 운영 main/origin/main 및 개발 detached clean. 미배포 마크다운4ac2f0b는 기존 보존 ref에 남기고 이번 배포에 포함하지 않는다. 코드 수정/검사/메인리뷰·커밋/공식배포·현재실행확인을 각각 확인했다.
- 과거4게시실패는 그대로 failed이고 실제 게시재시도·마감·삭제는 미실행이다. 위 성공은 짧은 제목의 생성·실제 입력 전달 확인이며 공개 게시 성공이 아니다.
- 후속 실게시 수락/공개 공고 생성 확인은 실제 게시가 승인될 때 기존 공식 경로로 진행한다. 피플앤잡의 현재 실제 폼/서버 제한 확인은 해당 사이트를 사용하는 별도 범위에서 진행해야 한다. 미확인 값을 추측하거나 사이트 설정·멤버십·인증을 변경하지 않았다.
- 다른 controlroom 작업의 수정/신규 파일은 보존하고 이 문서와 README의 본 작업 링크만 별도 커밋한다. 현재 상태 정본은 첫 최종 결과 절과 이 재개 정보다.
