# 다른 자동게시 사이트의 제한된 판단 연결

## 최종 운영 반영 — 2026-09-17 00:02 KST 확인

- 주인님의 “다른 수정 사항도 포함해서 커밋, 푸시, 운영 배포해”와 “Hermes 개선도 함께 적용” 지시를 완료했다. 최종 통합 커밋은 `de20cf82c962a3acc140e669a67cd350886e9eb2`다. 아래 이전 미배포·대기 기록은 당시의 이력이다.
- 포함: 자동게시 3개 사이트의 제한 판단, 공통 테스트 준비, 오류 한국 시간·처리 이력, 추천 명단의 비이력서 제외, Hermes의 제한된 조회·단일 MCP 출력. 원본 별도 worktree 커밋 `8084524b`·`836eb5a6`는 그대로 보존했다.
- 공식 앱 배포는 23:52:04 KST `prod ok de20cf82`, 공식 Hermes 배포는 23:53:33 KST 기록과 exit 0을 확인했다. 앱을 먼저 반영하고 직원 6명을 같은 커밋으로 재생성했다.
- GitHub main·운영 clean main·개발 clean detached HEAD·앱/SSE/알림 수집 컨테이너·Hermes 공통 MCP·관리 서비스가 모두 같은 커밋이다. 앱 image는 `exdigm_app:20260916235044`, SHA-256은 `8bfc2cfcd5e9ccb9004927e3675b5fd5e0bbfb52094be03616948ed58bf7ba50`다.
- 검증: 최종 통합 733 passed/4 skipped, 직원 비서 관련 204 passed, 보호 216 passed, Django/Ruff/catalog/diff 통과. 배포 이미지의 실제 DOC/DOCX/PDF 전달 계약도 통과했다. 기존 skip 조건과 18개 보호 목록·검사·기대값은 유지했다.
- 운영 확인: HTTPS 200, Exdigm 5개 Swarm 서비스 각 1/1, 운영 작업자/지원 11개 active·jobs 0·drain off·쓰기 모드 read-write, Hermes 6개 active/healthy와 manager/gateway 정상.
- 6개 직원 각각의 실제 SDK stdio → 운영 HTTPS API 6개 확인을 통과했다. 세 도구 등록, 목차·schema·집계·없는 기록 텍스트 범위·비밀 필드 거절·OpenAPI를 확인했고 매 응답은 structuredContent 중복 없이 text block 하나다. 시험 Telegram 메시지나 업무 쓰기를 실행하지 않았다.
- 원래 오류 49건·게시 실행 147건·사이트 실행 314건·게시 연결 175건의 이전 필드 해시가 같다. 원본 명단과 정상 이력서 FileData 전체 행·연결·큐도 같다. 기존 오류에 처리 성공을 쓰거나 실패 게시/추출을 재실행하지 않았다.
- Hermes 보존: 과거 메시지 2,976개와 세션 기본 정보 26개, cron 가변 파일을 제외한 11,408개 파일 내용, 인증값이 유지됐다. bundled manifest 1개의 소유권만 root→10000으로 정상화됐음을 원래 그룹 해시 재구성으로 확인했다. cron heartbeat·SQLite 임시 파일은 가동 중 갱신되므로 불변 파일에서 구분한다. 관리형 설정은 재생성 대상이며 모델 3.8 Flash·100,000토큰 압축·daily 4시 설정과 새 SOUL/업무 스킬을 확인했다.
- 승인된 활성 Telegram 세션 1개는 종료됐고 다음 메시지부터 새 세션이다. 과거 본문은 그대로다. 일일 리셋 시간대 보완·개인 메일 인증값 교체·운영 대화 전체 재현은 이번 배포 범위가 아니다.
- 2026-09-17 00:02 KST 재확인에서 새 운영 오류 0건, 기존 오류 49건의 처리 기록 없음 상태, 보호 데이터·운영 커밋·서비스 정상 상태를 유지했다.
- GBrain의 배포/Hermes/운영 오류/명단 사건 정본 4개에 검증 기준 갱신, 별도 detached 작업 보존·통합, 새 재사용 계약과 운영 적용 경계를 기록하고 기존 본문 보존·재조회까지 확인했다. 재개는 이 문서의 최종 판본을 기준으로 현재 서버 상태부터 대조한다.
- 문서 동기화 경계: Exdigm 문서 6개만 별도 로컬 커밋한다. controlroom에는 다른 프로젝트의 서버 통합 지침·카드·계획이 아직 수정 중이며 공식 `controlroom push`가 전체 변경을 자동 stage하므로 실행하지 않는다. Exdigm 앱 소스 GitHub push·운영 배포는 완료 상태다. 다른 작업이 정리된 뒤 공식 문서 동기화를 재개한다.

## 별도 검증 worktree의 미배포 변경 포함 — 2026-09-16

- 기본 worktree의 자동게시·오류 기록 변경은 `6dd40643`으로 23:36:07 KST 공식 배포 완료했다. 아래 합동 배포 기록은 첫 적용분이다. 운영 오류 49건/게시 실행 147건/사이트 실행 314건/게시 연결 175건의 원래 필드 해시가 유지됐고 새 오류 0건, 새 기록 필드·Asia/Seoul·0069 적용을 확인했다.
- “다른 수정 사항도 포함” 지시의 누락을 막기 위해 등록된 모든 worktree를 대조했다. 별도 clean detached 커밋 `8084524b`(비이력서 명단 제외 3파일)와 `836eb5a6`(Hermes MCP 조회 개선 7파일)가 아직 운영에 없었다. 두 원본 worktree는 보존하고 기본 debug에 검증된 차이만 통합한다.
- 추가 원천·목적·계약은 `resume-roster-error-20260916.md`와 `hermes-mcp-read-improvement-20260916.md`다. 명단은 상세 추출 전에 제외하고, 실제 이력서의 D8/신원/저장 경로는 유지한다. Hermes는 세 도구·직원별 읽기 권한·오류/건수/부분 결과 표시를 보존하면서 응답 크기와 중복 전달을 제한한다.
- 변경 경로: 기존 text_to_pipeline_result → 기존 문서 판정 보완 → 기존 D8 또는 비이력서 큐 종료, 기존 Hermes skill → code_query/orm_read/exdigm_request → 동일 JSON의 단일 MCP 텍스트. 새 실행기·모델·업무별 API는 없다.
- 최소 구현 게이트: 2단계에서 멈춤. 검증된 두 커밋과 기존 배포 경로를 재사용한다. `git worktree list`, 각 원본 status/show, 코드 목차의 데이터 추출/Hermes 주제 및 직접 diff로 경로를 확인했다.
- 수정 전 기준선: 현재 `6dd40643`의 관련 174개 중 173 passed/1 failed. 기존 notification catalog의 hint 수가 10개 계약을 초과했다. Hermes 커밋이 같은 연결을 aliases/inspect에 보존하며 이를 정리하지만, 이번 오류 기록의 새 연결 2개도 함께 남겨야 한다. 기대값을 늘리지 않는다.
- 추가 보호: 이미 배포된 한국 시간/처리 이력 및 자동게시 변경, 기존 후보자/이력서/실패 FileData, 직원별 인증·장기 기억·개인 workspace/skill·과거 메시지. 보호 pin `6dd40643`의 18파일을 변경하지 않는다. 운영 데이터 소급 재분류·추출 재실행·시험 메시지 발송·모델/리셋 시계 변경은 제외한다.
- 추가 검증: 관련 전체 테스트와 기존 보호 테스트, 원본 커밋과 통합 파일 대조, 실제 SDK 결과 및 운영 코드/필드/서비스 확인. Hermes 전 직원 재생성은 활성 대화를 새 세션으로 시작하는 효과를 설명했고, 주인님이 “Hermes 개선도 함께 적용”으로 승인했다. 기억·개인 파일·과거 메시지를 보존하며 앱 배포 후 직원 6명에게 같은 판본을 적용한다.
- 통합 수정: notification의 두 변경을 하나의 목록으로 합친다. 중복 안내는 aliases/inspect로 옮기고 오류 처리 명령 파일은 직접 hint로 유지해 기존 10개 상한을 지킨다. 기존 검색 검사에 오류 처리 이름·함수·검사·명령 파일의 조회 회귀만 추가한다. 기존 검사나 기대값은 줄이지 않는다.

### 추가 통합 리뷰 계약

1. 원천: 최신 전체 변경 배포 지시, Hermes 직원 6명 적용 승인, 위 두 개별 계획과 현재 공식 코드.
2. 역할: 문서 종류 판단을 상세 이력서 추출 앞에 연결하고 Hermes의 직원 권한 내 조회 결과를 작고 정확하게 전달한다.
3. 경계: base `6dd40643` 대비 10개 통합 파일과 직접 호출자·소비자. 두 원본 worktree와 이미 배포한 변경을 보존한다.
4. 입력: 원래 파일 본문·기존 분류 근거, 인증된 직원·허용된 ORM 조회/텍스트 범위, 기존 코드 목차 질문.
5. 절차: 모호한 기본 문서만 기존 모델로 판정한 뒤 D8 또는 기존 비이력서 종료 경로로 연결한다. 조회는 기존 권한 검사 후 한정된 페이지/문자 범위를 단일 MCP 텍스트로 전달한다.
6. 출력: 근거 있는 이력서/비이력서 또는 명시적인 판정 실패, 누락을 숨기지 않는 조회 건수·다음 위치·오류, 찾을 수 있는 모든 기존 안내 이름.
7. 보호: 한국 시간·오류 이력·자동게시, 진짜 이력서의 신원/필드/D8 저장, 권한·비밀값 차단, 보호 검사 18파일, 직원 기억·파일·과거 메시지.
8. 비목표: 운영 문서 재추출/소급 분류, 실게시, 시험 메시지 발송, 모델/리셋 시각 변경, DB 인프라·DNS·서버 이전.
9. 근거: 전체 diff와 직접 호출자 두 관점, 관련 회귀·보호 검사, 기존 실제 모델/SDK 증거와 통합 코드 동일성, 배포 전후 운영 데이터·프로필·실행 판본 대조.

### 추가 통합 검증과 최종 리뷰

- 집중 검사 282개, Hermes 배포·온보딩·권한·조회 검사 204개, 보호 검사 216개가 통과했다.
- 최종 통합 검사는 733 passed/4 skipped, 157.67초다. 자동게시 전체·오류 기록·ORM 권한·비이력서 분류·실시간 소비자·실제 명단/정상 이력서 응답 저장 재검사 5개를 포함한다. 기존 실제 모델 조건부 skip 4개를 유지했다.
- Django check, Ruff, diff check, catalog current/valid 및 깨진 참조 없음. 보호 pin `6dd40643`의 18개 파일은 그대로다.
- 원본 roster 3파일과 Hermes 제품 코드 4파일은 원본 검증 커밋과 byte 단위로 같음을 Git diff로 확인했다. 통합 차이는 기존 오류 조회 검사 보존과 목차의 두 변경 병합·검색 회귀 추가다.
- 전체 10파일 diff 및 직접 소비자(실시간 updater/저장, 직원 권한 ORM, MCP 등록/SDK 소비, 코드 목차)를 재리뷰했다. 승인된 추가 finding 및 열린 제품 계약 질문은 없다.
- 운영 읽기 전용 비교 기준: 원본 명단과 정상 이력서의 FileData 전체 행 해시·Resume/후보자 연결·큐 상태를 잠갔다. 명단은 Resume 0/후보자 연결 없음/큐 닫힘을 유지한다.
- Hermes 재생성 전 기준: 직원 6명, 관리형 exdigm-work를 제외한 기억·workspace·개인 skill·home·plan·cron·hook 및 auth 파일 11,438개, 메시지 2,976개, 세션 26개의 본문/기본 정보 해시를 잠갔다. 환경값은 값 대신 해시로 비교하고 모든 .env 권한은 0600이다. 활성 Telegram 세션 1개의 종료는 승인된 재생성 효과이며 과거 본문은 보존 대상이다.

## 합동 운영 배포 승인과 기준선 — 2026-09-16

- 최신 지시: 주인님이 “다른 수정 사항도 포함해서 커밋, 푸시, 운영 배포해”로 기존 운영 오류 한국 시간·처리 결과 변경을 포함한 Exdigm 합동 배포를 승인했다. 아래의 이전 미배포 기록은 당시 증거다.
- 잠근 시작 상태: 운영 clean main `87683dae`, 개발 detached `884b0874`와 운영 오류 변경 10개 경로. 다른 프로젝트의 서버 통합 작업과 controlroom 미완료 변경은 포함하지 않는다.
- 원하는 결과: 검증된 두 묶음을 한 최종 커밋으로 연결하고 공식 `scripts/deploy/deploy.sh prod`에서 GitHub·운영 코드·앱·작업자에 같은 판본을 반영한다.
- 보호 조건: 원래 오류 id/발생 순간/본문·게시 원문/공고번호/실행 이력·조회 권한·dry-only·일반 알림을 유지한다. 기존 실패를 재실행하거나 처리 성공 이력을 임의로 쓰지 않는다. DB 인프라·Hermes fleet·DNS는 바꾸지 않는다.
- 검증: 두 묶음의 전체 직접 검사, 18개 보호 파일 차이 대조와 기존 보호 테스트, Django/migration/catalog/diff, 이미지 실파일 계약, 배포 전후 읽기 전용 데이터 확인, 서비스/작업자/HTTPS/실행 커밋 대조.
- 보호 기준 `fb177d6a`와 달라진 파일은 승인된 `tests/conftest.py`의 테스트 DB flush 및 기본 업무 유형 준비 보완이다. 검사기·보호 목록·기대값을 우회하지 않는다. 주인님이 “기존 검사와 합격 기준은 그대로 유지하고, 검증이 끝난 테스트 환경 수정본을 정상 기준으로 등록한 뒤 커밋·푸시·운영 배포”에 “그렇게 진행해”로 명시 승인했다. 이전 pin/ref를 보존하고 검증된 최종 커밋으로 갱신한다.
- 최소 구현 게이트: 2단계에서 멈춤 — 기존 검증·기록·배포 진입점을 재사용한다. 검색 근거는 `scripts/debug_workspace.sh`의 test/contracts/shell-readonly와 `scripts/deploy/deploy.sh`의 prod, 기존 오류 관리 서비스·명령이다. 새 배포 도구나 제품 실행 경로는 만들지 않는다.

### 합동 리뷰 계약

1. 원천: 최신 합동 배포 지시, 이 계획과 `operational-error-time-results-20260916.md`의 승인된 동작, 현재 공식 코드.
2. 역할: 사이트별 구조화된 선택과 오류 조사 장부를 기존 워커/조회 흐름에 통합한다.
3. 경계: 운영 base `87683dae`부터 개발 후보 전체 22개 파일과 직접 호출자·DB/ORM 소비자. 별도 프로젝트·Hermes 실행기 개선은 제외한다.
4. 입력: 승인 payload·현재 옵션·시간대 있는 오류/처리 시각·기존 오류 UUID·관리자 읽기 권한.
5. 절차: 고정 사이트 단계 안의 제한 판단/반영 확인, 원래 오류 수집 번호 보존, 행 잠금 안의 처리 이력 추가, 공식 커밋→검사→prod.
6. 출력: 확인된 선택 또는 명시적 실패, 동일 순간의 한국 시간 및 확인된 처리 이력, 모든 운영 소비자의 같은 판본.
7. 보호: 위 보호 조건과 기존 성공 테스트·검사기/보호 목록·비밀값/외부 효과 경계.
8. 비목표: 실게시/재시도·과거 오류 성공 소급·권한 확대·테스트 삭제·운영 소스 직접 편집·다른 서비스 이전.
9. 근거: 전체 diff 및 직접 소비자 두 관점, 기존 519개/70개 검사와 공식 dry 증거, 합동 재검사와 배포 후 직접 대조. 시간대가 기존 수집 번호를 바꾸는지와 공통 테스트 정리가 다른 보호 테스트에 영향을 주는지 집중 확인한다.

### 합동 검증과 리뷰 결과

- 최초 합동 검사: 589 passed/4 skipped/1 setup error. 이전 오류 관리 작업에도 있던 `action_type_submit`의 기본 데이터 누락을 동일하게 재현했다. 공통 테스트 준비의 승인 범위에서 기존 migration `0002_seed_actiontypes.ACTION_TYPES`의 해당 한 행만 get_or_create로 재사용했다. 제품 코드와 권한 검사 기대값은 변경하지 않았다.
- 수정 후 오류 기록·일반 알림·ORM 권한 71개 검사를 두 번 통과했다(6.58초/6.46초). 최종 합동 검사는 590 passed/4 skipped, 153.16초다. 4 skip은 기존 명시적 실제 모델 실행 조건이며 새 skip을 추가하지 않았다.
- 보호 테스트 216개와 배포 검사기 12개, 총 228개를 수정 전후 통과했다. 보호 파일은 18개 중 테스트 준비 파일 하나만 변경됐고 나머지 17개 및 manifest/검사기/기대값은 동일하다.
- Django check, migration drift 없음, 변경 파일 Ruff/diff 검사, catalog current/valid 및 broken reference 없음. 전체 diff/직접 소비자 재리뷰에서 승인된 추가 finding과 열린 제품 계약 질문은 없다. 기존 테스트 준비 오류 수정은 위 승인 범위 안의 보완이며 새 제품 결함으로 분류하지 않았다.
- 운영 읽기 전용 기준선: `exdigm_debug_ro`/transaction_read_only on. cutoff `2026-09-16T23:30:26.644948+09:00` 이전의 오류 49건, 게시 실행 147건, 사이트 실행 314건, 게시 연결 175건의 원래 전체 필드 해시를 잠갔다. 새 처리 필드를 제외하고 시각을 같은 UTC 표현으로 정규화해 배포 후 비교한다.

## 승인과 보호 범위

2026-09-16 주인님이 점검 보고의 세 단계(공통 테스트 정리 → 비즈니스피플 선택 완료 확인 → 인크루트·사람인 제한된 AI 판단 연결)를 승인했다. 개발·검증·리뷰·커밋 범위이며 운영 배포와 외부 저장·게시·마감·삭제는 포함하지 않는다.

- 조정실: Windows `C:/Users/chaconne/projects/exdigm`; 계획 정본은 이 문서다.
- 코드: SSH `chaconne@49.247.202.197`, `/home/chaconne/exdigm-debug`, detached HEAD.
- 시작 기준: 운영과 개발 모두 clean `87683dae905f56676bb6f0d4b9916c2d86bcfb0d`.
- 기존 잡코리아, 인증·공유 브라우저, 공고번호/마감 checkpoint, 익명화, 개발 dry-only, 운영 기록, 별도 이력서·Hermes 작업을 보존한다.
- Exdigm 홈페이지와 비활성 PeopleNJob의 절차·설정은 바꾸지 않는다.

## 실행과 판단의 경계

- 스크립트: 사이트별 순서, 화면 준비, 정확한 현재 선택지 수집, 허용된 선택 실행, 반영값 검사, 시간/호출 제한, 실패 증거.
- LLM: 원본 사실과 현재 선택지의 의미 대응. 이전 선택값은 참고이며, 사이트에 현재 존재하는 후보만 반환한다.
- 명백한 현재 선택은 그대로 실행하고, 현재 후보와 불일치하거나 의미 판단이 필요한 지점에서만 AI를 호출한다.
- 게시·결제·인증 우회·다른 단계 조작 권한은 AI에 주지 않는다. 필수 선택/반영 확인이 없으면 다음 단계로 진행하지 않는다.
- 기존 `autoposting → AutoPosting.main → common → sites/<site>.run` 경로만 사용한다. 새 런타임 모듈·실행기·의존성을 만들지 않는다.

## 단계와 기준선

1. 공통 테스트 환경과 오래된 테스트 조건을 정리한다.
   - 현재 실패: 확장 검사 312 pass/15 fail/1 teardown error. 후속 검사 217 pass/71 setup error.
   - 2026-09-16 재현: `test_scheduled_enqueue_runs_only_after_transaction_commit`이 남은 boss_u 중복과 보존된 옛 테이블의 FK 때문에 setup/teardown 모두 실패했다.
   - `accounts.0023/0024`는 운영 기록 보존을 위해 모델 상태만 제거한다. 운영 테이블은 지우지 않는다.
   - 테스트 프로세스의 정리만 Django 표준 flush의 cascade로 처리한다. 기존 운영 계약에 맞게 fixture를 준비하고, 의도적으로 바뀐 post/repost/dry 계약을 검사한다. 검사 자체를 삭제하거나 성공 기준을 느슨하게 하지 않는다.
   - 최소 구현 게이트 4단계: 공통 conftest에 DB 정리 보완이 없음을 검색했고, 설치된 Django의 표준 flush가 연결된 테이블 정리를 지원한다.
2. 비즈니스피플부터 한 사이트씩 수정한다.
   - 기존 업종/직무 부분일치 선택과 클릭 여부만의 완료 판정을 제거한다.
   - 실제 후보 선택과 최종 반영값 확인을 같은 단계에 연결한다.
3. 인크루트 지역/업종과 사람인 직무의 기존 검증을 유지하며 현재 선택지 재판단을 연결한다.
   - 공통 부분은 기존 common/workflow에 통합하고 각 사이트가 절차와 소유 범위를 정한다.

## 검증과 종료 조건

- 공식 `scripts/debug_workspace.sh test`의 격리 DB/공용 잠금을 사용한다. 같은 관련 묶음을 두 번 실행해 임시 데이터 잔류를 확인한다.
- 정상값, 현재 선택지 변경, 모호한 후보, 없는 후보, 잘못된 모델 응답, 반영 실패, 범위 밖 선택을 검증한다.
- 사이트별 공식 `manage.py autoposting --mode dry` 결과와 증거를 확인한다. 저장 없는 실제 입력 검증과 격리 테스트의 결과를 구분한다.
- 기존 인증 dry 승인 경계를 확인한 뒤 동일 공식 경로만 사용한다. 비밀값 복사/출력, 운영 DB 쓰기, 실게시·마감은 하지 않는다.
- 잡코리아 및 직접 소비자 회귀, Django 검사, 코드 카탈로그 갱신, diff 검사, code-review-loop를 완료한다.
- 실제 경로 검증이 막히면 완료·커밋으로 포장하지 않고 남은 범위와 재개 조건을 적는다.

## 재개 정보

- 상태: 세 사이트 구현·실제 dry·전체 회귀·최종 리뷰 완료. 원격 개발 커밋 `884b0874`.
- 기본 묶음은 396개 검사를 두 번 연속 통과했다. 전체 자동게시 검사에서는 513 pass/5 fail/4 skip을 확인했고, 남은 5개는 오래된 생성/상태 문구/사이트 설정 준비 계약으로 확인해 정정했다. 해당 모델·알림 검사 22개가 통과했다.
- 실제 dry: 비즈니스피플 `20260916211847`(커밋 후 최종 재확인), 인크루트 `20260916211225`(중복 라벨 결합 수정 후), 사람인 `20260916210205`. 각 결과는 원격 개발 worktree의 `runtime/auto_posting/evidence/<site>/<run_id>/result.json`에 있다.
- 기본 DB와 인증 DB 모두 `exdigm_debug_ro`, `transaction_read_only=on`, dry-only=true, publish=false를 매 실행 확인했다. 운영 인증값을 복사하지 않고 기존 승인된 프로세스 참조 예외를 사용했다.
- 합성 실제 모델 검사 `test_constrained_live_model_matches_human_region_name`이 통과했다. 비공개 코드표 없이 원문 `충남 천안시`와 현재 옵션 `충청남도`를 연결했다. 운영 모델/운영 배포 완료의 증거는 아니다.
- 작업 도중 별도 운영 오류 관리 변경이 같은 worktree에 생겼다. `common/operational_errors.py`, `projects/services/operational_errors.py`, 관련 모델/설정/명령/마이그레이션/검사와 카탈로그 변경은 이번 작업 소유가 아니다. 보존하고 자동게시 변경만 커밋한다.
- 최종 전체 검사: `scripts/debug_workspace.sh test tests/test_auto_posting*.py --tb=short`에서 519 pass/4 skip을 두 번 연속 확인했다(140.60초, 135.27초). 4 skip은 명시 실행 조건을 가진 합성 실제 모델 검사이며, 이번 신규 실제 모델 검사는 별도로 1 pass다.
- Django check, 변경 파일 Ruff, diff check, code_knowledge catalog_update(current/valid, broken reference 없음)를 통과했다. 카탈로그의 기존 다른 작업 변경은 이번 커밋에 포함하지 않았다.
- GBrain `project/exdigm-auto-posting-process-policy`에 재사용 결정만 추가하고, 기존 본문 보존 및 새 절 조회를 재확인했다.
- 운영 체크아웃은 clean `87683dae`를 유지한다. 코드 push/운영 배포/실게시/마감 없음.
- 문서 동기화 보류: 같은 controlroom 저장소에 별도 운영 오류 관리 문서 변경이 남아 있고, `controlroom push`는 `git add -A`로 이를 함께 커밋한다. 다른 문서를 임의로 포함하거나 숨기지 않는다. 이번 문서는 별도 커밋으로 보존하고, 병행 문서 작업이 정리된 뒤 공식 `controlroom push`로 동기화한다.
- 다음 행동: 별도 운영 배포 승인 시 현재 worktree 전체 상태와 병행 작업의 검증·커밋을 다시 확인하고 공식 배포 경로를 사용한다. 비게시 입력 검증은 실게시 성공의 증거가 아니다.

## 리뷰 계약과 확인 기록

- 원천: 위 세 단계 사용자 승인, `auto-posting`, `agent-script-role`, SSP 및 변경 무결성 지침.
- 목적·역할: 워커의 사실/준비값을 사이트별 절차로 입력하며, 불일치한 선택만 모델에 맡긴다. 사이트별 절차를 모델이 바꾸지 못하게 한다.
- 경계: base `87683dae` 대비 common의 추가 선택 도구, 세 사이트의 업종/직무/지역 선택, workflow의 제한된 선택 판단, 공통 test cleanup 및 직접 소비 테스트. 별도 운영 오류 관리 변경은 제외한다.
- 입력: 워커가 만든 승인 공고 payload, 현재 단계가 수집한 선택지, 준비된 라벨. 준비 라벨은 힌트이며 원문 사실을 대체하지 않는다.
- 절차: 준비 마커 → 현재 선택지 판정 → 같은 관측 항목 실행 → 다음 마커 → 실제 반영값 검증. 일치한 힌트는 추가 모델 호출 없이 진행하고, 불일치 판단 응답은 오류 피드백 후 1회만 재요청한다.
- 출력: 현재 옵션에 연결된 선택과 확인된 반영값 또는 명시적 실패. 모델의 성공 선언만으로 다음 단계나 게시로 넘어가지 않는다.
- 보호: 잡코리아 경로, 사이트 인증/공유 브라우저 수명, dry-only, 공고번호/마감 상태 보존, 익명화, 운영 데이터/실게시 권한.
- 비목표: 홈페이지·비활성 피플앤잡 변경, 전국 코드표 추가, 임의 브라우저 행동 에이전트, 생산 배포, 고객 데이터 변경.
- 검증: 공식 dry 결과, 격리된 실제 브라우저의 선택지 변경/지연/반영 실패, 범위 밖 모델 응답·오류 재요청 상한, 중복 라벨, 워커/알림/모드·공고 이력 검사.

### 리뷰 1 — 승인된 결함과 수정

- 위반 계약: 모델이 고른 현재 항목과 실행할 항목이 같아야 한다.
- 변경 원인: 인크루트의 새 선택 판단은 항목 참조를 반환하지만, 기존 `_select_industry` 실행부는 라벨만 다시 검색했다.
- 도달 상태: 같은 이름의 상세 업종이 둘이고 모델이 두 번째 항목을 선택하는 현재 목록.
- 인과: 참조 제거 → 라벨의 첫 항목 클릭 → 다른 전송값 → 라벨 검증만으로 성공 가능.
- 직접 재현: `test_incruit_live_industry_waits_for_new_child_options[duplicate]`에서 원하는 `second-child` 대신 `new-child`가 선택되며 실패했다.
- 범위·수정: 직접 소비자인 같은 업종 단계만 수정했다. 현재 화면에서 읽은 전송 식별자와 라벨을 함께 실행·검증하며, 정적 코드표는 추가하지 않았다.
- 수정 후: 중복 라벨과 정상/변경/지연 선택 관련 4개 검사가 통과했다. 같은 범위의 전체 diff 재리뷰가 남아 있다.

### 리뷰 2 — 종료

- 같은 사용자 목적, base `87683dae`, 승인 경계로 갱신된 전체 diff와 직접 호출자를 재검토했다.
- 승인된 추가 finding 및 열린 계약 질문 없음. 최소 구현은 기존 common/workflow 도구 재사용이며 런타임 파일·의존성·별도 실행기 추가 없음.
- 스크립트가 사이트별 순서·선택 범위·최종 반영을 통제하고, 모델은 현재 선택지의 의미 대응만 수행한다. 범위 밖 응답·재질문 상한·실제 반영 실패·정상 무추가호출을 검사했다.
- 변경한 사이트의 최종 경로 검증과 잡코리아를 포함한 자동게시 회귀 검사가 통과했다. 보안 챌린지·필수 사실 부족·새 절차 필요 등 모든 미지 변칙의 자동 해결을 보장하지 않는다.
