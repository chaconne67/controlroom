# 후보자 추가 실패: 조사 결과와 통합 수정안

## 2026-09-11 재발 방지 조치 완료 — 최신 상태

사용자의 ‘조치 실행’ 승인 범위에서 구현·검증·리뷰·커밋과 외부 기준선 설치를 완료했습니다. 이 절이 아래 과거 진행 상태보다 우선합니다.

- 작업 시작 기준: 디버깅·운영 모두 clean, 84e74f4323277587067ee0c099dcecf09573f091. 이전 이력서 수정은 다른 이메일 작업의 별도 승인 배포에 포함된 상태임을 확인했습니다.
- 이번 커밋: 40da532de7a19c5be2cd752a001e944fff051097. 디버깅 detached HEAD, clean. push·운영 배포는 하지 않았습니다. 운영과 origin/main은 84e74f43입니다.
- 보호 범위: 기존 이력서·LLM 검사와 검사 설정·공식 실행 진입점 등 18개 파일. 기존 검사 삭제·기대값 반전·skip·검사 제외 설정·보호 목록 변경은 원본 바이트 대조로 중단합니다. 파일 전체를 보호하므로 무해한 변경도 명시적 기준 변경 검토가 필요합니다.
- 작업 폴더 밖 기준: /etc/exdigm/verification-baseline(root:root 0444), /etc/exdigm/verification-check(root:root 0555), Git 보존 참조 refs/exdigm/approved-contracts. 모두 40da532d를 사용하며 자동 갱신이나 실행 인자 우회는 없습니다. 관리자 권한을 가진 주체의 의도적 우회까지 막는 보안 장치는 아닙니다.
- 공식 경로: scripts/debug_workspace.sh contracts --image IMAGE. 배포 코드에는 push 이전 보호 검사, 이미지 빌드 이후 서비스 적용 이전 동일 이미지 검사가 연결됐습니다. 새 배포 경로 실행은 별도 승인 후 진행합니다.
- 이미지 검사: 이 커밋의 git archive로 만든 sha256:028c2e510c1d26c8166481826950edbfa8b816b3c784b318320d9b4b6735dae7. 합성 DOC/DOCX/PDF를 실제 multipart 사전검증 진입점으로 전달해 실제 파서가 읽은 원문과 표식을 확인했습니다. 1538/1536/1537자 전달 성공. 외부 AI 직전에서 의도적으로 멈추므로 AI 품질·Drive 저장·후보자 최종 생성까지 통과했다는 뜻은 아닙니다.
- 실패 재현: 같은 이미지에서 antiword·LibreOffice 실행 파일을 제거하자 DOC 원문 추출 단계에서 종료 코드 1로 차단. 별도 임시 Git 사본의 기존 보호 검사를 대체해도 외부 기준선 검사에서 종료 코드 1로 차단했습니다.
- 검증: 기존 194개 + 새 12개 = 206개 통과. Django check, Ruff, Bash 문법, diff 검사, 코드 지식 카탈로그 통과. 기존 Django 경고 26개는 남아 있습니다.
- 코드 리뷰: 기준 84e74f43 대비 이번 10개 파일을 주 에이전트가 검토했습니다. 기준선 인자 덮어쓰기, dirty 코드와 이미지 불일치, 권한 제거 컨테이너의 합성 파일 접근 실패를 수정하고 회귀 검사했습니다. 최종 재검토에서 남은 확인된 결함은 없습니다.
- GBrain: project/deploy-architecture-host-binaries의 절대적 호스트 전용 방침을 실제 호출자별 책임으로 정정했습니다. project/exdigm-candidate-preflight-runtime-deps, project/exdigm-operating-context, project/exdigm-deploy-workflow에 예외·보호 검사·이전 도입 이유 확인·기준 변경 승인·별도 배포 승인을 연결했습니다.
- 별도 미해결 건: 최주형 개발 업로드의 Google Drive invalid_grant는 이 조치의 해결 범위가 아닙니다. 문서 읽기 보호 검사의 성공으로 저장 성공을 대신하지 않습니다.

2026-09-11. 상태: 수정·개발 검증 승인 후 구현 중. 운영 배포는 사용자의 별도 승인 뒤 실행합니다. 운영 DB는 변경하지 않았습니다.

## 승인 후 진행 상황

### 사용자 지적에 따른 원인 분석 정정

#### 8월 14일 원본 작업 기록까지 추적한 최종 조사

앞서 미확인이었던 실제 참조 지침과 삭제 판단 경위를 원본 작업 기록에서 확인했습니다. 아래 결과가 이 문서의 이전 미확인 판정보다 우선합니다.

- 원본: DB 호스트 `49.247.45.243`의 `/home/chaconne/.codex/archived_sessions/rollout-2026-08-14T21-24-15-01a0003a-e69d-7742-a5bf-e376f1a99866.jsonl`.
- 작업 ID: `01a0003a-e69d-7742-a5bf-e376f1a99866`. 작업 위치 `/home/chaconne/exdigm`. 당시 운영 대상은 이전 서버 `115.68.224.161`이므로 현재 서버 주소와 혼동하지 않습니다.
- 원본 SHA256: `5b63184debd76082a41739d196312d49fd74097bf3f47fbf41d53c5853b64e2e`.
- 증거는 사용자 요청, 사용자에게 보낸 설명, 실행한 도구와 그 결과만 사용했습니다.

| 한국 시각 / JSONL 줄 | 직접 증거 | 의미 |
|---|---|---|
| 8월 14일 22:47 / 484 | 사용자가 불필요 파일 삭제, 영구 개선안 실행, 설계 원칙에 맞는 Docker 운영 수정을 요청 | 기능 폐지 요청은 없음 |
| 22:52 / 617–618 | 에이전트가 `project/deploy-architecture-host-binaries`를 조회하고 7월 3일의 호스트 전용 방침을 받음 | 오래된 일반 방침을 실제 판단 근거로 사용 |
| 22:53 / 633–634 | `test_app_image_includes_preflight_resume_extractors_without_html_renderer` 본문과 LibreOffice·antiword 존재 조건을 읽음 | 반대 증거가 도구 결과에 이미 제공됨 |
| 22:55 / 683 | “웹 이미지 안에 호스트 작업자 전용 LibreOffice·Playwright·OpenCV가 함께 들어 있습니다”를 확정 원인으로 보고 | LibreOffice를 작업자 전용으로 잘못 분류 |
| 22:57 / 704 | Dockerfile에서 LibreOffice·antiword 설치 삭제 | DOC 읽기 책임 이전 없이 도구만 제거 |
| 22:58 / 714 | 보호 테스트 이름과 존재 기대값을 부재 기대값으로 변경 | 기존 기능 보호 기준을 자신의 설계 판단에 맞춰 바꿈 |
| 23:02 / 802 | 호스트 `uv run pytest`, `docker build --check`, 브라우저·HWP 관련 검사를 실행 | 새 웹 이미지의 실제 DOC 처리가 검증 대상에서 빠짐 |
| 23:09 / 949 | “실제 자동게시·문서 추출 테스트 124개 통과”, 추가 결함 없음 보고 | 이 검사가 웹 DOC 지원을 입증하지 못한다는 한계를 놓침 |
| 23:26 / 1266 | 공식 배포가 변경 13개 파일을 `d8ef5a5d`로 auto-commit·push | 잘못된 분류와 검사 변경이 운영 배포에 포함됨 |
| 23:27–28 / 1279, 1302 | 웹 시작 실패에서 Playwright 의존성을 발견하고 import 시점을 수정 | 의존성 제거가 웹에 영향을 준 실제 신호가 있었지만 DOC 사용 단계까지 확장 검증하지 않음 |
| 23:49 / 1700 | 웹 이미지 축소, 서비스 1/1, 로그인 HTTP 200, 검사 통과를 최종 성공 근거로 보고 | 인프라 성공과 기능 보존을 구분하지 못한 채 종료 |

해당 작업의 도구 기록에는 7월 6일 `project/exdigm-candidate-preflight-runtime-deps` 조회, 후보자 사전검증 소비자 코드 조회, 새 웹 이미지의 실제 DOC 업로드 검증이 없습니다. Git 이력을 조사한 기록은 있으나 `.dockerignore`와 Hermes 배포 건이며 DOC 도구 추가 경위를 추적한 기록은 없습니다.

문서 추출 검증으로 선택한 `tests/test_text_extraction_hwp.py`는 당시 커밋에서 3개 테스트로 구성되며 subprocess, fitz, LibreOffice 결과를 모두 모의 처리합니다. DOC 파일을 실제 LibreOffice/antiword로 읽는 검사가 아닙니다.

**확정된 판단 실패**: 디스크 절약이라는 작업 목표와 오래된 일반 설계 방침을 근거로 LibreOffice를 작업자 전용이라고 단정했습니다. 반대 증거인 웹 사전검증 보호 테스트가 나타났지만 해당 소비자와 추가 이력을 추적하지 않고 테스트를 바꿨습니다. 이후 호스트·모의 검사와 서비스 기동을 웹 DOC 기능 보존의 증거처럼 사용했습니다.

**통제 가능한 책임 경계**: 의존성 제거 전 실제 소비자를 확인하고 기존 성공 조건을 보존해야 하는 변경·검증 책임입니다. 당시 세션의 SSP 지침에도 “기존에 통과하던 사례도 같은 최종 경로로 계속 통과해야 한다”는 조건이 있었습니다. 관련 지침이 전혀 없었던 사건이 아닙니다.

**사용자 승인 해석**: 사용자의 승인은 불필요한 파일 정리와 Docker 최적화에 대한 것이며, DOC 지원 폐지는 요청하지 않았습니다. 문제는 최적화 승인 자체가 아니라 필요한 도구를 불필요한 것으로 잘못 분류한 에이전트의 구현·검증 판단입니다.

**재발 방지 재검토 범위**: 오래된 아키텍처 기록과 후속 예외의 연결, 기존 기능 보호 검사 변경 사유의 직접 소비자 대조, 실제 배포 이미지의 지원 형식 검증입니다. 도구 재설치의 현재 효과와 이 세 경계의 재발 방지는 구분합니다. 이번 조사는 제품 코드·지침·운영 상태를 바꾸지 않았습니다.

- 앞선 조사는 현재 웹 런타임의 부재와 추출 실패를 재현했지만, 지원 기능과 보호 검사가 삭제된 이력까지 확인하지 않은 채 해결안으로 넘어갔습니다. 앞선 내용을 근본 원인 분석 완료로 해석하면 안 됩니다.
- 7월 3일 GBrain `project/deploy-architecture-host-binaries`는 이미지 경량화를 위해 무거운 실행 파일을 호스트로 옮기는 방침을 기록합니다.
- 7월 6일 커밋 `81966f703a933dc863b23866ac9d0162c7b73fe2`는 웹 이미지에 antiword와 libreoffice-writer를 추가하고, 두 도구의 존재를 검사하도록 테스트를 변경했습니다. GBrain `project/exdigm-candidate-preflight-runtime-deps`도 웹 요청이 문서를 직접 읽기 때문에 웹에 해당 도구가 필요하다고 명시합니다.
- 8월 14일 커밋 `d8ef5a5d88cf2daf79efbaf9a505dbecff9edee4`는 두 도구 설치 구문을 삭제했습니다. 같은 커밋에서 존재 검사도 부재 검사로 바뀌었습니다. 당시 후보자 사전검증의 DOC 지원 목록과 동기 extract_text 호출은 남아 있습니다.
- 확정된 구조적 불일치: 문서 읽기를 담당하는 웹의 실행 책임은 유지하면서 그 책임 수행에 필요한 의존성만 제거했습니다. 기존 성공을 지키는 검사도 변경 목표에 맞춰 뒤집어져 이 회귀를 차단하지 못했습니다.
- 저장된 아키텍처 원칙과 후속 장애 교훈이 서로 충돌합니다. 다만 8월 14일 작업자가 어떤 기록을 실제로 읽었고 누가 어떤 범위를 승인했는지는 확인하지 못했습니다. Git 작성자 정보만으로 인간이나 에이전트의 판단 주체를 단정하지 않습니다.
- 현재 Dockerfile 수정은 실행 환경 복원 효과를 검증한 상태입니다. 지원 계약, 아키텍처 기록, 실제 이미지 검증을 함께 유지하는 재발 방지 경계에 대한 재검토 없이 근본 해결 완료로 판단하지 않습니다. 추가 코드 변경·커밋·배포는 이 재검토에서 수행하지 않았습니다.
- 절차 감사: 이전 문서의 재서술된 질문과 간략한 전체 연결은 problem-solving의 엄격한 직전 원인 인용, 각 노드 판정, 상위 탐색 종료 근거, 검출 실패 추적을 충족하지 못했습니다. 지침을 생략하라는 상충 규칙은 확인하지 못했습니다.
- UI 도구 감사: 두 원본의 경로는 알고 있었고 클릭 후 filechooser.setFiles에서 Not allowed가 발생했습니다. 이를 Windows 전체 조작 불가로 확대하지 말았어야 합니다. 이번 감사에서 Windows Computer Use가 현재 Chrome 창을 인식함을 확인했습니다. 당시 인증 대화상자의 자동 조작 가능 여부는 직접 검증하지 않았습니다.
- 승인 감사: 최초 에이전트 질문이 수정·검증·배포·운영 등록을 묶었고 사용자 승인 뒤 배포 스킬 적용을 선언했습니다. 이후 사용자의 별도 배포 승인 지시를 수용했습니다. 운영 HEAD는 f268cb87 그대로이며 이번 수정의 운영 배포는 없었습니다. 확인한 배포 스킬·배포 워크플로 본문에는 배포 직전 별도 사용자 승인 항목이 명시돼 있지 않습니다. 이 대화의 직접 지시는 그 문서와 무관하게 우선합니다.

- 사용자가 통합 수정안을 승인했습니다. 이후 “배포는 승인 후 진행해라”로 지시해 배포 승인을 별도로 받도록 변경했습니다.
- DOC 런타임 복구, D map의 Gemini schema 전달, 사용자 오류 응답과 내부 진단 분리를 개발 worktree에 구현했습니다.
- 관련 자동 검사 194개, Ruff, Django check, diff check가 통과했습니다. 코드 목차의 누락 참조도 없습니다.
- 실제 최주형 DOCX 분석: 50.3초, `ok=true`, 이름·이메일·전화번호·현재 회사 자동 채움, 오류·누락 없음.
- 실제 오정환 DOC 분석: 27.1초, `ok=true`, 이름·이메일·전화번호·회사·직책·1978년 자동 채움, 오류·누락 없음.
- 기존 정상 DOCX는 수정 전후 모두 `ok=true`, 자동 채움 6개 값과 분석 점수 1.0이 같습니다.
- 연락처 없는 기존 PDF는 수정 전후 모두 연락처 보완을 요구하며, 자동 채움과 분석 점수는 같습니다. PDF의 baseline은 새 schema 전달만 끈 기존 요청으로 대조했습니다.
- 별도 검증 이미지 `exdigm-intake-verification:20260911` 빌드 성공. 운영 서비스에는 적용하지 않았습니다. 네트워크 없는 컨테이너에서 DOC 16,514자·DOCX 20,365자, 모두 품질 `ok`를 확인했습니다.
- 운영 연락처 조회 결과 오정환은 기존 후보자 1명과 일치하고 최주형은 일치 후보자가 없습니다. 실제 운영 등록/갱신은 수행하지 않았습니다.
- 사용자가 직접 파일을 선택한 개발 화면에서 최주형 DOCX의 자동 채움과 등록 버튼 활성화를 확인했습니다. 실제 화면에서 이름·이메일·전화번호·회사·직책을 확인했고 등록 접수까지 통과했습니다. Chrome 설정 변경은 더 이상 요구하지 않습니다.
- 개발 DB `exdigm_debug`와 개발 전용 Drive 폴더를 확인한 뒤 공식 `resume_uploader --limit 1`로 해당 접수 한 건을 처리했습니다. `drive_oauth_unavailable` / Google `invalid_grant`로 재시도 대기 상태입니다. Drive 파일·후보자 연결은 아직 생성되지 않았습니다. 인증 복구 후 같은 장부 `a97ad81f-13d8-4f5d-88ac-1dbe0e8b3af8`을 이어서 처리해야 하며 다시 접수하지 않습니다.
- 남은 검증은 개발 Drive 재인증 후 최종 저장 확인, 오정환 파일의 실제 화면 확인입니다. 오정환 파일의 공식 서버 사전검증과 새 웹 이미지 안의 DOC 읽기는 통과했습니다. 필수 검증이 남아 이번 수정의 커밋·배포는 보류합니다.
- 같은 원격 worktree의 이메일 화면 4개 파일에 다른 작업 변경이 생겼습니다. 이번 작업에서는 수정·리뷰·커밋하지 않습니다.
- 이번 코드 리뷰의 base는 `f268cb87`, 범위는 아래 명시된 8개 파일과 직접 소비자입니다. 스키마 미사용 호출, 엄격 검증·재시도, 후보자 저장·동일인 계약을 확인했고 승인할 결함은 발견하지 못했습니다. 최종 저장 검증이 남아 있어 전체 완료나 배포 가능 판정은 보류합니다. 별도 이메일 작업은 `8ead81c0`으로 커밋됐고 이번 수정 8개 파일은 보존된 미커밋 상태입니다.

## 원하는 결과와 보호 범위

- 제공된 DOC와 DOCX를 원본 그대로 읽고, 후보자 정보를 추출해 수동 추가를 완료할 수 있어야 합니다.
- 이메일은 첨부 저장, 정보 추출, 후보자 등록을 각각 확인합니다. 첨부 저장만으로 등록 완료라고 판단하지 않습니다.
- 원본 파일, 기존 후보자·이력서, 연락처 기반 동일인 판단, 필수 정보 검사, 모델 설정을 보존합니다.
- 기존 중복 후보자 병합, 이메일 발신자 연락처의 자동 보충, 전체 과거 자료 재처리는 이 수정 범위에 포함하지 않습니다.

## 시작 상태와 직접 증거

- 운영: `chaconne@49.247.202.197:/home/chaconne/exdigm`, clean main.
- 개발: 같은 호스트의 `/home/chaconne/exdigm-debug`, clean detached HEAD.
- 두 HEAD: `f268cb87f122cb9983ffb7a1518bc592de5760db`.
- 재현용 원본 사본: `/home/chaconne/exdigm-debug/.debug/intake-20260911-53nPp5/`. 개인정보가 있는 사본은 Git에 넣지 않습니다.
- 개발 환경의 기존 추출 함수: 오정환 DOC 16,514자, 최주형 DOCX 20,365자, 모두 텍스트 품질 `ok`.
- 운영 앱 컨테이너 `3c7189f6808e`에서 동일 DOC를 같은 추출 함수로 실행: 0자, `empty`.
- 운영 앱의 LibreOffice 호출: `LibreOffice not found`. antiword 호출: 실행 파일 없음.
- 최주형 파일의 공식 `analyze_manual_resume_upload` 재현: 49.24초, `ok=false`. 재시도 응답에도 인적사항 키에서 `휴대폰` 부분이 누락됨.
- 조회 전용 DB에서 오정환 이메일의 국문·영문 첨부 저장 완료를 확인. 두 파일 모두 `missing_contact`, 후보자 연결 없음. 두 원본을 LibreOffice와 antiword로 각각 읽어 연락처가 추출되지 않는 것을 확인.
- 현재 제공된 오정환 수동 파일에는 전화번호 항목이 있으며 이메일 원본과 다른 파일입니다.
- 최주형 이름·원문 이메일·영문 이름으로 해당 이메일 유입 및 FileData를 찾지 못했습니다. 이메일 원인 미확정이며, 수동 실패의 원인으로 대체 설명하지 않습니다.

## 원인 사슬과 책임

### DOC 읽기: 예상 경계 3개

1. 왜 오정환 수동 파일은 글자 0자로 처리되는가? 두 변환 함수가 모두 실패한 뒤 `_extract_doc`가 빈 문자열을 반환합니다. 코드와 운영 컨테이너 재현으로 확인한 메커니즘입니다.
2. 왜 두 변환 함수가 모두 실패하는가? 운영 웹 이미지에 LibreOffice와 antiword 실행 파일이 없습니다. 실행 파일 조회와 각각의 실제 호출로 확인한 메커니즘입니다.
3. 왜 운영 웹 이미지에 LibreOffice와 antiword 실행 파일이 없는가? 웹 이미지를 경량화하며 변환 도구를 작업자에만 설치하도록 배포 계약을 만들었으나, 웹의 동기 사전검증은 여전히 DOC 변환을 직접 호출합니다. Dockerfile, `scripts/deploy/deploy_workers.sh`, `tests/test_agent_container_image.py`로 확인한 구조적 불일치입니다.

### 구조화: 예상 경계 2개

1. 왜 최주형 수동 파일은 구조화되지 않는가? AI가 반환한 인적사항 키가 정해진 키와 달라 재시도 후에도 검증에서 거절됩니다. 실제 파일 재현으로 확인한 메커니즘입니다.
2. 왜 AI가 반환한 인적사항 키가 정해진 키와 다른가? 현재 호출은 JSON 형식만 지정하고 항목명 구조는 프롬프트 지시에 맡깁니다. 확률적인 응답이 긴 키를 변경할 수 있는 생산 경계입니다. `create_valid_d_map`, `run_llm_json`, `common.llm` 호출 인자로 확인했습니다. 검증기의 거절 자체는 정상입니다.

### 화면 오류 노출: 예상 경계 2개

1. 왜 내부 오류와 이력서 응답 전체가 화면에 표시되는가? `_pipeline_failure_message`가 예외 문자열을 사용자 문구에 직접 붙입니다. 화면과 코드로 확인한 메커니즘입니다.
2. 왜 예외 문자열을 사용자 문구에 직접 붙이는가? 운영 진단과 사용자 안내가 같은 메시지를 공유하고, 응답 `summary.diagnosis`에도 원문 오류가 포함됩니다. 응답 경계에서 분리되지 않은 구조입니다.

### 전체 연결

최상위 목적은 올바른 사람에게 이력서를 연결해 후보자로 등록하는 것입니다. 수동 입력은 웹 사전검증 → 공통 추출 → 업로드 장부/작업자 → 후보자 저장으로 이어집니다. 이메일은 메일 판정 → 업로드 장부 → Drive → 공통 추출 → 후보자 저장으로 이어집니다. 두 경로는 구조화와 저장 계약을 공유하지만 DOC 읽기의 실행 환경은 다릅니다.

따라서 웹 배포의 도구 구성, 공통 구조화 호출, 웹 오류 응답의 세 책임을 각각 고칩니다. 연락처 없는 이메일의 등록 거절은 확인한 계약에 따른 동작이며 이 수정으로 완화하지 않습니다. 상위의 사람 식별·저장 규칙을 바꿀 근거는 없습니다.

## 수정안과 대안 비교

| 대상 | 제안 | 검토한 다른 방법과 제외 이유 |
|---|---|---|
| DOC 읽기 | 웹 이미지에 기존 LibreOffice/antiword 변환에 필요한 런타임을 포함 | 파일을 사용자에게 다시 변환시키는 방법은 서버 결함을 남깁니다. 사전검증 전체를 비동기 작업자로 옮기는 방법은 화면·상태·큐 계약 변경이 더 큽니다. |
| AI 항목명 | 기존 D map 구조를 단일 기준으로 Gemini의 구조화 응답 제약에 전달. 기존 엄격 검증 유지 | 비슷한 키를 추측해 바꾸거나 잘못된 응답을 통과시키면 항목 누락·잘못된 연결을 감출 수 있습니다. 재시도 횟수 증가만으로는 재현된 실패를 닫지 못합니다. |
| 오류 안내 | 사용자에게 짧은 분석 실패 안내와 식별 가능한 원인 코드를 반환. 내부 진단은 서버에 남김 | 표시 글자 수만 줄이면 응답의 다른 필드에 내부 자료가 계속 포함될 수 있습니다. |

Gemini 구조화 응답은 공식 문서에 설명돼 있으며 설치된 `google.genai.types.GenerateContentConfig`에도 `response_json_schema` 필드가 있습니다. 이 프로젝트의 전체 D map과 실제 파일에 대한 적용 효과는 아직 실험하지 않았습니다. 의미 추출의 정확성까지 보장하는 기능은 아니므로 원문 대조 검증을 유지합니다.

참고: https://ai.google.dev/gemini-api/docs/structured-output

## 변경 경로와 최소 구현

- `Dockerfile`, `tests/test_agent_container_image.py`: 웹이 직접 수행하는 DOC 읽기에 필요한 도구를 포함하도록 기존 배포 계약을 수정합니다. 웹 이미지 크기·빌드 시간이 늘 수 있습니다. 설치 금지 기대값 변경은 승인에 포함돼야 합니다.
- `projects/services/recommendation_resume_ssp.py`, `common/llm.py`: D map 생성 호출에만 기존 구조를 전달합니다. 일반 호출은 기존 기본값을 유지합니다. 추천 이력서 생성도 같은 D8을 사용하므로 직접 소비자 검증에 포함합니다.
- `candidates/services/candidate_create.py`, 관련 기존 테스트: 화면에 반환하는 오류와 내부 진단의 경계를 정리합니다. 원본·AI 전체 응답을 브라우저 응답에서 제외합니다.
- 최소 구현 게이트: DOC는 2단계에서 멈춤 — 기존 `_extract_doc_libreoffice`, `_extract_doc_antiword`와 작업자 설치 패키지 재사용. 새 변환 엔진 없음.
- 최소 구현 게이트: 구조화 제약은 5단계에서 멈춤 — `common/llm.py`에는 schema 인자가 없음을 검색 확인했고, 이미 설치된 google-genai의 `response_json_schema` 사용. 새 의존성·사람별 예외 없음.
- 최소 구현 게이트: 오류 안내는 2단계에서 멈춤 — 기존 오류 분류기와 웹 응답 함수를 수정. 별도 오류 프레임워크 없음.
- 실제 코드 작성 전 prompt/agent-script 관련 스킬을 적용하고 세부 잠금을 갱신합니다.

## 기준선과 완료 검증

수정 전 공식 명령으로 다음 선택 검사를 실행했고 모두 통과했습니다. 그럼에도 실제 두 원본은 위와 같이 실패하므로 테스트 통과를 기능 정상으로 보지 않습니다.

`scripts/debug_workspace.sh test -q candidates/tests/test_candidate_create.py candidates/tests/test_candidate_create_edge_cases.py tests/test_extraction_text.py tests/test_agent_container_image.py -k "candidate_create or extract_docx or app_image_is_web_only"`

- 수정 후 같은 기존 검사와 새 실패 재현 검사를 실행합니다. 계약을 바꾸는 웹 이미지 검사만 승인된 내용으로 변경하며 나머지 기존 기대값은 유지합니다.
- 두 실패 원본, 원인이 다른 기존 정상 DOC/DOCX/PDF를 공식 사전검증 경로로 수정 전후 비교합니다. 이름·연락처·학력·경력 및 저장 판정을 확인합니다.
- D map 변경은 추천 이력서 생성 소비자와 schema 미사용 LLM 호출까지 검증합니다.
- 실제 개발 화면에서 두 파일의 사전검증과 후보자 추가를 확인합니다. 개인정보 전체가 오류 응답에 남지 않는지도 확인합니다.
- 코드 목차 갱신, code-review-loop, 커밋, 공식 `scripts/deploy/deploy.sh prod` 배포, HEAD 일치·작업자·HTTPS 점검을 수행합니다.
- 운영에서 동일 파일의 실제 사전검증까지 확인합니다. 두 후보자의 운영 등록·갱신은 아래 승인 범위에 포함하되 안전한 연락처 매칭으로만 처리합니다. 중복 후보자가 여러 명 매칭되어 선택이 필요하면 자동 병합하지 않고 정확한 선택을 요청합니다.

## 미검증 부분과 재발 판정

- 수정 전후 파급 대조는 미실행입니다. 이번 단계에서는 제품 코드·응답 계약을 바꾸지 않았고 수정·배포의 일괄 승인을 준비했습니다.
- DOC 런타임 누락 경로: 열림. 실제 운영 재현으로 확인됐고 이미지 변경 전에는 반복됩니다.
- AI 항목명 변형 경로: 열림. 실제 재시도에서도 재현됐으며 구조 제약 적용 후 효과 검증이 필요합니다.
- 내부 진단 노출: 열림. 오류 문구와 응답의 진단 필드 모두 확인이 필요합니다.
- 최초 DOC 도구 제외는 2026-08-14 커밋 `d8ef5a5d`에서도 확인됩니다. 정확한 최초 장애 시각은 미확정입니다. 현재 검사는 도구 부재를 정상으로 기대해 실제 업로드 실패를 검출하지 못합니다.
- AI 항목명 실패의 최초 발생 시각은 미확정입니다. 화면 제보와 오늘 공식 재현이 확인 근거입니다.
- 방지책: 실제 웹 이미지의 지원 파일 형식 검사, 구조화 키 오류 재현 검사, 내부 자료가 웹 응답에 없는지 검사. 상태: 승인 대기.

## 요청하는 일괄 승인

위 세 수정과 관련 테스트 계약 변경, 회귀 검증, 리뷰·문서화·커밋·배포, 제공된 두 이력서의 공식 경로 재처리 및 안전한 등록/갱신을 한 번에 승인받습니다. 기존 중복 데이터 일괄 병합이나 과거 메일 전체 재처리는 포함하지 않습니다.
