# Agent Client 프로토타입 기본기획 — Windows 설치형

작성일: 2026-09-23 · 근거: [통합 에이전트 서비스 구상 v0.2](통합-에이전트-서비스-구상-2026-09-23.md) 13절 1~2단계

## 1. 목적

Windows PC에 설치해 쓰는 데스크톱 앱으로 다음 두 가지를 확인한다.

1. **디자인**: 실제 대화에서도 "예쁘고 오래 읽어도 편하다"가 느껴지는가
2. **메모리**: 다음 작업에서 "그 얘기를 다시 안 해도 되네"를 경험하는가

요금제·커뮤니티·일반 사용자용 관리형 서비스·모바일은 이번 범위가 아니다.

## 2. 기술 선택

| 부분 | 선택 | 이유 |
|---|---|---|
| 화면 | 기존 시제품(메인서버 `rndlog/telegram_miniapp/frontend`)의 Vite + JavaScript 코드 재사용 | Markdown 정제·코드 강조·말풍선 구조·스트리밍 수신이 이미 있음 |
| 앱 포장 | Tauri 2, Windows 설치 파일(NSIS `.exe`·`.msi`) | Windows 내장 WebView2 사용으로 가벼움. 이후 macOS·모바일로 같은 방식 확장 |
| 에이전트 | 메인서버 기본 Hermes API (`/v1/chat/completions`, SSE 스트리밍) | 미니앱이 이미 같은 API로 동작 중 |
| 메모리 | Hermes에 이미 연결된 GBrain(스킬·CLI 경유)을 그대로 사용 | 새 메모리 시스템을 만들지 않음 |
| 접속 | SSH 터널 `localhost:8642 → main 172.30.31.128:8642` | 공개 주소를 열지 않음 |
| API 키 | 앱의 Rust 쪽이 Windows 자격 증명 관리자에 보관·사용 | 화면 코드와 설정 파일에 키가 남지 않음 |

Electron은 추가 설치가 필요 없지만 설치 크기가 커서 "가벼움" 기준에 맞지 않아 제외했다.

## 3. 개발 환경 (2026-09-23 확인)

- 있음: Node 24, npm 11, WebView2
- 설치 필요: Rust(rustup), Visual Studio 2022 Build Tools의 C++ 데스크톱 작업 항목

## 4. 만드는 순서

1. 개발 도구 설치
2. 빈 앱 설치 확인: 기존 시제품 화면을 Tauri로 감싸 설치 파일을 만들고, 이 PC에 설치해 실행
3. 대화 화면: 답변 전체 너비, 요청 약 3분의 2 폭 말풍선, `#FDFDFD / #202020 / #339CFF`, 읽기·코딩·야간 프리셋과 자간·행간 조절
4. Hermes 연결: 앱 내부에서 대화 전송·실시간 수신, 이전 대화 이어쓰기
5. 메모리 표시: 답변에 쓰인 기억을 펼쳐 보기. **Hermes API가 GBrain 조회 과정을 화면에 넘겨주는지 미검증**이므로 이 단계 첫 작업으로 확인
6. 실사용: 며칠간 실제 업무에 사용하고 불편한 점 기록

## 5. 만들지 않는 것

- 가입·로그인·결제·요금제, QR 초대·기기 해제
- 모바일·macOS 빌드, 앱스토어 제출
- 적응형 표현 AI
- 기존 Telegram 미니앱·RNDLOG·Hermes·GBrain 설정 변경

## 6. 코드 위치와 공유

- 코드: 조정실 `agent-client/app` (Windows PC 로컬)
- controlroom `.gitignore`가 `agent-client/docs`만 공유하므로 app 코드는 현재 Git 공유 대상이 아니다. 공유가 필요해지면 `.gitignore` 변경을 별도로 결정한다.
- 원본 미니앱 코드는 복사만 하며 원본은 수정하지 않는다.

## 7. 진행 기록

- 2026-09-23: 기획 승인. 1단계부터 진행.
- 2026-09-23: 1~2단계 완료.
  - 설치한 개발 도구: Rust 1.98.1, VS 2022 Build Tools(C++)
  - 기존 시제품 코드를 `agent-client/app`에 복사했다. 바꾼 것은 세 가지다: 독립 `index.html`, `dist` 출력으로 바꾼 `vite.config.js`, Tauri 2 뼈대(`src-tauri`, 식별자 `local.agentclient.prototype`).
  - 테스트: 복사 직후와 변경 후 모두 15/15 통과.
  - `npm run tauri build`로 NSIS 설치 파일(약 4.8MB)과 MSI 설치 파일(약 5.8MB)을 생성했다.
  - 설치 후 실행 확인: 한글 입력, 데모 응답 스트리밍, Markdown 표·코드 글꼴 표시.
  - 주의: Claude 데스크톱 앱 안에서 실행한 설치는 `AppData` 쓰기가 앱 패키지 저장소로 옮겨질 수 있다. 실제 사용 설치는 바탕화면의 설치 파일을 직접 실행한다.
  - 다음: 3단계 대화 화면 다듬기, 4단계 Hermes 연결(SSH 터널 + 자격 증명 관리자).
- 2026-09-23: 3~4단계 구현.
  - 화면
    - 상단 줄: 연결 상태, 새 대화, 설정
    - 사용자 말풍선 최대 66.7%
    - 입력창 포커스: 둥근 테두리 강조로 통일
    - 전송 버튼: 대비를 맞춘 진한 파랑 `#0B6FD6`
  - 설정 패널: 오른쪽에 열리고, 대화가 옆에 그대로 보여 변경 즉시 미리보기
    - 프리셋 3가지
      - 읽기: 17px / 1.70 / -0.012em / 820px
      - 코딩: 15px / 1.60 / 0 / 1040px
      - 야간: 어둡게, 나눔고딕코딩, 17px / 1.75 / -0.004em
    - 본문 글꼴: 함초롬돋움·맑은 고딕·나눔바른고딕·나눔고딕코딩
    - 글자 크기·행간·자간·본문 폭·테마 조절, 기본값 복원
  - 연결
    - 앱이 `ssh -L 127.0.0.1:8642:172.30.31.128:8642`를 창 없이 실행하고, 앱 종료 시 닫는다.
    - API 키는 Windows 자격 증명 관리자(`agent-client`)에만 저장한다. 화면 코드로는 SSE 본문만 전달한다.
    - 키가 없으면 "연결 전 · 예시 응답", 키가 있으면 실패를 실제 오류로 표시한다.
    - 매 전송 전에 터널을 확인해, 끊겼으면 다시 연다.
  - Telegram 전용 코드와 그 테스트 4개를 삭제했다. 전송 경로·새 대화·화면 설정 테스트 6개를 추가해 15/15 통과.
  - 실제 화면 확인
    - 예시 대화와 세 프리셋의 즉시 반영
    - 가짜 키로 터널 개통 → "API 키가 맞지 않습니다" 표시, 전송 시 같은 오류(예시 응답으로 대체하지 않음)
    - 키 삭제 후 "연결 전" 복귀
    - 앱 종료 시 ssh 종료
  - 코드 리뷰: 터널이 끊긴 뒤 재연결하지 않는 결함 1건을 수정했다. 재리뷰에서 finding 없음.
  - **미검증**: 실제 키로 Hermes 답변 스트리밍. 사용자가 설정 화면에서 키를 직접 입력한 뒤 확인한다.
  - 다음: 실제 대화 확인 → 5단계 메모리 표시(Hermes가 GBrain 조회 과정을 스트림으로 주는지 먼저 확인).
- 2026-09-23: 앱 아이콘 선택 기능.
  - 후보 6개: 둥근 말풍선(기본)·반짝임·생각 구름·이어지는 대화·기억·밝은 말풍선
    - 모양 출처: Lucide(ISC)·Tabler Icons(MIT). 라이선스 전문은 `app/THIRD_PARTY_NOTICES.md`
    - 바탕: 둥근 사각형 `#0B6FD6`. 밝은 말풍선만 흰 바탕
  - 설정 → 앱 아이콘(3×2, 이름 표시)에서 고르면 창·작업표시줄 아이콘이 즉시 바뀌고, 다음 실행에도 유지된다.
    - 방식: Tauri `setIcon`, 권한 `core:window:allow-set-icon`
  - 설치 파일·바탕화면·시작 메뉴 아이콘은 설치 때 고정되므로 기본값 1번으로 생성했다(`app/app-icon.svg` → `tauri icon`).
  - 확인: 테스트 15/15 통과. 실제 앱에서 기억 → 생각 구름 → 둥근 말풍선으로 바뀌는 것, 재실행 후 유지되는 것을 확인했다.
- 2026-09-23: 로컬 Hermes 연결과 모델 설정 — 작동하는 프로토타입.
  - 배경: 공식 Hermes Desktop(Electron)은 무겁고 범용이라 복잡해서 사용자가 쓰지 않는다. 이 앱은 설정 없이 바로 쓰는 쪽을 목표로 한다.
  - 로컬 Hermes: `%LOCALAPPDATA%\hermes`(HERMES_HOME), v0.21.4, Windows 직접 설치, 게이트웨이는 예약 작업 `Hermes_Gateway`.
    - `.env`에 `API_SERVER_KEY`(무작위)·`API_SERVER_HOST=127.0.0.1`·`API_SERVER_PORT=8642`를 추가하고 게이트웨이를 재시작했다.
    - 백업: `.env.before-agent-client-20260923-184136`
  - 연결 방식
    - 기본값 "이 PC의 Hermes": 앱이 로컬 Hermes `.env`에서 키를 직접 읽는다. 사용자는 키를 입력하지 않는다.
    - API 서버가 꺼져 있거나 Hermes가 멈춰 있으면 설정에 "Hermes 연결 켜기" 버튼이 나온다.
      - 동작: 키가 없으면 `.env`에 추가하고 `hermes gateway restart`를 실행한다. 실제로 약 50초 걸린다.
    - "메인서버": 기존 SSH 터널 방식. 로컬 Hermes와 겹치지 않게 터널 포트를 18642로 옮겼다.
  - 모델 설정
    - Hermes `/api/model/options`에서 로그인된 인증 방식과 모델 목록을 불러와 두 개의 선택 상자로 보여준다.
    - 기본값: `openai-codex`(ChatGPT·Codex 구독) + `gpt-6-sol`
    - 요청마다 `model`·`provider`를 보낸다.
  - 도구 사용 표시: Hermes SSE의 `event: hermes.tool.progress`를 받아 대기 줄에 "N초 · 도구 사용 중: …"을 표시한다.
    - 5단계 메모리 표시에도 이 이벤트를 쓸 수 있다.
  - 실제 확인
    - 날짜·폴더 질문에 도구 실행 표시 후 정답
    - 이어진 질문이 앞 대화를 기억함
    - 모델을 `gpt-6-luna`로 바꾸자 Hermes 로그에서 `model=gpt-6-luna provider=openai-codex`로 처리됨. 이후 `gpt-6-sol`로 되돌림
    - 게이트웨이를 멈춘 뒤 "Hermes 연결 켜기"로 복구되고 앱이 자동 재연결됨
    - 앱을 닫아도 게이트웨이는 계속 실행됨
  - 테스트 19/19 통과. 추가: 도구 이벤트 분리, 기본 연결값, 로그인된 인증 방식 필터.
  - 남은 일: 5단계 메모리 표시, 인증 방식 이름 한국어화("ChatGPT or Codex Subscription"은 Hermes 원문 그대로).
- 2026-09-23: 화면 다듬기.
  - 설정 → 화면에 "입력할 때 맞춤법 검사" 체크 추가. 기본은 켜짐이다.
    - 방식: 최상위 요소의 `spellcheck` 속성을 입력창이 이어받는다.
    - 실제 입력으로 확인: 켜면 "디렉토리"에 빨간 밑줄, 끄면 밑줄 없음. "기본값으로"를 눌러도 이 선택은 유지된다.
  - 전송 버튼: 글자 "↑" 대신 Lucide `arrow-up` 아이콘(선 굵기 2.4)으로 바꿨다.
  - 한글 조합 중 글자 표시 지연 신고(아래 기록 참조).
- 2026-09-23: Codex식 왼쪽 사이드바 — 프로젝트(폴더)와 대화 목록.
  - 구성
    - 위부터 "새 채팅", 프로젝트(+로 폴더 추가, 프로젝트별 대화 목록·접기, 행에 마우스를 올리면 새 대화/목록에서 제거), 채팅(프로젝트 밖 대화)
    - 상단 줄: 사이드바 접기 버튼, 현재 프로젝트 이름
  - 작업 폴더 지정: Hermes HTTP API에는 대화별 작업 폴더 지정이 없다(공식 앱의 `tui_gateway`에는 있으나 연결 방식 전면 교체가 필요).
    - 그래서 요청마다 system 메시지로 "현재 프로젝트 작업 폴더: <경로>, 명령·파일 작업은 이 폴더 기준"을 보낸다.
    - 실측: 프로젝트 대화는 `C:\Users\chaconne\controlroom\agent-client`, 일반 대화는 `C:\Users\chaconne`에서 실행됐다.
    - 한계: 지시문 방식이라 에이전트가 따르는 것에 의존한다. 강제가 필요해지면 `tui_gateway` 전환을 검토한다.
  - 저장: 작업공간(`agent-client:workspace:v1`)과 대화별 기록(`agent-client:chat:<id>`)으로 나눴다.
    - 대화 id가 곧 Hermes 세션 id다.
    - 예전 단일 대화는 첫 요청 id를 그대로 써서 "이전 대화"로 옮기도록 했다. 이번 PC에서는 예전 대화가 이미 비어 있어 옮길 것이 없었다.
    - 한 번도 질문하지 않은 빈 대화는 다른 대화로 옮길 때 자동으로 지운다.
    - 대화를 옮기면 진행 중 응답은 멈추고, 그때까지의 내용은 원래 대화에 저장한다.
  - 폴더 선택: Tauri 공식 `dialog` 플러그인
  - 창 하나만 허용: 공식 `single-instance` 플러그인을 넣었다. 앱을 두 번 실행하면 기존 창이 앞으로 나온다. 두 창이 같은 저장소를 덮어쓰는 문제를 막는다.
  - 확인: 테스트 23/23 통과. 실제 앱에서 폴더 추가 → 프로젝트 대화 → 일반 대화 → 대화 전환 → 재시작 후 복원 → 두 번 실행 시 창 1개 → 사이드바 접기/펴기를 확인했다.
- 한글 조합 중 글자 표시 지연 신고 기록: 천천히·보통 속도·답변 대기 중·답변 출력 중 모두 재현되지 않았고, 이후 사용자가 정상이라고 확인했다. 원인은 미확정이다.
