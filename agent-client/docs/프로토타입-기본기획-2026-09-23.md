# Agent Client 프로토타입 기본기획 — Windows 설치형

작성일: 2026-09-23 · 근거: [통합 에이전트 서비스 구상 v0.2](통합-에이전트-서비스-구상-2026-09-23.md) 13절 1~2단계

## 1. 목적

Windows PC에 설치해 쓰는 데스크톱 앱으로 다음 두 가지를 확인한다.

1. **디자인**: 실제 대화에서도 "예쁘고 오래 읽어도 편하다"가 느껴지는가
2. **메모리**: 다음 작업에서 "그 얘기를 다시 안 해도 되네"를 경험하는가

요금제·커뮤니티·일반 사용자용 관리형 서비스·모바일은 이번 범위가 아니다.

## 2. 기술 선택

| 부분 | 선택 | 이유 |
|---|---|---|
| 화면 | 기존 시제품(메인서버 `rndlog/telegram_miniapp/frontend`)의 Vite + JavaScript 코드 재사용 | Markdown 정제·코드 강조·말풍선 구조·스트리밍 수신이 이미 있음 |
| 앱 포장 | Tauri 2, Windows 설치 파일(NSIS `.exe`·`.msi`) | Windows 내장 WebView2 사용으로 가벼움. 이후 macOS·모바일로 같은 방식 확장 |
| 에이전트 | 메인서버 기본 Hermes API (`/v1/chat/completions`, SSE 스트리밍) | 미니앱이 이미 같은 API로 동작 중 |
| 메모리 | Hermes에 이미 연결된 GBrain(스킬·CLI 경유)을 그대로 사용 | 새 메모리 시스템을 만들지 않음 |
| 접속 | SSH 터널 `localhost:8642 → main 172.30.31.128:8642` | 공개 주소를 열지 않음 |
| API 키 | 앱의 Rust 쪽이 Windows 자격 증명 관리자에 보관·사용 | 화면 코드와 설정 파일에 키가 남지 않음 |

Electron은 추가 설치가 필요 없지만 설치 크기가 커서 "가벼움" 기준에 맞지 않아 제외했다.

## 3. 개발 환경 (2026-09-23 확인)

- 있음: Node 24, npm 11, WebView2
- 설치 필요: Rust(rustup), Visual Studio 2022 Build Tools의 C++ 데스크톱 작업 항목

## 4. 만드는 순서

1. 개발 도구 설치
2. 빈 앱 설치 확인: 기존 시제품 화면을 Tauri로 감싸 설치 파일을 만들고, 이 PC에 설치해 실행
3. 대화 화면: 답변 전체 너비, 요청 약 3분의 2 폭 말풍선, `#FDFDFD / #202020 / #339CFF`, 읽기·코딩·야간 프리셋과 자간·행간 조절
4. Hermes 연결: 앱 내부에서 대화 전송·실시간 수신, 이전 대화 이어쓰기
5. 메모리 표시: 답변에 쓰인 기억을 펼쳐 보기. **Hermes API가 GBrain 조회 과정을 화면에 넘겨주는지 미검증**이므로 이 단계 첫 작업으로 확인
6. 실사용: 며칠간 실제 업무에 사용하고 불편한 점 기록

## 5. 만들지 않는 것

- 가입·로그인·결제·요금제, QR 초대·기기 해제
- 모바일·macOS 빌드, 앱스토어 제출
- 적응형 표현 AI
- 기존 Telegram 미니앱·RNDLOG·Hermes·GBrain 설정 변경

## 6. 코드 위치와 공유

- 코드: 조정실 `agent-client/app` (Windows PC 로컬)
- controlroom `.gitignore`가 `agent-client/docs`만 공유하므로 app 코드는 현재 Git 공유 대상이 아니다. 공유가 필요해지면 `.gitignore` 변경을 별도로 결정한다.
- 원본 미니앱 코드는 복사만 하며 원본은 수정하지 않는다.

## 7. 진행 기록

- 2026-09-23: 기획 승인. 1단계부터 진행.
- 2026-09-23: 1~2단계 완료.
  - 설치한 개발 도구: Rust 1.98.1, VS 2022 Build Tools(C++)
  - 기존 시제품 코드를 `agent-client/app`에 복사했다. 바꾼 것은 세 가지다: 독립 `index.html`, `dist` 출력으로 바꾼 `vite.config.js`, Tauri 2 뼈대(`src-tauri`, 식별자 `local.agentclient.prototype`).
  - 테스트: 복사 직후와 변경 후 모두 15/15 통과.
  - `npm run tauri build`로 NSIS 설치 파일(약 4.8MB)과 MSI 설치 파일(약 5.8MB)을 생성했다.
  - 설치 후 실행 확인: 한글 입력, 데모 응답 스트리밍, Markdown 표·코드 글꼴 표시.
  - 주의: Claude 데스크톱 앱 안에서 실행한 설치는 `AppData` 쓰기가 앱 패키지 저장소로 옮겨질 수 있다. 실제 사용 설치는 바탕화면의 설치 파일을 직접 실행한다.
  - 다음: 3단계 대화 화면 다듬기, 4단계 Hermes 연결(SSH 터널 + 자격 증명 관리자).
- 2026-09-23: 3~4단계 구현.
  - 화면
    - 상단 줄: 연결 상태, 새 대화, 설정
    - 사용자 말풍선 최대 66.7%
    - 입력창 포커스: 둥근 테두리 강조로 통일
    - 전송 버튼: 대비를 맞춘 진한 파랑 `#0B6FD6`
  - 설정 패널: 오른쪽에 열리고, 대화가 옆에 그대로 보여 변경 즉시 미리보기
    - 프리셋 3가지
      - 읽기: 17px / 1.70 / -0.012em / 820px
      - 코딩: 15px / 1.60 / 0 / 1040px
      - 야간: 어둡게, 나눔고딕코딩, 17px / 1.75 / -0.004em
    - 본문 글꼴: 함초롬돋움·맑은 고딕·나눔바른고딕·나눔고딕코딩
    - 글자 크기·행간·자간·본문 폭·테마 조절, 기본값 복원
  - 연결
    - 앱이 `ssh -L 127.0.0.1:8642:172.30.31.128:8642`를 창 없이 실행하고, 앱 종료 시 닫는다.
    - API 키는 Windows 자격 증명 관리자(`agent-client`)에만 저장한다. 화면 코드로는 SSE 본문만 전달한다.
    - 키가 없으면 "연결 전 · 예시 응답", 키가 있으면 실패를 실제 오류로 표시한다.
    - 매 전송 전에 터널을 확인해, 끊겼으면 다시 연다.
  - Telegram 전용 코드와 그 테스트 4개를 삭제했다. 전송 경로·새 대화·화면 설정 테스트 6개를 추가해 15/15 통과.
  - 실제 화면 확인
    - 예시 대화와 세 프리셋의 즉시 반영
    - 가짜 키로 터널 개통 → "API 키가 맞지 않습니다" 표시, 전송 시 같은 오류(예시 응답으로 대체하지 않음)
    - 키 삭제 후 "연결 전" 복귀
    - 앱 종료 시 ssh 종료
  - 코드 리뷰: 터널이 끊긴 뒤 재연결하지 않는 결함 1건을 수정했다. 재리뷰에서 finding 없음.
  - **미검증**: 실제 키로 Hermes 답변 스트리밍. 사용자가 설정 화면에서 키를 직접 입력한 뒤 확인한다.
  - 다음: 실제 대화 확인 → 5단계 메모리 표시(Hermes가 GBrain 조회 과정을 스트림으로 주는지 먼저 확인).
- 2026-09-23: 앱 아이콘 선택 기능.
  - 후보 6개: 둥근 말풍선(기본)·반짝임·생각 구름·이어지는 대화·기억·밝은 말풍선
    - 모양 출처: Lucide(ISC)·Tabler Icons(MIT). 라이선스 전문은 `app/THIRD_PARTY_NOTICES.md`
    - 바탕: 둥근 사각형 `#0B6FD6`. 밝은 말풍선만 흰 바탕
  - 설정 → 앱 아이콘(3×2, 이름 표시)에서 고르면 창·작업표시줄 아이콘이 즉시 바뀌고, 다음 실행에도 유지된다.
    - 방식: Tauri `setIcon`, 권한 `core:window:allow-set-icon`
  - 설치 파일·바탕화면·시작 메뉴 아이콘은 설치 때 고정되므로 기본값 1번으로 생성했다(`app/app-icon.svg` → `tauri icon`).
  - 확인: 테스트 15/15 통과. 실제 앱에서 기억 → 생각 구름 → 둥근 말풍선으로 바뀌는 것, 재실행 후 유지되는 것을 확인했다.
- 2026-09-23: 로컬 Hermes 연결과 모델 설정 — 작동하는 프로토타입.
  - 배경: 공식 Hermes Desktop(Electron)은 무겁고 범용이라 복잡해서 사용자가 쓰지 않는다. 이 앱은 설정 없이 바로 쓰는 쪽을 목표로 한다.
  - 로컬 Hermes: `%LOCALAPPDATA%\hermes`(HERMES_HOME), v0.21.4, Windows 직접 설치, 게이트웨이는 예약 작업 `Hermes_Gateway`.
    - `.env`에 `API_SERVER_KEY`(무작위)·`API_SERVER_HOST=127.0.0.1`·`API_SERVER_PORT=8642`를 추가하고 게이트웨이를 재시작했다.
    - 백업: `.env.before-agent-client-20260923-184136`
  - 연결 방식
    - 기본값 "이 PC의 Hermes": 앱이 로컬 Hermes `.env`에서 키를 직접 읽는다. 사용자는 키를 입력하지 않는다.
    - API 서버가 꺼져 있거나 Hermes가 멈춰 있으면 설정에 "Hermes 연결 켜기" 버튼이 나온다.
      - 동작: 키가 없으면 `.env`에 추가하고 `hermes gateway restart`를 실행한다. 실제로 약 50초 걸린다.
    - "메인서버": 기존 SSH 터널 방식. 로컬 Hermes와 겹치지 않게 터널 포트를 18642로 옮겼다.
  - 모델 설정
    - Hermes `/api/model/options`에서 로그인된 인증 방식과 모델 목록을 불러와 두 개의 선택 상자로 보여준다.
    - 기본값: `openai-codex`(ChatGPT·Codex 구독) + `gpt-6-sol`
    - 요청마다 `model`·`provider`를 보낸다.
  - 도구 사용 표시: Hermes SSE의 `event: hermes.tool.progress`를 받아 대기 줄에 "N초 · 도구 사용 중: …"을 표시한다.
    - 5단계 메모리 표시에도 이 이벤트를 쓸 수 있다.
  - 실제 확인
    - 날짜·폴더 질문에 도구 실행 표시 후 정답
    - 이어진 질문이 앞 대화를 기억함
    - 모델을 `gpt-6-luna`로 바꾸자 Hermes 로그에서 `model=gpt-6-luna provider=openai-codex`로 처리됨. 이후 `gpt-6-sol`로 되돌림
    - 게이트웨이를 멈춘 뒤 "Hermes 연결 켜기"로 복구되고 앱이 자동 재연결됨
    - 앱을 닫아도 게이트웨이는 계속 실행됨
  - 테스트 19/19 통과. 추가: 도구 이벤트 분리, 기본 연결값, 로그인된 인증 방식 필터.
  - 남은 일: 5단계 메모리 표시, 인증 방식 이름 한국어화("ChatGPT or Codex Subscription"은 Hermes 원문 그대로).
- 2026-09-23: 화면 다듬기.
  - 설정 → 화면에 "입력할 때 맞춤법 검사" 체크 추가. 기본은 켜짐이다.
    - 방식: 최상위 요소의 `spellcheck` 속성을 입력창이 이어받는다.
    - 실제 입력으로 확인: 켜면 "디렉토리"에 빨간 밑줄, 끄면 밑줄 없음. "기본값으로"를 눌러도 이 선택은 유지된다.
  - 전송 버튼: 글자 "↑" 대신 Lucide `arrow-up` 아이콘(선 굵기 2.4)으로 바꿨다.
  - 한글 조합 중 글자 표시 지연 신고(아래 기록 참조).
- 2026-09-23: Codex식 왼쪽 사이드바 — 프로젝트(폴더)와 대화 목록.
  - 구성
    - 위부터 "새 채팅", 프로젝트(+로 폴더 추가, 프로젝트별 대화 목록·접기, 행에 마우스를 올리면 새 대화/목록에서 제거), 채팅(프로젝트 밖 대화)
    - 상단 줄: 사이드바 접기 버튼, 현재 프로젝트 이름
  - 작업 폴더 지정: Hermes HTTP API에는 대화별 작업 폴더 지정이 없다(공식 앱의 `tui_gateway`에는 있으나 연결 방식 전면 교체가 필요).
    - 그래서 요청마다 system 메시지로 "현재 프로젝트 작업 폴더: <경로>, 명령·파일 작업은 이 폴더 기준"을 보낸다.
    - 실측: 프로젝트 대화는 `C:\Users\chaconne\controlroom\agent-client`, 일반 대화는 `C:\Users\chaconne`에서 실행됐다.
    - 한계: 지시문 방식이라 에이전트가 따르는 것에 의존한다. 강제가 필요해지면 `tui_gateway` 전환을 검토한다.
  - 저장: 작업공간(`agent-client:workspace:v1`)과 대화별 기록(`agent-client:chat:<id>`)으로 나눴다.
    - 대화 id가 곧 Hermes 세션 id다.
    - 예전 단일 대화는 첫 요청 id를 그대로 써서 "이전 대화"로 옮기도록 했다. 이번 PC에서는 예전 대화가 이미 비어 있어 옮길 것이 없었다.
    - 한 번도 질문하지 않은 빈 대화는 다른 대화로 옮길 때 자동으로 지운다.
    - 대화를 옮기면 진행 중 응답은 멈추고, 그때까지의 내용은 원래 대화에 저장한다.
  - 폴더 선택: Tauri 공식 `dialog` 플러그인
  - 창 하나만 허용: 공식 `single-instance` 플러그인을 넣었다. 앱을 두 번 실행하면 기존 창이 앞으로 나온다. 두 창이 같은 저장소를 덮어쓰는 문제를 막는다.
  - 확인: 테스트 23/23 통과. 실제 앱에서 폴더 추가 → 프로젝트 대화 → 일반 대화 → 대화 전환 → 재시작 후 복원 → 두 번 실행 시 창 1개 → 사이드바 접기/펴기를 확인했다.
- 2026-09-23: 프로젝트별 Hermes 세션 분리.
  - 원인: 모든 요청이 같은 `X-Hermes-Session-Key`(`agent-client:desktop`)를 보냈다.
    - Hermes에서 이 키는 대화 범위를 정한다: 장기 기억 범위, 모델 재지정, 터미널 세션 키.
    - `X-Hermes-Session-Id`는 대화별로 달라서 기록(transcript)만 분리돼 있었다.
  - 변경
    - 프로젝트 대화: `agent-client:project:<프로젝트 id>`. 같은 프로젝트의 대화끼리는 공유한다.
    - 프로젝트 밖 대화: `agent-client:chat:<대화 id>`. 대화마다 따로 쓴다.
  - 확인: Hermes `state.db`의 `sessions.session_key`
    - 새 프로젝트 대화: `agent-client:project:ecf672e6-…`
    - 새 일반 대화: `agent-client:chat:2b318eff-…`
    - 변경 전에 만든 두 대화의 행은 Hermes가 이미 기록한 키(`agent-client:desktop`)를 그대로 둔다. Hermes는 기존 행의 키를 다시 쓰지 않는다. 이후 요청에는 새 키가 전달된다.
  - 테스트 24/24 통과
- 2026-09-23: `/new`와 Hermes 세션 삭제.
  - `/new`: 입력창에 `/new`를 입력하면 같은 대화창에서 새 Hermes 세션(새 `X-Hermes-Session-Id`)을 시작한다.
    - 화면에 "새 세션 · 시각" 구분선을 남긴다. 이전 내용은 보이지만 Hermes는 기억하지 않는다.
    - `/`를 입력하면 명령 안내가 나온다.
    - 대화마다 사용한 세션 목록(`chat.sessions`)을 저장하고, 마지막 세션으로 요청한다.
    - 세션 키(범위)는 세션이 바뀌어도 대화·프로젝트 단위로 유지한다.
    - 빈 대화에서나 연속으로 `/new`를 입력하면 무시하고 "이미 새 세션입니다"라고 알린다.
  - 삭제: 대화를 삭제하거나 프로젝트를 목록에서 빼면 그 대화들이 쓴 Hermes 세션을 모두 `DELETE /api/sessions/{id}`로 지운다. 404는 이미 없는 것으로 본다.
  - 실측
    - 암호를 알려준 뒤 `/new`를 하고 다시 물으니 "모르겠습니다"라고 답했다.
    - 대화를 삭제하자 두 세션(원래 세션과 `/new` 세션)의 `sessions`·`messages` 행이 모두 0건이 됐다.
  - 발견·수정한 결함: 폴더 선택용 dialog 플러그인이 들어간 뒤 `window.confirm`이 항상 참인 Promise를 돌려줘, 삭제 확인 없이 바로 지워졌다.
    - 수정: 플러그인의 `confirm`(권한 `dialog:allow-message`)으로 바꿨다.
    - 확인: 확인 창이 나오고, "취소"를 누르면 유지되며 "삭제"를 누르면 지워진다.
    - 영향: 결함이 있던 동안 지워진 것은 시험용 대화 1건뿐이다.
  - 테스트 26/26 통과
- 2026-09-23: 명령어 메뉴와 슬래시 명령.
  - Hermes HTTP API는 슬래시 명령을 실행하지 않는다(입력하면 일반 질문으로 전달됨). 그래서 앱이 명령을 해석해 API 기능으로 처리한다.
  - 명령 12개: 영어 이름과 한글 별칭 모두 사용 가능
    - 대화: `/new` 새 세션, `/clear` 화면 지우기, `/retry` 다시 시도, `/stop` 응답 중지, `/title` 대화 이름 바꾸기
    - 모델: `/model` 모델 선택, `/reasoning` 추론 강도, `/fast` 빠른 모드
    - Hermes: `/status` 상태 보기, `/usage` 사용량 보기, `/restart` Hermes 재시작, `/help` 명령어 도움말
    - 별칭 예: 새세션·지우기·다시·중지·이름·모델·추론·빠르게·상태·사용량·재시작·도움말
  - 입력창 왼쪽 메뉴 버튼: 대화·모델·Hermes로 묶은 목록을 "아이콘 + 한글 이름 + /명령" 형식으로 보여준다.
    - `/`를 입력하면 같은 목록이 걸러져 나온다. ↑↓와 Enter(또는 Tab)로 실행하고, Esc로 닫는다.
    - `/model 이름`처럼 인자가 있으면 입력한 그대로 실행한다.
  - 결과는 대화 안에 작은 카드로 보여주고 기록에는 저장하지 않는다. 모델·추론 강도는 목록에서 고르는 창을 연다.
  - 추론 강도와 빠른 모드는 Hermes `model_options`로 전달한다.
    - 추론 강도: `reasoning.effort`. Hermes 허용값은 none·minimal·low·medium·high·xhigh·max·ultra이고, 앱은 기본·낮음·보통·높음·매우 높음을 쓴다.
    - 빠른 모드: `service_tier: priority`
    - 설정 → 모델에도 같은 항목을 추가했다. 상단 표시에 "추론 높음·빠른 모드"가 붙는다.
  - `/status`·`/usage`: 새 Rust 명령 `hermes_info`가 `/health`와 `GET /api/sessions/{id}`를 읽는다. 구독의 남은 한도는 API에 없어 표시하지 않는다.
  - `/restart`: 이 PC 모드에서만 동작한다. 확인 창을 거친 뒤 `hermes gateway restart`를 실행하고 다시 연결한다.
  - 함께 고친 것
    - 앱 첫 화면에서 입력창 높이가 두 줄로 계산되던 문제: 대화 표시·창 크기 변경·글꼴 로드 후 높이를 다시 잰다.
    - 메뉴가 열리자마자 닫히던 문제: 바깥을 클릭할 때만 닫는다.
  - 실측: 메뉴 표시, 모델 선택 창(사용 중 모델 체크), `/추론 높음`→상단 표시, `/상태` 카드(v0.21.4·세션 번호), 실제 질문 후 `/사용량`(입력 16,501·출력 6 토큰), `/fast` 켜기·`/빠르게 끄기`, `/이름`으로 사이드바 이름 변경, `/재시작` 확인 창(취소). 시험 후 추론 강도는 기본값, 빠른 모드는 끔으로 되돌렸다.
  - 테스트 30/30 통과
- 2026-09-23: 선택 창 뒤로 가기, 앱 아이콘 선택 제거.
  - 모델·추론 강도 선택 창 맨 위 제목 줄을 "‹ 제목" 뒤로 버튼으로 바꿨다. 누르면 명령 메뉴로 돌아간다.
    - 키보드: Esc는 먼저 뒤로 가고 다시 누르면 닫힌다. 입력창이 비어 있으면 ←로도 뒤로 간다.
    - 메뉴에서 연 경우 고른 명령이 강조된 자리로, 슬래시로 연 경우 전체 메뉴로 돌아간다.
    - 실측: 모델 선택 → 뒤로 → "모델 선택"이 강조된 전체 메뉴
  - 사용자 결정으로 앱 아이콘 선택 기능을 없앴다. 아이콘은 1번 둥근 말풍선으로 고정한다(설치 아이콘은 `app-icon.svg`에서 생성).
    - 지운 것: 설정의 앱 아이콘 칸, 화면 설정의 `icon` 값, 창 아이콘 변경 코드(`setAppIcon`)와 권한 `core:window:allow-set-icon`, `src/app-icons.js`, 관련 스타일
    - `THIRD_PARTY_NOTICES.md`: 더 이상 쓰지 않는 Tabler 고지를 뺐다. Lucide 고지만 남는다.
  - 참고: 시험 대화 "명령어 시험"이 비어 보인 것은 저장 기록상 화면 지우기(`/clear`) 결과다. 앱에서 빈 기록을 저장하는 경로는 이것뿐이다.
  - 테스트 31/31 통과
- 2026-09-23: 설정 선택 상자의 기본 화살표가 오른쪽 끝에 붙어 보여, 기본 화살표를 숨기고 오른쪽에서 14px 안쪽에 16px 화살표를 그렸다(어두운 테마는 밝은 회색). 실제 화면에서 확인했다.
- 2026-09-23: 설정 드롭다운을 앱이 그리는 목록으로 교체(`src/dropdown.js`).
  - 이유: Windows가 그리는 `<select>` 목록은 모서리·그림자를 바꿀 수 없다.
  - 방식: 원래 `<select>`는 숨겨 둔 채 값 저장소로 두고, 버튼과 목록을 대신 보여준다. 기존 코드의 `.value` 읽기·쓰기와 change 이벤트는 그대로 동작하며, 코드로 값을 바꾼 뒤에는 `refresh()`로 표시를 맞춘다.
  - 모양: 명령 메뉴와 같은 12px 둥근 모서리·그림자·항목. 선택 항목은 체크와 굵은 글씨로 표시하고, 아래 공간이 부족하면 위로 열린다.
  - 조작: ↑↓·Enter·Esc, 바깥 클릭 시 닫힘. 라벨을 누르면 열린다.
  - 확인: 테스트 33/33 통과, 실제 화면에서 인증 방식 목록을 확인했다.
- 2026-09-24: 스크롤바 정리.
  - 문제: 드롭다운 목록의 Windows 기본 스크롤바(▲▼ 버튼, 네모 막대)가 둥근 모서리에 붙어 모서리가 깨져 보였다.
  - 변경: 앱 전체 스크롤바를 화살표 없는 10px 둥근 막대로 바꿨다. 드롭다운·명령 메뉴는 막대 길을 위아래 10px 줄여 모서리에 닿지 않게 했다.
  - 테스트 33/33 통과. 사용자가 앱을 쓰는 중이라 재설치·화면 확인은 하지 않았다. 새 설치 파일만 바탕화면에 두었다.
  - 미확인: 드롭다운의 Esc 닫기. ↓ 키는 동작했고, 도구로 보낸 Esc에는 설정 패널도 반응하지 않았다. 시스템 Esc를 보낸 뒤의 결과는 화면 확인이 거절돼 보지 못했다.
- 2026-09-24: 화면 설정 슬라이더 옆 값 입력 칸.
  - 슬라이더 오른쪽에 96px 입력 칸을 붙였다. 숫자는 오른쪽 정렬, 단위(px·배·em·px)는 고정 폭 칸 안 오른쪽에 둬 네 줄의 숫자와 단위가 세로로 맞는다.
  - Enter나 다른 곳 클릭으로 적용한다. 범위를 벗어나면 끝값으로 맞추고, 숫자가 아니면 되돌린다. ↑↓는 한 칸씩 움직이고, Esc는 입력만 취소한다(설정 패널은 닫지 않음). 슬라이더를 움직이면 입력 칸도 같이 바뀐다.
  - 테스트 33/33 통과. 새 설치 파일을 바탕화면에 두었고, 화면 확인은 아직 하지 않았다.
- 2026-09-24: 슬라이더 디자인 교체.
  - 기본 슬라이더(두꺼운 막대·큰 손잡이)를 4px 둥근 막대로 바꿨다. 채워진 부분만 강조색이고 나머지는 옅은 회색이다. 손잡이는 16px 흰 원에 얇은 테두리와 부드러운 그림자를 줬다.
  - 마우스를 올리면 손잡이가 조금 커지고, 끌거나 키보드로 선택하면 옅은 파란 테두리가 생긴다.
  - 채움 위치는 `--fill`로 그리며 값이 바뀔 때마다 갱신한다.
  - 실제 설치 앱에서 확인했다: 입력 칸의 숫자·단위 정렬, 새 슬라이더 모양, 마우스 올림 효과.
  - 참고: Claude 앱 안에서 실행한 설치는 Windows가 별도 공간(패키지 LocalCache)에 저장해 사용자 앱과 기록이 따로 논다. 이번부터는 WMI(`Win32_Process.Create`)로 실행해 사용자의 실제 설치를 갱신했다. 사용자의 exdigm 대화가 그대로 남아 있는 것을 확인했다.
- 2026-09-24: 입력창 모서리를 버튼과 같은 중심으로 맞췄다. 44px 원형 버튼 반지름 22px + 여백 7px = 29px. 여백은 사방 7px로 통일하고, 좁은 화면에서 왼쪽 여백을 15px로 바꾸던 예전 규칙은 지웠다. 실제 설치 앱에서 확인했다.
- 2026-09-24: 전송·중지 버튼 다듬기.
  - 입력이 비면 옅은 회색 버튼(입력창에 `is-empty` 클래스), 글자가 있으면 파란 버튼이 된다.
  - 파란 버튼: 위쪽 은은한 빛(그라데이션), 안쪽 밝은 선, 파란빛 그림자
  - 마우스 올림: 더 밝아지고 1px 떠오르며 그림자가 진해지고 화살표가 위로 1px 움직인다.
  - 누름: 0.94배로 눌린다. 키보드 선택: 파란 링
  - 전환은 0.15초이며 애니메이션 줄이기 설정을 따른다.
  - 실제 설치 앱에서 빈 상태·입력 상태·마우스 올림을 확인했다.
- 2026-09-24: 세그먼트 버튼(연결·프리셋·테마) 여백 통일.
  - 원인: 틀 여백이 3px였고, 선택 버튼 그림자가 아래로만 떨어졌으며, 모서리가 바깥 11px·안쪽 8px로 어긋났다.
  - 수정: 여백과 간격을 4px로, 바깥 모서리를 12px(8+4)로 바꿨다. 선택 버튼은 사방 1px 선과 아주 작은 그림자로 바꿨다.
  - 실제 설치 앱에서 확인했다.
- 2026-09-24: 설정 분류 재정리.
  - 문제: "화면" 칸에 글자 모양(읽기·코딩, 글꼴, 크기 등), 색(야간, 테마), 입력 동작(맞춤법)이 섞여 있었다. "야간" 프리셋은 색과 글자를 함께 바꿔 테마 "어둡게"와 겹쳤다.
  - 변경
    - "본문 스타일" 칸: 모드(읽기·코딩), 본문 글꼴, 크기·행간·자간·폭, 기본값으로
    - "테마" 칸: 밝게·어둡게·시스템
    - "입력" 칸: 맞춤법 검사
  - "야간" 프리셋 제거. 프리셋은 글자 모양만 바꾸고 테마를 건드리지 않는다. 전에는 "코딩"을 누르면 테마가 밝게로 돌아갔다.
    - 테마를 바꿔도 모드는 그대로 둔다. "기본값으로"는 본문 스타일만 되돌린다.
  - 기존 "야간" 사용자: 어두운 테마와 글자 값은 유지되고, 모드 표시만 직접 설정이 된다(테스트 추가, 실제 앱에서 확인).
  - 이름: "화면 모드"는 테마와 다시 헷갈려서, 칸 제목은 "본문 스타일", 읽기·코딩 줄 이름은 "모드"로 했다.
  - 테스트 34/34 통과
- 2026-09-24: 설정 칸 접기·펴기.
  - 각 칸 제목 줄 전체를 토글 버튼으로 만들었다(⌄ 펼침, › 접힘, 0.15초 회전). 칸 구조는 그대로 두고 설정 화면을 만든 뒤 제목을 버튼으로 바꾼다.
  - 처음에는 "모델"만 펼친다. 펼친 상태는 `agent-client:settings-sections:v1`에 저장한다.
  - 연결 오류가 나면 "연결" 칸을 자동으로 펼쳐 "Hermes 연결 켜기"가 보이게 한다.
  - 칸 제목 글씨를 12px에서 13px로 키웠다.
  - 실제 앱에서 기본 상태와 테마·모델 칸 열고 닫기를 확인했다.
- 2026-09-24: 설정 화면 UX 전면 정리.
  - 문제
    - 칸 제목(흐린 회색)이 항목 이름(진한 굵은 글씨)보다 약해 위계가 뒤집혔다.
    - "모델" 칸 안에 "모델" 항목이 겹쳤다.
    - 칸 사이 구분이 얇은 선뿐이라 약했다.
    - 맨 위 "설정"이 밋밋했고, 접힌 칸은 현재 값을 보여주지 않았다.
  - 변경
    - 맨 위 "설정" 앞에 30px 둥근 사각형 아이콘 타일(조절 아이콘)을 두고 아래에 구분선을 넣었다.
    - 각 칸을 옅은 배경의 둥근 카드(14px)로 만들고 카드 사이를 10px 띄웠다.
    - 칸 제목 앞에 26px 아이콘 타일을 두었다. 연결=플러그, AI=칩, 본문 스타일=T, 테마=해·달, 입력=키보드
    - 칸 제목은 진하게(14px), 항목 이름은 한 단계 낮게(12px 회색) 했다.
    - 접힌 칸 오른쪽에 현재 값을 보여준다. 예: "이 PC · 연결됨", "gpt-6-sol", "어둡게"
    - 이름 정리: "모델" 칸 → "AI", "본문 글꼴" → "글꼴", "본문 폭" → "줄 너비". 연결 칸에 "연결 대상" 이름을 붙였다.
  - 모델 선택(사용자 요청 반영)
    - 인증 방식 → 모델 두 단계 선택기(`src/model-picker.js`)로 바꿨다. 열면 현재 인증 방식의 모델이 나오고, 맨 위 "‹ 인증 방식 이름"으로 인증 방식 목록에 올라간다.
    - 빠른 모드는 체크 칸 대신 모델 목록의 한 줄로 넣었다. Hermes `capabilities[model].fast`인 모델만 "모델 ⚡" 줄이 하나 더 생긴다.
    - 별도 "인증 방식"·"빠른 모드" 칸은 없앴다.
    - `/model`도 같은 두 단계로 동작하고, 뒤로는 인증 방식 → 명령 메뉴 순이다.
    - `/fast`는 지원하지 않는 모델에서는 안내만 하고 바꾸지 않는다.
  - 테스트 38/38 통과. 실제 설치 앱에서 카드·아이콘·요약, 모델 목록(⚡), 인증 방식 목록으로 뒤로 가기를 확인했다.
- 2026-09-24: 모델 분류를 "제공사 → 모델"로 바꾸고 인증 방식을 조건부로 표시.
  - 사용자 지적
    - 상위 분류는 인증 방식이 아니라 제공사(OpenAI·Anthropic 등)여야 한다.
    - 같은 모델을 구독 인증과 API 키로 모두 쓸 수 있다.
    - 쓸 수 없는(인증 안 된) 모델은 목록에 보일 필요가 없다.
    - 아이콘 색(파랑)이 튄다.
  - 변경
    - `src/model-catalog.js`: Hermes 채널(인증 경로)들의 모델을 제공사 → 모델로 묶는다. "anthropic/claude-x"와 "claude-x"는 같은 모델의 두 경로다.
    - 인증 종류 판정: 로컬 Hermes `auth.json`의 채널별 `auth_type`(값 없이 종류만 읽음, Rust `auth_kinds`)이 정본이고, 없으면 Hermes 등록표(`openai-codex`·`xai-oauth`·`qwen-oauth`·`minimax-oauth`·`nous` = 구독)를 쓴다.
    - 목록 제외: 인증 안 된 채널, `moa`, 재로그인이 필요한 채널(현재 Nous Portal).
    - 설정의 "인증 방식" 칸은 고른 모델에 경로가 2개 이상일 때만 나타난다. 구독 경로를 먼저 보여준다.
    - `/model`도 제공사 → 모델(⚡) → (경로가 여럿이면) 인증 방식 순이다.
    - 설정 아이콘 타일을 회색 배경·회색 아이콘으로 바꿨다.
  - 확인: 테스트 40/40 통과. 실제 앱에서 회색 아이콘, 제공사 목록, claude-opus-5·claude-fable-5.1의 "구독 · Claude / API 키 · OpenRouter" 선택, claude-opus-5.5는 OpenRouter 경로만 있음(Claude 구독 채널 목록에 없음), `/model` 동작을 확인했다. 기본값은 gpt-6-sol(구독 · ChatGPT)로 되돌렸다.
  - 다음 제안: 새 제공사 추가(구독 로그인·API 키 등록)와 재로그인(Nous Portal)을 위한 별도 화면.
- 2026-09-24: 모델 선택과 Provider 추가를 별개 기능으로 분리 (위 "제공사 → 모델" 구조를 대체).
  - 사용자 기준
    - 모델 선택: 지금 쓸 수 있는(인증된) Provider → 모델만. 빠른 속도는 ⚡ 줄. 인증 방식은 나오지 않는다.
    - Provider 추가: 인증 방식은 여기서만 고르고, 방식이 하나뿐이면 묻지 않는다. 속도와는 관계없다.
    - 용어는 "제공사" 대신 "Provider". 한국어가 길거나 애매하면 영어를 쓴다.
  - Provider 묶음(`src/providers.js`): Hermes는 인증 방식마다 채널이 따로라 회사 단위로 묶는다. OpenAI(openai-codex·openai-api), Anthropic(anthropic·claude-code), xAI(xai-oauth·xai), MiniMax, Qwen(qwen-oauth·alibaba), GitHub Copilot, Google(gemini). 나머지는 Hermes 이름 그대로.
    - 한 Provider에 채널이 둘이면 구독 채널을 먼저 쓴다(예: xAI Grok은 SuperGrok 구독 우선).
  - 설정에 "Provider" 칸 추가(`src/provider-panel.js`)
    - 연결된 Provider와 모델 수를 보여준다.
    - "Provider 추가": 아직 연결 안 된 Provider만 목록에 나온다(재로그인이 필요한 Nous Portal 포함).
    - 기기 코드 로그인(ChatGPT·xAI·Nous·MiniMax): 코드 표시 + 로그인 페이지 열기 + 승인 대기 후 자동 반영.
    - 터미널 로그인(Claude·Qwen·Copilot): Hermes가 정한 명령으로 터미널을 연다. Claude 구독은 Anthropic 정책상 Hermes도 터미널로만 허용한다.
    - API 키: 입력한 키를 Hermes로 먼저 검사한 뒤 Hermes 설정에 저장한다.
    - 메인서버 연결일 때는 "서버에서 관리"로 안내하고 추가 버튼을 숨긴다.
  - 실행 경로: Hermes 설정 API는 채팅 서버(8642)에 없고 `hermes serve`(Hermes Desktop이 쓰는 헤드리스 백엔드)에 있다. 앱이 "Provider 추가"를 누를 때 임의 포트·일회용 토큰으로 띄우고(첫 기동 약 16~20초) 앱 종료 때 프로세스 트리째 끈다(Rust `hermes_admin`, `open_login_terminal`).
  - 확인: 테스트 43/43 통과(Provider 묶음·추가 흐름 테스트 포함). 실제 앱에서 Provider 7개 목록, AI 칸의 인증 방식 칸 제거, 추가 목록 로딩, Qwen의 인증 방식 선택(구독/API 키), API 키 입력 화면, Qwen 터미널 열림(명령 확인 후 닫음), Nous Portal 기기 코드 발급과 로그인 페이지 열림을 확인했다. 실제 로그인·키 저장 완료는 사용자 몫이라 검증하지 않았다.
- 2026-09-24: 본문 스타일 모드(읽기·코딩)별로 조정값을 기억하도록 수정.
  - 문제: 슬라이더·글꼴을 바꾸면 모드가 "직접 설정"으로 바뀌어 읽기·코딩 어느 쪽도 선택 표시가 없었다. 다시 "읽기"를 누르면 읽기 기본값으로 덮어써 조정한 글자 크기가 풀렸다.
  - 변경(`src/settings.js`): 모드마다 조정값을 `modes`에 따로 저장한다. 조정해도 현재 모드 선택이 유지되고, 모드를 바꾸면 그 모드의 조정값이 돌아온다. "기본값으로"는 현재 모드만 되돌린다. 예전 "직접 설정" 값은 읽기 모드 값으로 이어받는다.
  - 확인: 테스트 44/44 통과. 실제 앱에서 읽기 16px → 코딩 15px → 읽기 16px 복원, 슬라이더 조정 후에도 읽기 선택 유지를 확인했다.
- 2026-09-24: 설정 화면 다듬기.
  - 파란 테두리: 키보드 이동 표시(focus-visible)인데, Chromium은 마우스 클릭 뒤 아무 키(Shift, 화면 캡처 단축키 등)만 눌러도 켠다. 마우스로 누른 뒤에는 숨기고 Tab으로 이동할 때만 보이게 했다(`main.js`의 `data-pointer`).
  - 칸 제목 호버: 칸이 열려 있으면 위쪽 모서리만 둥글고 아래는 평평하게(엎어놓은 그릇 모양), 닫혀 있으면 카드 안쪽 반지름(13px)으로 전체 둥글게.
  - Provider 칸 간격: 빈 안내 문구가 자리를 차지하던 것 제거(`.field-note:empty`), 인증 방식 칸 등 모든 묶음을 같은 8px 간격(`.field-group`)으로, 취소 버튼은 "기본값으로"처럼 왼쪽 정렬.
  - 확인: 테스트 44/44 통과. 실제 앱에서 클릭+Shift 때 테두리 없음, Tab 이동 때 테두리 표시, 호버 모양, 추가 양식 간격을 확인했다.
- 2026-09-24: Provider 목록을 "무엇이 연결됐고 어떻게 인증됐는지" 중심으로 변경.
  - 모델 수 표시를 없애고 Provider마다 인증 방식 배지(구독·API 키, 둘 다면 둘 다)를 초록으로 표시한다. 이름 앞에 초록 체크 아이콘.
  - 한 번 연결됐다가 로그인이 끊긴 Provider(Hermes auth.json에 재로그인 오류가 남은 것, 현재 Nous Portal)는 빨간 경고 아이콘 + "연결 안 됨" 배지 + "다시 로그인" 버튼. 버튼은 추가 양식을 그 Provider로 선택된 채 연다.
  - 한 번도 연결하지 않은 Provider는 목록에 넣지 않고 "Provider 추가"에만 둔다(50여 개가 섞이면 중요한 상태가 묻힘).
  - GitHub Copilot은 GitHub 로그인으로 쓰는 구독이라 "구독"으로 분류.
  - 확인: 테스트 46/46 통과. 실제 앱에서 7개 연결(초록)과 Nous Portal(빨강), "다시 로그인" → Nous Portal 선택된 양식을 확인했다.
- 2026-09-24: Provider 목록 디자인 단순화(바로 위 항목의 아이콘·배지·"연결 안 됨" 표시를 대체).
  - 사용자 기준: 끊긴 연결과 처음 연결을 구분할 가치가 없다. 연결 안 된 것은 전부 "Provider 추가"에서 로그인한다. 덕지덕지 붙이지 말고 포인트 색 하나만.
  - 각 줄: "Provider 이름 ON"(ON만 밝은 주황 `--on`) + 오른쪽 끝에 인증 방식을 회색 글씨로("구독", "API 키", "구독 · API 키"). 체크 아이콘·배지·빨간 표시·"다시 로그인" 버튼 제거.
  - "Provider 추가" 버튼: + 아이콘에 어두운 배경·밝은 글씨(본문색 반전, 다크 테마에선 밝은 배경), 호버 때 배경이 조금 밝아진다.
  - 확인: 테스트 46/46 통과. 실제 앱 화면 확인.
- 2026-09-24: 모델 선택을 접었다 펴는 한 목록으로 변경(설정 선택기와 `/model` 공통, `modelMenuRows`).
  - 열면 사용 가능한 Provider가 모두 접힌 채 나오고, 현재 모델의 Provider만 펼쳐져 현재 모델이 강조된다. 다른 Provider를 누르면 그쪽이 펼쳐지고 나머지는 접힌다(한 번에 하나).
  - 이전의 "Provider 목록 ↔ 모델 목록" 두 단계와 뒤로 가기 단계를 대체.
  - 함께 고친 오류: `/model` Provider 항목의 데이터를 메뉴가 묶음 제목으로 쓰는 `group` 이름에 담아 "[object Object]"가 제목으로 보이던 문제.
  - 확인: 테스트 46/46 통과. 실제 앱에서 `/model`과 설정 선택기 모두 OpenAI 펼침 + gpt-6-sol 강조, Anthropic 클릭 시 전환을 확인했다(모델은 바꾸지 않음).
- 2026-09-24: 입력창을 Codex형 기능으로 개편(디자인은 다르게).
  - 디자인 기준(수상작보다 공개 디자인 시스템에서 수치를 가져옴: GitHub Primer, Vercel Geist, Apple HIG, Linear)
    - 강조는 한 곳: 보내기 버튼(진한 원, 다크 테마에선 반전). 파란색은 키보드 이동 표시에만.
    - 크기: 도구 버튼 32px, 보내기 34px, 칩 28~32px. 모서리: 입력창 20px, 칩·버튼 8~9px, 메뉴 14px.
    - 배치: 입력창 위 = 어디서 일하는가(프로젝트·연결·브랜치, 박스 없는 칩). 입력창 안 아래줄 왼쪽 = 넣는 것·권한(명령 메뉴, +, 권한), 오른쪽 = 모델·음성·보내기.
  - 기능과 근거
    - 프로젝트 칩: 누르면 프로젝트 목록·새 폴더·프로젝트 없이 대화. 프로젝트가 없어도 "프로젝트 없음"으로 보인다(기존 테스트 기대값을 이 규칙으로 변경).
    - 연결 칩: 이 PC/메인서버 + 상태 점(초록 연결됨). 누르면 설정. 상단 바의 프로젝트·연결 표시는 중복이라 제거.
    - 브랜치 칩: 프로젝트 폴더가 Git 저장소일 때만. 저장하지 않은 변경이 있으면 전환하지 않는다(Rust `git_info`/`git_switch`). exdigm 로컬 폴더(C:\Users\chaconne\projects\exdigm)는 Git 저장소가 아니라 표시되지 않는 것이 정상.
    - 첨부(+, 붙여넣기, 끌어 놓기): 이미지는 본문에 바로 넣는다(Hermes가 모델별로 직접 보여주거나 설명으로 바꿈). 그 밖의 파일은 Hermes 자체 채널과 같은 방식으로 Hermes 문서 캐시(`cache/documents`)에 복사하고 "저장 위치 + 직접 읽으라"는 안내문을 앞에 붙인다. 메인서버 연결에서는 이미지만. 대화 기록에는 파일 이름·종류만 저장.
    - @ 파일: 프로젝트 폴더에서 이름으로 찾아 상대 경로를 넣는다(링크 폴더 제외, 파일명 일치 우선).
    - 모델 칩: 모델 이름 + 추론 강도(옅게) + ⚡. 누르면 "추론 강도" 한 줄 + Provider 접이식 목록. 칩 쪽(오른쪽)에 열린다.
    - 마이크: 녹음 → Hermes 받아쓰기(`/api/audio/transcribe`, 현재 xAI) → 입력칸에 삽입. 이 PC 연결에서만. 연결 직후 Hermes 설정 기능을 미리 켠다.
    - 권한 표시: Hermes `approvals.mode`가 off면 주황 "전체 액세스"(보기만).
    - 답변 중 입력은 대기열에 쌓였다가 순서대로 보낸다.
  - 확인
    - 테스트 54/54 통과(첨부 요청 형식, 대기열, @ 파일, 모델 칩, 첨부 이름 저장 추가).
    - 실제 앱: 이미지+README 첨부 질문에 "빨간색, 'Agent Client — 제품·사업 구상'"으로 정확히 답함. 한국어 합성 음성이 "안녕하세요. 음성 입력 시험입니다."로 받아써짐. 앱 재시작 뒤 첨부 이름 유지. 모델 메뉴 위치·눌림 표시, 스크롤바 제거 확인.
    - 마이크 실제 녹음은 사람 음성이 필요해 미확인(첫 사용 때 Windows 마이크 허용 창이 한 번 뜸).
  - 사고 기록: 설치 중 앱 재시작이 주인님의 진행 중 질문을 끊었다. 이후 앱 재시작은 먼저 허락을 받는다.
  - 다음 작업(주인님 지정): Worktree(별도 작업 사본), 권한 승인 방식(명령마다 허용/거부 — Hermes 실행 단위 API 필요).
- 2026-09-24: 입력창 버튼마다 "누르면 무엇을 하는지" 분명하게(Codex 실제 동작 확인 후 반영).
  - Codex 확인(설치된 앱을 UI Automation으로 읽음, 설정은 바꾸지 않음): 모든 버튼이 제목(질문) + 설명 달린 선택지 메뉴를 연다. 작업 위치(로컬/새 로컬 워크트리/클라우드), 브랜치(검색 + 목록 + 새 브랜치 생성 및 체크아웃), +(추가: 파일 및 폴더·계획 모드… + 플러그인), 권한("어떻게 승인할까요?" 승인 요청/나 대신 승인/전체 권한 + 설명), 모델(추론 파워 6단계 + 빠른 모드 + 모델 선택).
  - 반영
    - 메뉴 공통: 제목, 선택지 설명 한 줄, 고를 수 없는 항목 표시, 검색칸, 아래 안내문. 메뉴는 누른 버튼 바로 위에 열린다(모델 메뉴는 오른쪽 정렬). 마우스·키보드 강조가 한 줄만 되도록 통일(두 줄이 붙어 강조되던 문제). 열 때 사용 중인 항목이 보이게 스크롤.
    - 이 PC 칩 → "작업 위치": 이 PC / 메인서버(바로 전환·재연결) / 연결 설정…
    - 브랜치 칩: 검색 + 새 이름이면 "브랜치 만들고 전환"(git check-ref-format 검사 후 switch -c). 저장 안 한 변경이 있으면 다른 브랜치는 고를 수 없게 하고 안내.
    - + → "추가": 파일 및 이미지 / 프로젝트 파일 언급(@).
    - 권한 칩 → "Hermes 명령 승인 방식": 승인 요청(manual)/자동 승인(smart)/전체 액세스(off) 설명과 현재 값. 변경은 다음 작업(Hermes 전체 설정이라 다른 채널에도 적용).
    - 모든 버튼에 무엇을 하는지 툴팁. 녹음 중에는 마이크 옆에 경과 시간.
    - 보낸 메시지의 첨부 칩과 말풍선 사이 간격(8px).
  - 확인: 테스트 55/55 통과(메뉴 검색·새 이름·비활성 행 건너뛰기 추가). 실제 앱에서 작업 위치·승인 방식·추가·모델·프로젝트 메뉴 화면 확인.
  - 사고 방지 기록: Codex 메뉴를 닫으려 같은 자리를 다시 누르면 위로 열린 메뉴 항목이 눌릴 수 있었다. 확인 결과 브랜치·작업 위치·권한·모델 모두 그대로였고 진행 중 작업도 계속됐다. 다른 앱 확인은 읽기 위주로 하고 닫기는 메뉴 밖을 누르는 방식으로 한다.
- 2026-09-24: 메뉴 항목과 실제 동작 일치 점검.
  - 쓸 수 없는 항목은 보이지 않게: "+"의 프로젝트 파일 항목은 프로젝트가 있을 때만 "프로젝트 파일 찾아 넣기"로 보인다(누르면 @ 입력 + 파일 검색). 승인 방식 메뉴는 고를 수 없는 선택지 대신 현재 방식과 설명만 보여주는 안내(`info` 행)로 바꿨다.
  - 실제 앱에서 모든 항목을 눌러 확인: 프로젝트 전환, 파일 찾아 넣기(@AGENTS.md 삽입), 승인 방식 안내(눌러도 변화 없음), 추론 강도 변경(칩에 반영 후 기본값으로 되돌림), 연결 설정 열림, 작업 위치 메인서버↔이 PC 전환(되돌림), 프로젝트 없이 대화, 파일 선택 창·폴더 선택 창 열림(닫음, 추가된 것 없음).
  - 테스트 56/56 통과.
- 2026-09-24: 화면 문구 전체 점검(323개). 기준과 주요 변경.
  - 기준: 처음 보는 사람이 알아듣는 동사형 기능 이름 / 이름과 설명이 같은 말을 반복하지 않음 / 같은 대상은 같은 말(대화·답변·Hermes·승인 요청) / 개발 과정 말투("다음 작업에서 추가") 금지 / 정확한 용어(Git은 "커밋하지 않은 변경").
  - 주요 변경: "+" 메뉴 "파일 첨부 — 문서·이미지를 첨부합니다", "프로젝트 파일 선택 — exdigm의 파일을 골라 질문에 넣습니다(@)". 입력칸 "무엇이든 요청하세요". 새 채팅·채팅 → 새 대화·대화. 봇 응답 → Hermes 답변. 승인 필요 → 승인 요청, 메뉴 제목 "명령 실행 승인", 안내에서 "다음 작업" 문장 제거. 작업 위치 설명 단순화, 메인서버 키가 없으면 "API 키를 저장하면 쓸 수 있습니다". 프로젝트 메뉴 "프로젝트 추가… — 작업할 폴더를 고릅니다". 브랜치 "새 브랜치 'x' 만들기". 음성 오류·상태 문구, 명령 힌트("~하기" 형태) 정리.
  - 테스트 56/56 통과.
- 2026-09-24: 글꼴 7개 추가와 굵기 설정.
  - 추가: Pretendard(가변), 본고딕(Noto Sans KR 가변), 나눔고딕, 스포카 한 산스 Neo, 에스코어 드림, 페이퍼로지(이상 고딕), 나눔손글씨 펜(손글씨). 기존 함초롬돋움·나눔바른고딕·맑은 고딕·나눔고딕코딩 유지. 목록은 고딕/손글씨/코딩 묶음 제목으로 표시(`dropdown.js`가 optgroup 제목을 그림).
  - 파일과 라이선스: npm(`pretendard`, `@fontsource-variable/noto-sans-kr`, `@fontsource/nanum-gothic`, `@fontsource/nanum-pen-script`, 버전 고정), 공식 GitHub woff2(스포카 Subset, 페이퍼로지), 에스코어 공식 zip의 OTF 원본(파일 수정 금지 조건 → 변환하지 않음). 저작권 안내는 `public/licenses/fonts/`(에스코어 안내 이미지 포함)로 앱에 함께 배포. 설치 파일 약 29.5MB.
  - 굵기: 본문 스타일에 "굵기" 선택. 글꼴마다 실제로 있는 굵기만 보여주고(각 버튼을 그 굵기로 그림), 굵기가 하나뿐이면 "이 글꼴은 굵기가 한 가지입니다". 모드별 저장, 대화 글(.markdown)에만 적용, 강조는 본문보다 300 굵게.
  - 함께 고친 문제: Hermes 0.21.4부터 설정 백엔드(`hermes serve`)가 PC당 하나만 뜬다. 앱이 강제 종료되면 남은 백엔드가 새 실행을 막아 권한 표시·Provider 추가·음성 입력이 멈췄다. 앱이 띄운 백엔드의 포트·토큰을 Windows 자격 증명 관리자에 기억해 남은 것을 다시 쓰고, 다른 프로그램이 쥐고 있으면 그 사실을 알린다(`admin_busy`). 정상 종료 때는 함께 끈다(넘겨받은 것은 포트로 PID를 찾아 종료).
  - 확인: 테스트 58/58. 실제 앱에서 8개 글꼴 모든 굵기 로드, 설정 화면 굵기 선택, 강제 종료 후 재실행 시 같은 백엔드 재사용, 정상 종료 시 백엔드 종료 확인. 시험으로 바꾼 본문 스타일은 원래(함초롬돋움 15px)로 되돌림.
- 한글 조합 중 글자 표시 지연 신고 기록: 천천히·보통 속도·답변 대기 중·답변 출력 중 모두 재현되지 않았고, 이후 사용자가 정상이라고 확인했다. 원인은 미확정이다.
